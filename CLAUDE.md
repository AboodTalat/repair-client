# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This file scopes guidance to the `repair/` frontend. The root `RepByRep/CLAUDE.md` covers the backend (`Server/`) and the sibling `client/` app — read it first when a task crosses the wire, since the GraphQL contract, auth model, and resolver inventory all live on the server side.

## What this app is

`repair/` is the **e-commerce frontend** for the "repair" clothing-store system described in the project brief. It is the **only** frontend for that system — there is no separate admin app. The four user roles in the requirements (Customer, Admin, Delivery, Accounting) are all served by this same Next.js app, gated by role at runtime. The sibling `client/` directory is a different product (RepByRep forms); do not import from it or copy patterns out of it.

This app talks exclusively to the **`repair` sub-server** at `POST /repair/graphql`. Do **not** point requests at `/form/graphql` — that is the formServer sub-server for the other product, with a different user model, different schema, and no e-commerce resolvers.

## Current state

Auth and marketing surfaces are largely presentational. The landing header fetches live data from the backend (the first GraphQL call wired up from this app). The customer storefront's `/shop` page is built end-to-end as a presentational shell against mock data — every state from Figma is implemented (default grid, sale/discount cards, empty state, "coming soon" overlay, filter sheet, add-to-cart sheet) but no backend call yet; the data source is `src/lib/mockShop.js` and will swap to `repairQuery("myAppListProducts", …)` when the storefront resolvers land. The **admin console** is scaffolded end-to-end as presentational UI against `src/lib/mockAdmin.js` — **16 pages** under `(admin)/r3pr-console/*` (dashboard, categories, products, **taxonomies**, orders, stock-alerts, messages, reports, **wishlist-insights**, **storefront** content, **subscribers**, promo-codes, users, discounts, broadcast, settings). **The Users page (`/r3pr-console/users`) is fully wired to the backend** via `repairCall` — it calls `myAppAdminListUsers` (query with role/search filters), `myAppAdminCreateUser`, `myAppAdminUpdateUser`, `myAppAdminToggleUserActive`, and `myAppAdminDeleteUser`. The UI adapts to the `users` model shape (email + phone + role + is_active + created_at — there is no `name` or `marketingOptIn` field on the backend model). **The Categories page (`/r3pr-console/categories`) is fully wired** — fetches the full tree via `myAppListCategoriesTree({ includeHidden: true })` (enriched with `product_count` per category), CRUD via `myAppAdminCreate/Update/DeleteMajorCategory` and `myAppAdminCreate/Update/DeleteSubCategory`, reorder via `myAppAdminReorderCategories`, visibility toggle via the update resolvers. Each category (major + sub) has two display flags surfaced as inline quick-toggles + editor toggles + row badges: **Active** (the `is_visible` flag, relabeled — off = hidden from the storefront entirely) and **Coming soon** (the new `coming_soon` flag — when active + coming soon, the storefront shows it as a non-shoppable teaser; `storeNav.js` maps it to `active:false` AND exposes a `comingSoon` flag on every nav node, so it routes through the existing coming-soon overlay). The storefront nav surfaces (`shared/Sidebar.jsx` drilldown + root, `public/homePage/Header.jsx` + `customer/shop/ShopHeader.jsx` desktop dropdowns) render `comingSoon` categories/subs greyed + non-clickable with a right-aligned `shared/ComingSoonTag.jsx` "COMING SOON" label (Figma node 198:4804) — active items keep their existing styling, nav layout is otherwise unchanged. **The Taxonomies page (`/r3pr-console/taxonomies`) is fully wired** — Materials panel lists/creates/updates/deletes via `myAppListMaterials` + `myAppAdminCreate/Update/DeleteMaterial`; Sub-Category Types panel reads from `myAppListCategoriesTree({ includeHidden: true })`. **The Products page (`/r3pr-console/products`) is fully wired** — lists via `myAppListProducts({ includeHidden })` with search + category filter + total_stock enrichment (list items also carry `extra_sub_category_ids` / `extra_major_category_ids`); the list's Category column (a `CategoryCell`) shows the primary placement (dark, non-removable) plus removable extra sub/major tags and an inline "+ Assign category" picker that calls `myAppAdminSetProductCategories` directly (optimistic, reverts on failure) so admins can attach/detach additional categories without opening the editor; Color/Size panels CRUD via `myAppAdminListColors`/`ListSizes` + Create/Delete; product detail via `myAppGetProductDetail` (enriched with `material_ids`/`collection_ids` + `extra_sub_category_ids`/`extra_major_category_ids`); multi-step save flow creates/updates the product then diffs variants, syncs images (real UploadThing uploads via `@uploadthing/react`), sets material tags and collection tags, and sets additional categories via `myAppAdminSetProductCategories` (the "Additional categories" card on the Details tab — a product keeps one required PRIMARY sub-category but can also be listed under extra sub-categories and featured directly under major categories; the backend stores only the extras and unions the primary back in on read, so pricing/discounts stay tied to the primary); composition stored as formatted string in `material` column; single `lowStockThreshold` applied to all variants on save. All other admin pages remain against mock data. The **accountant ledger** is scaffolded as **3 read-only pages** under `(accountant)/r3pr-ledger/*` (overview / export history / audit log) against `src/lib/mockFinance.js`. The URL prefixes `/r3pr-console` and `/r3pr-ledger` are **deliberately obscure** so neither role surface is discoverable by URL guessing — keep them that way; don't rename to `/admin` or `/finance`.

```
src/app/
  (auth)/
    layout.js             ← white-bg split shell (HeroPanel + form column, md:w-[40%]). Wraps children in `<GuestGuard>` so any returning, already-logged-in user lands at their role's home instead of seeing the auth form. The guard covers all 5 auth routes: sign-in / sign-up / email-sent / reset-password / complete-profile. `/complete-profile` is intentionally inside the guard — during Google signup the user has no DB row yet so `isLoggedIn === false` and they pass through; the guard only kicks in if a logged-in user wanders to that URL by accident.
    sign-in/page.js       ← reads `?next=` from searchParams and forwards it to the "Sign Up" link (`/sign-up?next=…`) so the round-trip survives if the user switches between sign-in and sign-up. Same for desktop AuthHeader link.
    sign-up/page.js       ← same `?next=` forwarding back to the "Sign In" link. Both SignInForm and SignUpForm already call `postAuthDestination({ searchParams, role })` after success, which honors `?next=`.
    email-sent/page.js    ← "enter email to receive reset link"
    reset-password/page.js
  (customer)/
    layout.js             ← white shell with sticky ShopHeader + dark ShopFooter (footer is `hidden md:block` — desktop-only, to match Figma mobile which has no footer); used by every customer-facing page past the public landing. **No AuthGuard here** — storefront pages (shop, products, cart, checkout, contact) are accessible to guest (unauthenticated) users. Only the `/account/*` subtree is auth-gated (see `account/layout.js`).
    shop/page.js          ← server component, reads searchParams; routes to CategoryPicker when no ?category=, otherwise ShopPageClient
    products/[slug]/page.js ← server component, hands a MOCK_PRODUCT to <ProductPageClient> for now; will swap to `repairQuery("myAppGetProductDetail", { slug })` once that resolver lands
    account/
      layout.js           ← wraps all `/account/*` routes in `<AuthGuard>`. This is the only auth gate in the `(customer)` group — everything else under `(customer)/` is public.
      page.js             ← server component, the customer account hub. Renders Personal Information + Payment Methods + Address Information sections in one screen by handing PROFILE / PAYMENT_METHODS / ADDRESSES to AccountClient. Addresses are managed here — there is no separate `/account/addresses` page.
      orders/page.js      ← server component, reads ?status=<csv>&date=<slug>; runs `filterOrders(ORDERS, …)` and hands the filtered list + parsed filters to OrdersPageClient
      orders/[id]/page.js ← **"use client"** order tracking page (#13). Uses React's `use(params)` to unwrap the dynamic id, resolves via `findOrder(id)` in `mockOrders.js`, renders `<OrderTrackingPage order={...} />`. Inline "Order not found" fallback (back-to-orders CTA) when the id doesn't match — does NOT use Next's `notFound()` because the route was hitting 404 issues on first cold load before the client-component switch.
      wishlist/page.js    ← server component, reads ?type=<csv>&price=<slug>; runs `filterWishlist(WISHLIST, …)` and hands the filtered list + totalCount + parsed filters to WishlistPageClient
    contact/page.js       ← server component, narrow centered eyebrow heading + <ContactForm />. Figma mobile 66:2449 only — no separate desktop frame, so the mobile design is centered and gently scaled up at md/lg. Linked from Sidebar's `contactHref="/contact"`. Public (no auth required).
    cart/page.js          ← server component, hands off to <CartPageClient />. Pure presentational against `src/lib/mockCart.js` for now — swap to `repairQuery("myAppGetMyCart")` once the storefront cart-read resolver lands on `cart.ts` (toggle/add/remove primitives are already there). Tax formula in `mockCart.js` is **9% on `subtotal + shipping`** (no discount subtraction) so the seeded JOD 186 / JOD 8 cart computes the same `JOD 17.46 → JOD 192.86` as Figma 83:5144. **Guest cart CTA**: when the user is not logged in, the mobile `StickyCheckoutBar` and the desktop `OrderTotalsBlock` replace the "Continue to Next Step" button with a dual-CTA: "SIGN IN TO CONTINUE" (links to `/sign-in?next=/cart`) + "CONTINUE AS A GUEST" (proceeds to `/checkout`). Figma 451:4827.
    checkout/page.js      ← server component, hands off to <CheckoutDetailsClient />. Step 2 of the cart→details→payment flow (Figma mobile 82:3618 + desktop 119:5592). Adds the contact-info / shipping-address / delivery-method sections on top of the shared cart chrome (stepper + free-shipping banner + items + promo + order totals + trust + payment + support). Reads from `CHECKOUT_CONTACT` / `CHECKOUT_ADDRESSES` / `SHIPPING_METHODS` in `mockCart.js`. Mounted under (customer)/.
    checkout/payment/page.js ← server component, hands off to <PaymentPageClient />. Step 3 of the checkout flow (Figma mobile 84:6733 + desktop 119:5877). Payment-method radio list (cards + Apple Pay + Google Pay + Cash on Delivery), read-only shipping-address display, T&C checkbox gating the place-order CTA. Reads from `PAYMENT_METHODS` + `CHECKOUT_ADDRESSES` in `mockCart.js`. Mounted under (customer)/.
    checkout/success/page.js ← server component, hands off to <OrderSuccessClient />. Post-checkout success screen reached when `PaymentPageClient`'s "PAY & CONFIRM ORDER" CTA fires (Figma mobile 85:8692 + desktop 119:6418). Renders the dark check badge + "Order Successful!" heading, an "Apply for coaching" promo card (with the real coach image at `/public/cart/coaching-model.png` — downloaded from Figma node 128:6031 to dodge the 7-day asset URL expiry), the order-info card (#ORD-2847391 + bag tile + Estimated Delivery + Total + Payment Method), the items list, and the shipping-address card. Mounted under (customer)/.
    checkout/failed/page.js ← server component, hands off to <OrderFailedClient />. Payment-declined screen (Figma mobile 111:3587 + desktop 119:6627). Dark X badge + "Payment Declined" heading, two-section Payment Error card (red-tinted top with reason + bottom meta rows for Transaction ID / Attempted Amount / Card Ending), items list, and a 3-CTA stack: `TRY ANOTHER METHOD` / `RETRY PAYMENT` / `RETURN TO CART`. Desktop right column swaps to an Action Card with the "ACTION REQUIRED" eyebrow + "Payment Failed" heading + Visa-chip Attempted Method row + Total Amount + the same CTAs, plus a help box with two pipe-separated "Contact Bank Support | Chat with Us" links. Mounted under (customer)/.
  (admin)/
    layout.js             ← wraps every admin page in <AdminShell> (sticky dark SideNav + white TopBar + #f7f7f8 content panel); sets metadata.robots = noindex,nofollow
    r3pr-console/
      page.js             ← redirect("/r3pr-console/dashboard")
      dashboard/page.js   ← KPIs, sales LineChart, revenue Donut, top products, low-stock alerts; renders RecentOrdersTable (client child) — see note on the function-prop boundary
      categories/page.js  ← <CategoryManager /> — **WIRED TO BACKEND** (myAppListCategoriesTree / AdminCreate/Update/DeleteMajorCategory / AdminCreate/Update/DeleteSubCategory / AdminReorderCategories)
      products/page.js    ← <ProductManager /> — **WIRED TO BACKEND** (myAppListProducts / AdminListColors / AdminListSizes / GetProductDetail / AdminCreate/Update/DeleteProduct / AdminCreate/Update/DeleteProductVariant / AdminCreate/Update/DeleteProductImage / AdminSetProductMaterials / AdminSetProductCollections + real UploadThing image uploads)
      taxonomies/page.js  ← <TaxonomyManager /> — **WIRED TO BACKEND** (Materials: myAppListMaterials / AdminCreate/Update/DeleteMaterial; Sub-Category Types: myAppListCategoriesTree read-only panel)
      orders/page.js        ← <OrderManager />
      stock-alerts/page.js  ← <StockAlertManager /> — pending "notify when available" requests
      reports/page.js       ← <ReportsView />
      wishlist-insights/page.js ← <WishlistInsightsManager /> — most-wishlisted products + per-product / bulk Notify CTAs
      storefront/page.js    ← <StorefrontManager /> — content editor for Hero / Colorways / Stats / Browse tiles / Product page sections / Coaching CTA / Footer; each section has a Show preview toggle
      subscribers/page.js   ← <SubscribersManager /> — newsletter list with status chips + search + CSV export + delete
      promo-codes/page.js   ← <PromoManager /> — **WIRED TO BACKEND** (myAppAdminListPromoCodes / AdminCreate/Update/DeletePromoCode)
      users/page.js         ← <UserManager /> — **WIRED TO BACKEND** (myAppAdminListUsers / CreateUser / UpdateUser / ToggleUserActive / DeleteUser)
      discounts/page.js     ← <DiscountManager />
      broadcast/page.js     ← <BroadcastComposer />
      messages/page.js      ← <ContactManager />
      settings/page.js      ← <SettingsManager />
  (accountant)/
    layout.js             ← wraps every accountant page in <AccountantShell> (sticky dark SideNav + white TopBar + #f7f7f8 content panel — forks the admin shell shape but with a finance-only nav and no NotificationPanel); sets metadata.robots = noindex,nofollow
    r3pr-ledger/
      page.js             ← redirect("/r3pr-ledger/overview")
      overview/page.js    ← server component, renders <PageHeader /> from admin + <FinanceOverview /> client child
      exports/page.js     ← server component, renders <PageHeader /> + <ExportHistory /> client child
      audit/page.js       ← server component, renders <PageHeader /> + <AuditLog /> client child
  (deliveryCompany)/
    layout.js             ← wraps every delivery page in <DeliveryShell> (sticky dark SideNav + white TopBar + #f7f7f8 content panel — forks the accountant shell shape with a delivery-only single-item nav and an "On shift" pill instead of read-only); sets metadata.robots = noindex,nofollow
    r3pr-dispatch/
      page.js             ← redirect("/r3pr-dispatch/dashboard")
      dashboard/page.js   ← server component, renders <PageHeader /> from admin + <DeliveryDashboard /> client child
      orders/[id]/page.js ← **"use client"** order detail shell (uses React's `use(params)` to unwrap the dynamic id). Renders <OrderDetailPage params={params} />. Inline "Order not found" fallback (back-to-assigned-orders CTA) when the id doesn't match — does NOT use Next's `notFound()`, same reason as the customer order tracking page (see dynamic-route convention in Conventions).
  layout.js               ← root: loads Geist + Zalando_Sans + Zalando_Sans_Expanded; sets html h-full / body min-h-full flex-col. Wraps `{children}` in `<StoreProvider>` (from `src/components/shared/StoreProvider.jsx`) so every page below has access to the Zustand store and gets its initial cart/wishlist sync on mount.
  page.js                 ← public marketing landing (NOT under a (customer) group); renders <HeaderShell /> at the top
  globals.css             ← Tailwind v4 import + @theme tokens (font-display = Zalando Expanded, font-body = Zalando Sans) + splash keyframes

src/lib/
  repairApi.js            ← thin POST helper for /repair/graphql, **server-side only** — uses `next.revalidate` ISR caching (default 300s), no auth header. Used from server components (e.g. `HeaderShell.jsx`). Accepts a `tags` option for on-demand invalidation: the category-tree fetches in `storeNav.js` are tagged `CATEGORY_CACHE_TAG` ("repair-categories"), and the admin Categories page calls the `revalidateStorefrontCategories` server action (`categoryActions.js` → `revalidateTag`) after every category mutation so a newly added/edited category surfaces on the next storefront refresh instead of waiting out the 300s window. Without this the nav looked stale for up to 5 minutes after an admin change.
  repairClientApi.js      ← **client-side** GraphQL transport. Two exports: `graphqlFetch(operationName, variables, { token, isQuery })` (raw call, caller supplies the token) and `graphqlFetchWithRetry(operationName, variables, { getToken, refresh, isQuery })` (auto-refreshes once on a 401). Store-free by design so the dependency direction stays one-way (useRepairStore → repairClientApi); the refresh closure is passed in by the caller. Synthesises `.status === 401` on application-level `"Un Authenticated User"` failures (the repair `isAuth` middleware is non-blocking, so UNAUTH arrives as HTTP 200 with `blnRequestSuccessful: false` — without this synthesis the retry layer would never trigger). URL from `NEXT_PUBLIC_REPAIR_GRAPHQL_URL` env, falling back to `/repair/graphql`.
  repairAuthedApi.js      ← **high-level** client API. Exports a single function `repairCall(operationName, variables, { isQuery })` that wires `graphqlFetchWithRetry` to the Zustand store's `authInfo.token` + `refreshAuth()`. **Use this from every client component / event handler that needs an authed call** — it's the single API surface so 401-refresh-retry behaviour stays uniform across the app. Lives in its own file (not in `repairClientApi.js`) because it imports from `useRepairStore.js`, which already imports from `repairClientApi.js` — separating them avoids a circular import.
  useRepairStore.js       ← Zustand store for global client state. Five persisted slices + one transient: **authInfo** (JWT + refresh token + user — persisted, AES-encrypted), **cartInfo** (item count + last-synced timestamp — persisted, lightweight cache; cart items live server-side), **wishlistInfo** (product-id list + last-synced — persisted, for instant heart-icon state), **lastOrder** (most recent successfully-placed order — persisted so `/checkout/success` survives a refresh), **checkoutInfo** (in-progress checkout selections — NOT persisted, stale promo codes are bad UX), and **sessionExpired** (transient boolean — flips true when refresh fails or a sibling tab broadcasts session death; StoreProvider subscribes and triggers the toast + `?next=` round-trip). Encrypted via CryptoJS AES-256 in localStorage under key `RepairStore_v1`; key from `NEXT_PUBLIC_STORAGE_SECRET_KEY` (obfuscation, not real secrecy — real security is server-side refresh-token rotation). `skipHydration: true` + manual `rehydrate()` from StoreProvider to avoid SSR mismatch. Module-level `_refreshInFlight` single-flight lock so concurrent 401s share one refresh Promise — without it, a second in-flight refresh would reuse an already-rotated token and the server would revoke the entire token family.

  **Auth lifecycle actions** (the important distinction): `setAuthInfo({ token, refresh_token, user })` after login/signup/Google/refresh (also broadcasts to sibling tabs); `clearAuth()` is the **explicit-logout** path — wipes EVERYTHING user-scoped (auth + cart + wishlist + lastOrder + checkout) because the next person on this device may be someone else; `handleSessionExpired()` is the **forced-sign-out** path (refresh failed, token reuse detected server-side, account deactivated, 30-day refresh window finally elapsed) — wipes only the auth slice and **preserves cart/wishlist/lastOrder so the user signs back in and finds their basket intact**, and sets `sessionExpired: true` so StoreProvider surfaces the toast and redirects with `?next=`. `refreshAuth()` returns the new token or null and now calls `handleSessionExpired` on failure (NOT `clearAuth`) — never reintroduce `clearAuth` here or the user loses their cart on every 30-day rotation timeout.

  **Proactive refresh**: `maybeProactiveRefresh()` peeks at the JWT `exp` claim (read-only — server is the only place that can trust the signature) and fires `refreshAuth` only when fewer than `PROACTIVE_REFRESH_WINDOW_SEC` (5 min) remain. Cheap to call. StoreProvider wires it to `visibilitychange` / `focus` / `online` and to the post-rehydrate sync; sensitive flows (checkout) can call it manually before opening a form to dodge mid-action 401s. **Keep `PROACTIVE_REFRESH_WINDOW_SEC` comfortably below the server's `ACCESS_TOKEN_TTL` (currently 30 min)** — if you ever cut the server TTL, also drop the window here.

  **Cross-tab auth sync via `BroadcastChannel("repair-auth")`**: every `setAuthInfo` / `clearAuth` / `handleSessionExpired` posts a message; sibling tabs receive it through `initRepairStoreListener` and apply via `applyAuthInfoFromBroadcast` / `applySessionExpiredFromBroadcast` (the applier paths deliberately DO NOT rebroadcast, which prevents echo storms in 3+ tab setups). Without this, tab A refreshing would leave tab B holding the old refresh token; B's next 401 would refresh too, reusing A's already-rotated token, and the server's reuse-detection would revoke the entire family — silently logging the user out everywhere. The single-flight lock alone is not enough because tabs don't share JS scope; the channel is what keeps them coherent.

  Cart/wishlist actions: `syncCart()` / `syncWishlist()` pull from server via `graphqlFetchWithRetry` so 30-minute access-token expiry is transparent; `incrementCartCount` / `decrementCartCount` (optimistic), `toggleWishlistItem(id)` (optimistic), `setLastPlacedOrder(order)` / `clearLastOrder()`, `applyPromoCode` / `clearPromoCode`, `setSelectedAddress` / `setShippingMethod` / `setSelectedPaymentMethod`, `clearCheckout()` (does NOT touch `lastOrder`), `resetStore()` (hard reset + clears encrypted localStorage), `clearSessionExpired()` (call after the UI has handled it), `getAccessTokenSecondsLeft()` (read-only helper). Selectors: `selectIsLoggedIn` / `selectUser` / `selectToken` / `selectCartCount` / `selectWishlistIds` / `selectCheckoutInfo` / `selectAppliedPromoCode` / `selectLastPlacedOrder` / `selectSessionExpired`. Also exports `initRepairStoreListener()` which watches for cross-tab + same-tab `localStorage.removeItem` AND attaches the BroadcastChannel receiver (one listener, two responsibilities). **Login flows must call `setAuthInfo(data)` AND `syncCart()` + `syncWishlist()`** so badge counts populate immediately — StoreProvider only fires that pair on page mount + post-rehydrate.
  authRedirect.js         ← single source of truth for the `?next=` round-trip used by every auth-redirect flow (AuthGuard → /sign-in, session-expired toast → /sign-in, sign-in / sign-up / Google / complete-profile → original destination, guest cart CTA → /sign-in?next=/cart, sign-in ↔ sign-up cross-links). Exports `homeForRole(role)` (admin → /r3pr-console, delivery → /r3pr-dispatch, accounting → /r3pr-ledger, customer → /shop — the canonical post-auth landing per role), `isSameOriginPath(p)` (open-redirect guard: rejects empty strings, non-`/`-leading paths, `//evil.com`-style protocol-relative URLs, and `/\\evil.com`-style IE-quirk URLs), `readNextParam(searchParams)` (returns the validated `?next` value or null), `buildSignInRedirect(here)` (encodes the path into `/sign-in?next=…`), and `postAuthDestination({ searchParams, role })` (the success-side resolver — honors `?next=` if safe, falls back to the role's home). **Seven places use this; if you add another auth-redirect flow, import from here rather than re-implementing the validation** — a bypass introduced in one form would otherwise widen the open-redirect surface for the others.
  mockShop.js             ← in-process mock catalogue for /shop. Exports MAJOR_CATEGORIES (Discover All / Just Dropped / Women / Men / Sale), ALL_PRODUCTS + SALE_PRODUCTS (with `salePrice` and optional `labels: string[]` — `["Best Seller", "Most Popular", "New Arrival", "Limited", "Low Stock"]`), `getCategory(slug)`, `listProducts({ category, sub, filters })`, `ACTIVE_FILTER_COUNT(filters)`, and FILTER_OPTIONS (numericSizes 36-48 / sizes S-XXL / 10 colors / 5 materials / `types`). **`FILTER_OPTIONS.types` is derived at module load from `MAJOR_CATEGORIES.subs`** (deduped sub-category names) — sub-categories ARE the "type" facet on the storefront. Don't reintroduce a hardcoded `types` array; if you need to surface more types, add sub-categories on `MAJOR_CATEGORIES` (or via the Categories admin page once wired). Swap to a repairQuery call when the backend exposes a storefront list resolver.
  mockOrders.js           ← in-process mock data for the customer `/account/orders` page AND the per-order `/account/orders/[id]` tracking page. Exports ORDERS (6 sample rows; each row now also carries `history`, `estimatedDelivery`, `shippingAddress`, `courier`, `trackingNumber` so the tracking page renders end-to-end), `badgeFor(status)` (collapses processing/prepared/handed_to_delivery — plus legacy pending/dispatched — into "On the way"), `ORDER_STATUS_OPTIONS` (4 multi-select chips — `delivered` / `on-the-way` / `cancelled` / `returned`; each carries `rawStatuses` so one chip covers all matching server-side statuses, **including the 4-step pipeline `processing` / `prepared` / `handed_to_delivery` / `delivered`**), `ORDER_DATE_RANGES` (single-select: `all` / `30d` / `6m` / `year`), `filterOrders(orders, { statuses, dateRange })`, `ACTIVE_ORDER_FILTER_COUNT(filters)`, **`findOrder(id)`** (resolves the dynamic `[id]` route), **`TRACKING_PIPELINE`** (the 4-step pipeline as `[{key, label, description}]`), and **`trackingProgress(status)`** → `{ stepIndex, terminal: "cancelled"|"returned"|null }` for the timeline UI. Swap to `repairQuery("myAppListMyOrders", …)` + `repairQuery("myAppGetMyOrder", { id })` once the customer-scoped order resolvers land on `orders.ts`.
  mockWishlist.js         ← in-process mock data for the customer `/account/wishlist` page. Exports WISHLIST (4 sample rows; each carries product display info + a swatch palette + a `type` matching FILTER_OPTIONS.types from mockShop), `WISHLIST_TYPE_OPTIONS` (multi-select pills sourced from `FILTER_OPTIONS.types`), `WISHLIST_PRICE_RANGES` (single-select preset: `all` / `under30` / `30to50` / `over50`), `filterWishlist(items, { types, priceRange })`, and `ACTIVE_WISHLIST_FILTER_COUNT(filters)`. **Filter axes (TYPE + PRICE) are invented** — there's no Figma drawer spec for wishlist; swap them when one lands. Swap to `repairQuery("myAppListMyWishlist", { filters })` once the customer-scoped wishlist list resolver lands on `cart.ts` (toggle/clear primitives are already there).
  mockAccount.js          ← in-process mock data for the customer `/account` hub. Exports PROFILE (email / phone / DOB), PAYMENT_METHODS (`{id, brand, last4, expiry, isDefault}`), and ADDRESSES (`{id, label, kind ("home"|"office"|"other"), line, phone, isDefault}` plus the structured `country`/`city`/`neighborhood`/`street`/`building`/`apartment` fields the edit drawer reads back). Swap to `repairQuery("myAppGetMyAccount", …)` + customer-scoped address/payment-method resolvers as they land — the address model exists in `addresses.ts` on the server; payment-methods is not yet modelled (decision pending: Stripe-managed vs. local PCI-scoped table).
  countryCodes.js         ← ISO 3166-1 alpha-2 country list (~245 rows) with `{iso2, name, dial}`. Exports `flagEmoji(iso2)` which derives the flag at render time from regional-indicator codepoints (no static flag table), and `DEFAULT_COUNTRY` (Jordan / +962 — the project's home market, matches `JOD` pricing throughout the storefront). Also exports **`phoneLengthFor(iso2)`** and the underlying **`PHONE_LENGTHS`** map keyed by ISO2: returns `{ min, max }` for the digit count expected AFTER the country code (Jordan / Saudi / UAE = 9 exactly; US / Canada / UK / Iraq / Egypt / India = 10 exactly; China = 11 exactly; Germany = 10–11; etc.). About 50 countries are mapped (MENA / Gulf / EU / NA / big APAC / key SSA); unmapped countries fall back to `DEFAULT_PHONE_LENGTH = { min: 7, max: 14 }` — the E.164 practical range (the dial code takes 1–3 of the 15-digit ceiling). Consumed by the auth `PhoneInput` (for `maxLength`) and by every form that validates a phone (SignUpForm / CompleteProfileForm / ContactForm). **When validating, strip the leading 0 trunk prefix and any duplicated dial code BEFORE measuring length** — otherwise a Jordan-style "0791234567" silently fails (the 0 inflates the count by one, the truncation drops a real digit, and the wire value ends up one digit short).
  mockCart.js             ← in-process mock data + pricing helpers for the cart + checkout flow. Exports CART_ITEMS (3 sample rows), PROMO_CODES (`SUMMER25` + `FIRST15`) + SUGGESTED_PROMOS, `formatJOD`, `calcSubtotal` / `calcDiscount` / `calcShipping` / `calcTotals` (the canonical 9%-on-`subtotal+shipping` tax formula matches Figma's JOD 186 → JOD 192.86), FREE_SHIPPING_THRESHOLD (200 JOD) + SHIPPING_FEE (8 JOD flat below the threshold). **Checkout-flow extensions**: CHECKOUT_CONTACT (email + dial-code + phone), CHECKOUT_ADDRESSES (Home + Office rows, each carrying both the flat `line` display string AND the structured country/city/neighborhood/street/building/apartment fields the `AddAddressDrawer` reads back), SHIPPING_METHODS (Standard / Express / Pickup) + DEFAULT_SHIPPING_METHOD_ID, PAYMENT_METHODS (Visa + Mastercard cards + Apple Pay + Google Pay + Cash on Delivery) + DEFAULT_PAYMENT_METHOD_ID, and `buildAddressLine(parts)` — rebuilds the flat `line` string from the structured drawer-submit fields (mirrors the pattern in customer/account/AccountClient). Swap to `repairQuery("myAppGetMyCart", ...)` + customer-scoped address / payment-method resolvers as they land on the server.
  mockFinance.js          ← in-process mock data + helpers for **every** accountant page (`/r3pr-ledger/overview`, `/exports`, `/audit`). Derives from `mockAdmin.js` where possible so a single edit there propagates here. Exports **FINANCE_DAILY** (30 rows synthesised from `SALES_SERIES_30` — each `{ day, revenue, prevRevenue, orders, prevOrders, discount, prevDiscount, net, prevNet }`; AOV ≈ JOD 75, discount ≈ 14% of gross), **DISCOUNT_BY_CODE** (joined to `PROMO_CODES` from mockAdmin + 2 synthetic codes, sorted by impact), **REVENUE_BY_PRODUCT** (extends `TOP_PRODUCTS` with 5 more rows so the product breakdown table doesn't read empty), **EXPORT_HISTORY** (9 sample export rows — `{ id, fileName, format ("PDF"|"CSV"), report, range, generatedBy: {name, email}, generatedAt (ISO), status ("ready"|"generating"|"expired"), sizeKb }`) + **EXPORT_STATUS_TONE** (pill tone map keyed by status) + **`formatBytes(kb)`**, **AUDIT_LOG** (16 sample rows — `{ id, kind, actor: {name, email}, detail, target, occurredAt (ISO), ip }`) + **AUDIT_ACTION_KINDS** (`{view, export, filter, signin, signout}` → `{label, bg, fg}` tone map) + **`formatAuditTime(iso)`** → `{date, time}`, and helpers `aggregate(rows)` / `pctDelta(curr, prev)` / `rollupBy(rows, "day"|"week"|"month")` (the rollup parses the implicit-2026 `"May 3"`-style day labels and buckets to ISO-week-ish or `Mon YYYY` keys). Re-exports `REVENUE_BY_CATEGORY` + `formatCurrency` + `formatNumber` from mockAdmin so the pages only import from one lib. Swap to `repairQuery("myAppFinance…", { from, to })` / `myAppFinanceListExports` / `myAppFinanceAuditLog` calls when the finance resolvers land — `Server/servers/repair/src/graphql/resolvers/reports.ts` already returns aggregated JSON close to this shape, so wiring is mostly renaming fields.
  mockDelivery.js         ← in-process mock data + helpers for the delivery role surface (`/r3pr-dispatch/*`). Exports **ASSIGNED_ORDERS** (6 sample rows — 3 currently `handed_to_delivery`, 2 `delivered`, 1 `failed_delivery`; each row carries `customer: {name, email, phone}` + `address` + `addressNote` + `items` + `paymentMethod` + `codAmount` + `status` + `total` + `placed` + `assignedAt` + `history` timeline, plus `deliveredAt` / `failedAt` / `failedReason` / `failedNote` when terminal), **DELIVERY_STATUSES** (3 keys: `handed_to_delivery` → "Out for Delivery" / `delivered` / `failed_delivery`), **DELIVERY_FILTERS** (chip definitions for the dashboard's status filter — each carries a `match: string[]` so one chip can collapse multiple raw statuses), **FAILED_DELIVERY_REASONS** (6 reasons: customer_unavailable / wrong_address / refused / unreachable / damaged_package / other — drives the failed-delivery picker modal), **DELIVERY_TONE** (badge color map; **introduces the new `failed_delivery` tone** `#fef2f2`/`#b91c1c`/`#ef4444` that is NOT yet in `STATUS_TONE` on mockAdmin — local to this surface until the status is added project-wide), **DRIVER** (`{email, name, initials, zone}` — the signed-in delivery account; matches the `by:` field on history entries), plus helpers `filterDeliveries(rows, filterKey)` / `findDelivery(id)` / `deliveryCounts(rows)` / `itemsSummary(items)` / `reasonLabel(key)` / `deliveryStatusLabel(key)` / `deliveryTone(key)` / `formatJOD(n)`. Swap to `repairQuery("myAppListMyDeliveries", { filter })` + `repairQuery("myAppGetMyDelivery", { id })` + `repairQuery("myAppDeliveryUpdateStatus", { orderId, status, reason?, note? })` once the delivery resolvers land on a new `delivery.ts` file — none exist yet, see Gaps.
  mockAdmin.js            ← in-process mock data for every admin surface. Exports KPIS, SALES_BY_DAY (7-day, used by the dashboard), **SALES_SERIES_30** (30-day, used by the reports Sales tab — each row `{ day, current, prev }` for comparison-line support), **SIGNUPS_SERIES_30** (30-day customer sign-ups, same `{ day, current, prev }` shape, used by the reports Customers tab), REVENUE_BY_CATEGORY, TOP_PRODUCTS, RECENT_ORDERS, LOW_STOCK, MAJOR_CATEGORIES + SUB_CATEGORIES, COLORS, SIZES, PRODUCTS (with per-variant qty + `subtitle` string + `composition` array of `{ pct, material }` rows + `labels: string[]` + `materialTags: string[]` + `collections: string[]`), ORDERS (with history timeline), PROMO_CODES, DISCOUNTS, USERS (admin/delivery/accounting/customer), BROADCAST_HISTORY, **CONTACT_MESSAGES** (9 sample messages; each row `{ id, firstName, lastName, email, phone, message, date, status }` where status ∈ `unread|read|replied|archived`), **STOCK_ALERTS** (8 sample notify-when-available requests; each row `{ id, customer: { name, email }, product, color, size, requestedAt, status }` where status ∈ `pending|notified|dismissed`), ORDER_STATUSES, STATUS_TONE (badge-color map keyed by status / role / payment state), plus `formatCurrency` / `formatNumber` helpers.

  **New exports backing the post-launch admin surfaces:**
  - **MATERIALS** — fabric tags (Cotton / Polyester / Nylon / Spandex / Elastane); editable on the Taxonomies page, multi-select on the product Filter facets card.
  - **COLLECTIONS** — curated tab inclusion (`col-sale`, `col-jd-weekly`, `col-jd-monthly`, `col-discover`); per-product checkbox list on the product Labels tab. **Product "type" is intentionally NOT a separate taxonomy** — the customer-facing type facet is the product's sub-category (see Catalog → Categories). The deprecated `PRODUCT_TYPES` export was removed.
  - **NEWSLETTER_SUBSCRIBERS** — 12 sample rows for `/r3pr-console/subscribers` (`{id, email, signedUp, status, source}`).
  - **Storefront content blocks**: `STOREFRONT_HERO`, `STOREFRONT_COLORWAYS` (per-row `{ id, image, imageAlt, badge, reversed, multiSwatch, ctaLabel, swatches: [{color, name, tagline}] }`), `STOREFRONT_STATS`, `STOREFRONT_BROWSE_TILES`, `STOREFRONT_PRODUCT_SECTIONS` (per-row `{ id, key, title, body, enabled }`), `STOREFRONT_FOOTER` (`{ brandCopy, social, columns }`). All drive `/r3pr-console/storefront`.
  - **COACHING_CTA** — `{ enabled, eyebrow, title, body, ctaLabel, ctaHref, image }` for the `/checkout/success` cross-sell card; editable on Storefront content page.
  - **WISHLIST_INSIGHTS** — top-wishlisted products with `wishlistCount` / `addedLast7Days` / `inStock` / `variantsLow`; powers `/r3pr-console/wishlist-insights`.
  - **PAYMENT_METHOD_SETTINGS** — per-method `{ enabled }` toggles surfaced on the Settings page (Visa/MC, Apple Pay, Google Pay, COD).
  - **TAX_SETTINGS** — `{ rate, inclusive, appliesToShipping }`.
  - **SHIPPING_METHODS** — `{ id, key, name, eta, enabled, removable }` for the per-method Settings editor (renames, ETA copy, on/off). Replaces the hardcoded list that used to live in `mockCart.js`'s `SHIPPING_METHODS` (which still exists for storefront flows but should follow this in time).
  - **PICKUP_LOCATIONS** — `{ id, name, address, hours }`; CRUD on the Settings page's Pickup Locations block, only shown when the Pickup shipping method is enabled.

  **ORDER_STATUSES has no `pending` entry** — the 6 statuses are `processing` / `prepared` / `handed_to_delivery` / `delivered` / `cancelled` / `returned`. Orders start at `processing` (history entries begin `from: null, to: "processing"`). STATUS_TONE entries for the order pipeline: `processing` blue `#1d4ed8`, `prepared` teal `#0d9488`, `handed_to_delivery` indigo `#4f46e5`, `delivered` green `#16a34a`; `dispatched` is kept as a legacy fallback tone only. STATUS_TONE entries for contact messages: `unread` blue, `read` grey, `replied` green, `archived` muted slate. STATUS_TONE entries for stock alerts: `notified` green (same tone as `replied`/`delivered`), `dismissed` muted slate (same tone as `archived`). STATUS_TONE for subscribers: `unsubscribed` muted slate. All tones are in the shared `STATUS_TONE` map so `StatusBadge` resolves them automatically. Swap individual sections to `repairQuery("myAppAdminXxx", …)` as the admin client wiring lands.

src/components/
  public/homePage/        ← landing-page sections
    SplashSection.jsx     ← 4-tile entry overlay, "use client", locks body scroll 3s then fades 700ms
    HeaderShell.jsx       ← async server component; fetches myAppListCategoriesTree (with dummy fallback), passes shaped tree to Header
    Header.jsx            ← "use client" — fixed translucent nav (bg-black/30 + backdrop-blur). Renders desktop nav + hover dropdown from `categories` prop, and forwards a sidebar item list to Sidebar. Also reads `selectIsLoggedIn` from the Zustand store and passes `isAuthenticated` + an `onSignOut` handler down — handler calls `myAppLogout` (mutation, with the stored `refreshToken` — best-effort, swallowed on failure) then `clearAuth()` then `router.push("/")`. Sidebar swaps "Sign in/Sign up" ↔ "Logout" off that flag.
    HeroSection.jsx
    ColorwaysIntro.jsx
    ColorwaySection.jsx   ← reusable, used 3× from page.js (Bright White multi-swatch, Deep Blue, Fresh Green)
    CraftedToLast.jsx
    StatsSection.jsx
    BrowseCollection.jsx
    Footer.jsx            ← white footer (contrasts the otherwise black landing); NOT used on /shop — see ShopFooter
    GlassButton.jsx       ← shared dark-glass CTA used by hero
  customer/shop/          ← shop surface (matches Figma file pLwxbw3UUexnNKG7lhFotU, frame sets 2065:4344 desktop + 15:810 mobile)
    ShopHeader.jsx        ← "use client" — white sticky header with scroll-shadow. Mobile: sort icon | RE logo (absolutely centered) | search + bag. Desktop: HOME/JUST DROPPED/WOMEN/MEN nav | RE logo | search + bag(+count badge) + account. Wires the same auth-aware Sidebar contract as the landing `Header.jsx`: reads `selectIsLoggedIn` + passes `onSignOut` (`myAppLogout` → `clearAuth()` → `router.push("/")`). **Desktop responsiveness:** the centered logo is dropped once there are ≥2 non-coming-soon categories (it would crowd the wider nav). And the desktop bar auto-collapses to the mobile layout (hamburger → Sidebar + centered logo + icons) when the horizontal nav would overflow — decided by measuring a hidden off-screen `MeasureNav` copy against the bar width (works both directions on resize), so long/many category labels never collide. Coming-soon nav items render greyed + a `shared/ComingSoonTag` "COMING SOON" label (see Categories admin / Figma 198:4804).
    ShopFooter.jsx        ← dark #11191f footer, 4 columns (Brand / Shop / Help / Newsletter) + bottom bar (copyright + Privacy / Terms / Cookies)
    FilterBar.jsx         ← "HOME/<CATEGORY>" breadcrumb + product count + Filter button. Mobile = small text link, desktop = outlined chip with inline 3-track "tune" SVG icon (vuesax-style: 3 horizontal lines + circular knobs at x=4.5 / 8.25 / 3.75 — see Figma 119:3876)
    FilterDrawer.jsx      ← "use client" — splits into MobileCard (bottom-anchored floating card matching AddToCartDrawer footprint; Figma 11:1797) + DesktopCard (full-height right-side floating drawer, 387px, 32px margins, radius-8; Figma 126:3792 → node 126:4056). Mobile = chips 24x24 / 1px border / 2px radius / footer side-by-side; desktop = chips 32x32 / 1.5px border / 3px radius / pills with 14px text / 16x16 price-slider handles / STACKED buttons (APPLY ALL filled on top, CLEAR ALL outlined below, h:48 each). Chip/swatch/pill components are parameterized so both breakpoints reuse them.
    ProductCard.jsx       ← "use client" — splits into MobileCard (compact prop, 176x264 image, 8x8 swatches, 24x24 plus button) + DesktopCard (326x489 image, 12x12 swatches @ left:16/bottom:16, 40x40 plus button @ right:16/bottom:16 with white/70 outline). Subtitle uses #78716c (stone-500). Sale layout: strikethrough JOD price + blue chip "JOD 15.99" below. **`LabelBadges`** renders an `absolute top-left` chip stack from `product.labels: string[]` using the `LABEL_TONES` map (`Best Seller` dark / `Most Popular` blue / `New Arrival` green / `Limited` purple / `Low Stock` red) — mounted on both card variants so admin Label edits propagate to the storefront. `ProductPageClient` has a matching `ProductLabelRow` rendered above the product name on both mobile + desktop.
    ProductGrid.jsx       ← renders two separate grids (mobile 2-col @ gap-x-2 gap-y-4 + desktop 4-col @ gap-6) with respective ProductCard variants — DO NOT use a single grid with display:contents (the wrapper propagates children to the grid container and breaks the layout)
    EmptyState.jsx        ← filter-remove icon (88x88 desktop / 48x48 mobile) + 22px Zalando Sans Expanded Medium "NO PRODUCTS WERE FOUND / TRY REMOVING A FILTER." (Figma 126:4397). flex-1 + items-center + justify-center so it fills the remaining vertical space below FilterBar
    ComingSoonOverlay.jsx ← grayscale + 8px-blurred product grid (8 cards both breakpoints: mobile 2x4 / desktop 4x2) + sticky centered overlay (104x104 timer icon + 28px Zalando Sans Expanded Medium "MORE PRODUCTS ARE COMING SOON. STAY TUNED."; Figma 198:5080). Sticky positioning keeps the overlay in the viewport center as the user scrolls through the tall mobile grid. Rendered without FilterBar — see ShopPageClient.
    CategoryPicker.jsx    ← rendered at bare /shop (no ?category= param). Grid of major-category tiles with image + name + tagline
    AddToCartDrawer.jsx   ← "use client" — quick-add dialog from the product-card plus button. Mobile: bottom-anchored card (361px wide, 24px above the viewport bottom; NO image, just COLOR / SIZE / QUANTITY / "ADD TO CART"; Figma 7:1358 → node 12:2286). Desktop: floating right-side card 387px wide / 32px margins top-right-bottom / radius-8 / WITH image + price; chip sizes 32x32 / 1.5px border / 3px radius (Figma 120:6999 → node 120:7368). Has its own minimal scrollbar via the `.drawer-scroll` class.
    SearchOverlay.jsx     ← "use client" — splits into MobileBody (full-screen white overlay; Figma 39:1525) + DesktopBody (fixed overlay slotted below the 80px sticky ShopHeader at top:80/inset-x-0/bottom:0, z-20 so the header's z-30 stays on top; Figma 120:5217). Mobile: 40px outlined input → "MOST POPULAR" / "RESULTS" rows (56x84 thumb + title/subtitle + 4 swatches + price). Desktop: 1px #11191F outlined search bar (radius 8, 17px T/B + 25px L/R, 0/4/12 shadow) with Zalando Sans Expanded Bold 18px uppercase input + 15x20 close-X on the right; "SEARCH RESULTS" / "MOST POPULAR" heading (Zalando Sans Expanded SemiBold 18/28, 0.45px tracking); 4-col grid (24px gap) reusing `ProductCard` DesktopCard (capped 8 cards empty / 12 filtering). Both variants share `query` state, scroll-lock, Escape-to-close, autofocus the visible input via `matchMedia("(min-width: 768px)")`, and linear 240ms fade via `.search-overlay`. Live-filters `listProducts({ category: "discover-all" })` by name + subtitle. No-results state on both breakpoints: centered "NO PRODUCTS WERE FOUND / TRY SEARCHING FOR SOMETHING ELSE." + "YOU MIGHT LIKE" 4 suggestions. Triggered by the magnifying-glass icon in ShopHeader on both mobile and desktop. Typed mobile text is Bold 14px #11191F (placeholder rule lives in globals.css).
    AddedToCartBanner.jsx ← "use client" — top toast that auto-dismisses after 3s (Figma 120:7466)
    ShopPageClient.jsx    ← "use client" — owns drawer + cart-banner state, lifts filter changes back into URL query params via next/navigation router. Hides FilterBar when `showComingSoon` is true so the blur extends from below the header to the footer. Render order: ComingSoonOverlay → EmptyState → ProductGrid.
  customer/account/       ← post-auth account surfaces (orders: Figma mobile 41:1420 + desktop 119:4406; wishlist: Figma mobile 41:1613 + desktop 119:4743)
    OrderCard.jsx         ← exports `MobileOrderCard` (drop-shadow card, 56x84 image, Zalando Sans Condensed body, h-32 buttons, 10px CTA text) + `DesktopOrderCard` (1px #f3f4f6 border + shadow, 96x144 image, Zalando Expanded Bold title/price, h-48 buttons, 14px CTA text). Split-by-variant per the customer/shop/ProductCard pattern because the title font family — not just size — differs. **`showTrackCta(status)` helper** decides whether a "Track Order" CTA shows above the Inquire / Buy Again row — true for processing / prepared / handed_to_delivery (plus legacy pending/dispatched), hidden for delivered / cancelled / returned. All three CTAs are real `<Link>` elements: Track Order → `/account/orders/${order.id}`; Inquire → `/contact`; Buy Again → `/products/${order.productSlug ?? ""}` (the nullish fallback keeps the link safe when an older mock row lacks a slug). When Track is shown, Buy Again de-emphasises to outlined so Track stays the primary action.
    OrderTrackingPage.jsx ← "use client" — full `/account/orders/[id]` detail. Header has a Back-to-orders breadcrumb + status badge, then a 4-step `Pipeline` (desktop = horizontal step line with connectors filling black to the current step / grey beyond; mobile = vertical step list). Terminal-state cards for `cancelled` / `returned`. Item summary + shipping/courier/tracking card row, then a reverse-chronological history list, then Inquire / Buy Again CTAs. Reads from `TRACKING_PIPELINE` + `trackingProgress(status)` in `mockOrders.js`.
    OrderStatusBadge.jsx  ← inline-SVG pill (no Figma asset URLs — those expire after 7 days). Tones: `delivered` #dcfce7/#166534 + check-circle, `on-the-way` #dbeafe/#1e40af + truck, `cancelled` #fee2e2/#991b1b + x-circle, `returned` #f3e8ff/#6b21a8 + return arrow. `size="sm"` for the mobile card (10px text), default for desktop (12px).
    OrderFilterDrawer.jsx ← "use client" — same footprint and motion as the shop FilterDrawer (mobile `.bottom-card`, desktop `.right-drawer`, shared `.drawer-backdrop`, local `useDelayedUnmount` hook, Escape-to-close). Sections: STATUS (multi pills from ORDER_STATUS_OPTIONS) + PURCHASE DATE (single-select pills from ORDER_DATE_RANGES). Mobile footer = side-by-side CLEAR ALL + APPLY (N) h-32; desktop footer = stacked APPLY ALL (filled) on top + CLEAR ALL (outlined) below, h-48.
    OrdersPageClient.jsx  ← "use client" — owns the Filter drawer's open/close state. Renders mobile single-column / desktop xl:3-col grid (lg:2-col between), shows "Filter (N)" with the active count, lifts filter changes back into URL query params via next/navigation (`?status=…&date=…`). Renders an EmptyOrders block when the filter set yields zero rows.
    WishlistCard.jsx        ← exports `MobileWishlistCard` (drop-shadow card, 56x84 image, 4 small 12x12 swatches with 0.75px rgba(17,25,31,0.10) border, 32x32 outlined heart button, h-32 buttons, 10px CTA text — Figma 41:1613 cards under 77:2248) + `DesktopWishlistCard` (1px #f9fafb border + drop shadow, 96x144 image, 24x24 swatches with 1.714px #e5e7eb border, 36x36 #e5e7eb heart button, h-48 buttons, 11px CTA text @ tracking 0.55px — Figma 119:4743 cards 126:3671 / 126:3697 / 119:4809 / 126:3723). Inlines the heart SVG (no Figma asset URLs). Split-by-variant per the OrderCard pattern.
    WishlistFilterDrawer.jsx ← "use client" — copy of OrderFilterDrawer's structure (same `.bottom-card` / `.right-drawer` / `.drawer-backdrop` classes, same local `useDelayedUnmount`, same chip primitive). Axes: TYPE (multi pills from `WISHLIST_TYPE_OPTIONS`, sourced from `FILTER_OPTIONS.types` in mockShop) + PRICE (single-select preset pills from `WISHLIST_PRICE_RANGES`). **Both axes are invented** — no Figma drawer spec for wishlist; swap when one lands.
    WishlistPageClient.jsx  ← "use client" — owns the Filter drawer's open/close state. Renders mobile single-column / desktop xl:3-col grid (lg:2-col between), shows "Filter (N)" with the active count, lifts filter changes back into URL query params via next/navigation (`?type=…&price=…`). EmptyWishlist branches by `totalCount > 0` — "NO ITEMS MATCH YOUR FILTERS" when filtered to zero vs "YOUR WISHLIST IS EMPTY" when there genuinely are none.
    AccountClient.jsx       ← "use client" — central state holder for the `/account` hub. Owns `paymentMethods` + `addressList` state and the open/close state for every drawer + toast on the page (add-card, delete-card, add/edit-address, delete-address, plus card-added / card-deleted / address-added / address-deleted / cannot-delete-default banners). Renders Personal Information + Payment Methods + Address Information cards once each, with `md:hidden` / `hidden md:flex` swapping the mobile vs desktop variants from a single component instance — **don't mount AccountClient twice per breakpoint** (creates two independent state instances, drawers and toasts fall out of sync). The default-toggle helper `setSoleDefault(rows, id)` enforces the at-most-one radio behaviour for both payment methods and addresses (see auto-memory `feedback_repair_default_toggle.md`). `cannotDeleteKind` is a `null | "card" | "address"` triple-state that doubles as visibility + which copy the shared `CannotDeleteDefaultBanner` should render.
    PersonalInformationCard.jsx ← exports `MobilePersonalInformationCard` + `DesktopPersonalInformationCard` plus the shared `SectionCard` + `SectionHeader` chrome (re-used by Payment + Address sections). Read-only on both breakpoints.
    PaymentMethodsCard.jsx  ← exports Mobile + Desktop variants. Each row renders a `BrandTile` (Visa / Mastercard inline glyphs — no Figma asset URLs because those expire in 7 days), name + (Default) chip, expiry, star toggle, and trash. ADD MORE header link triggers the add-card drawer.
    AddressInformationCard.jsx ← exports Mobile + Desktop variants. `AddressKindIcon` resolves the row's `kind` to `HomeIcon` / `BuildingIcon` / `LocationIcon` (pin) — the third option is for arbitrary user-chosen labels like "Mom's" / "Warehouse". Each row carries star toggle + trash + EDIT DETAILS (opens the same `AddAddressDrawer` in edit mode).
    AccountIcons.jsx        ← inline SVG icon library for the hub. `StarIcon` uses the exact Figma vuesax/linear/star path with a `filled` prop (filled = `currentColor`, unfilled = stroke only); `TrashIcon` hardcodes its stroke to `#A50013` (red danger affordance); `DangerIcon` hardcodes stroke white (sits on the red error-toast background). `HomeIcon` / `BuildingIcon` / `LocationIcon` are simple filled glyphs that inherit color. `BrandTile` renders inline VISA / Mastercard logos. **All icons are inlined deliberately — referencing the Figma asset URLs breaks after 7 days when they expire.**
    AddCardDrawer.jsx       ← "use client" — Figma mobile 79:3149. Mobile bottom-card + desktop right-drawer (same animation conventions as the shop drawers). Permissive validation (≥12 digits + name + 4 expiry digits + 3+ CVV digits) — the expiry input auto-inserts the `/` between MM and YY/YYYY. ADD CARD button background set inline (`#11191F` / `rgba(17,25,31,0.5)`) per the Tailwind v4 + Turbopack gotcha for conditionally-rendered components.
    DeleteCardDrawer.jsx    ← "use client" — Figma mobile 79:2191. Confirmation drawer with the same mobile/desktop footprint as `AddCardDrawer`. Body shows a `BrandTile` + "{Brand} ending in {last4}" + expiry on a `#f0f0f0` grey strip. Footer is CANCEL outlined + DELETE CARD red (`#A50013`, inline-styled). Last-shown method held in a ref so the preview survives the exit animation.
    AddAddressDrawer.jsx    ← "use client" — Figma mobile 79:4500. Same mobile/desktop footprint. Optional `initial` prop puts the drawer in edit mode: title becomes "EDIT ADDRESS", submit becomes "SAVE CHANGES", fields pre-fill from the row. Re-keyed by `initial?.id ?? "new"` so consecutive opens get a fresh form-state bucket. Form fields: Address Title + 3-tile KindPicker (Home / Office / Other) + Country / City / Neighborhood / Street / [Building | Apartment]. Apartment is optional; every other field is required. Submit emits `{ label, kind, country, city, neighborhood, street, building, apartment }`.
    DeleteAddressDrawer.jsx ← "use client" — Figma mobile 79:5098. Twin of `DeleteCardDrawer`. Body shows the kind icon on a white inset over `#f0f0f0` grey strip + label + (Default) chip + the address line + phone. Footer: CANCEL + DELETE ADDRESS (`#A50013`).
    CardAddedBanner.jsx     ← "use client" — dark `#11191F` full-width slab toast, "CARD ADDED SUCCESSFULLY", 3s auto-dismiss, fixed at `top-[64px] md:top-[80px]` (below the sticky ShopHeader). Background set via inline `style.backgroundColor` per the gotcha.
    CardDeletedBanner.jsx   ← "use client" — same chrome as `CardAddedBanner`, copy "CARD DELETED".
    AddressAddedBanner.jsx  ← "use client" — same chrome, copy "ADDRESS ADDED SUCCESSFULLY". Fired only on add, not on edit (the edit branch in `saveAddress` returns early).
    AddressDeletedBanner.jsx ← "use client" — same chrome, copy "ADDRESS DELETED".
    CannotDeleteDefaultBanner.jsx ← "use client" — red `#A50013` full-width slab with `DangerIcon`, 4s auto-dismiss, `role="alert"` `aria-live="assertive"`. Accepts a `kind` prop (`"card"` | `"address"`) that interpolates into "You can't delete your default {kind}" — same component serves both delete flows.
  customer/product/       ← product detail surface (matches Figma 2119:2515 hero + the 8 marketing sections below)
    ProductPageClient.jsx ← "use client" — full product detail page. Mobile: swipeable image carousel + product info + CTAs (touch-pan-y, 15%/50px swipe threshold). Desktop hero (md+): thumbnail strip (lg+ only) + main image + info column. **Widths flex** with `flex-1 min-w-0` and `lg:max-w-[508px]` / `lg:max-w-[545.33px]` caps — do NOT re-pin `lg:w-[508px]` / `lg:w-[545.33px]` (that's what overflowed everything between lg and the Figma 1376 design width — see Conventions). Below the hero, 8 shared marketing sections: Crafted to Last (stats grid-cols-3) → Stays Dry → Move Without Limits → Sculpted Support → Four Colorways → mobile-only Gallery row → The Details → Complete Your Set (related products grid). Inline `PlusIcon` / `MinusIcon` / `HeartIcon` / etc. avoid the 7-day Figma asset URL expiry. `LowStockBanner` reads `itemsLeft` + `lowStockLimit` from the product object — once wired, derive from `variant.quantity` + `variant.low_stock_threshold` (catalog.ts surfaces both). `CtaRow` and `PriceBlock` / `DesktopPriceBlock` are shared between mobile + desktop blocks.
  customer/contact/        ← contact-us surface (Figma mobile 66:2449 only)
    ContactForm.jsx       ← "use client" — 5-field presentational form (first name / last name / email / country-code + phone / message) + black SEND MESSAGE CTA. No onSubmit yet — matches the auth-pages posture. Every field wears the `.contact-input` class so native input + ::placeholder font-family pin to Zalando Sans Expanded Medium (the body's `font-family: Arial` would otherwise leak into native form controls in some UAs — see globals.css for the rule, and the matching `.add-card-input` precedent). Field heights/text scale up at md+ (40→48px, 10→12px) while preserving the mobile Figma proportions below md.
    CountryCodePicker.jsx ← "use client" — dial-code popover anchored to the `+XXX` cluster inside the phone field. Closes on outside-click + Escape. Search filters by country name / dial / iso2. Flag emoji computed from `flagEmoji(iso2)` (`@/lib/countryCodes`) — no static flag table. Defaults to Jordan / +962 via `DEFAULT_COUNTRY`.
  customer/cart/           ← /cart + /checkout + /checkout/payment + /checkout/success + /checkout/failed surfaces (Figma cart mobile 83:5144 + desktop 119:5240; details mobile 82:3618 + desktop 119:5592; payment mobile 84:6733 + desktop 119:5877; success mobile 85:8692 + desktop 119:6418; failed mobile 111:3587 + desktop 119:6627)
    CartPageClient.jsx    ← "use client" — owns the /cart page AND exports the shared chrome reused by the details + payment pages. Splits each section into per-variant render paths driven by a `variant: "mobile" | "desktop"` prop. **Named exports** consumed by sibling clients: `Icon`, `CHECKOUT_STEPS`, `condensed`, `Stepper`, `FreeShippingBanner`, `ItemsSection`, `PromoCodeSection`, `OrderTotalsBlock`, `TrustBadgesRow`, `PaymentMethodsRow`, `SupportCard`, `PoliciesFootnote`, `StickyCheckoutBar`. **`Stepper` accepts `activeStep: "cart" | "details" | "payment"`** (default `"cart"`) and derives per-step status (`completed`/`active`/`pending`). Step 1 (`"cart"`) special-cases its `active` state to show the check icon (the entry-point step visually merges with `completed`); steps 2+ show their index number when active. Desktop also colors each connector line: BLACK `#11191f` when the lower step is `completed`, GREY `#e5e7eb` otherwise — so /cart shows two grey lines, /checkout shows cart→details black + details→payment grey, /checkout/payment shows both lines black. Desktop stepper geometry follows Figma's exact absolute offsets: 3 steps at `left: 0 / 296.28 / 609.97` inside a 672×56 container; 2 connector lines centered at `top: 25` with 224px visible width (256 outer − 32 px-4 padding). `StickyCheckoutBar` accepts `ctaText` (default "Continue to Next Step"; payment page passes "PAY & CONFIRM ORDER"). Cart's "Continue to Next Step" routes to `/checkout` via `router.push`; promo chips render in the SAME outlined-white state on both mobile and desktop — the "applied" indicator lives only on the green discount pill in the Order Total card, never on the chips themselves. **Responsive split** between md and lg: the desktop two-column layout stacks below `lg` (1024px) — the right Order Summary column drops below the items list — and only goes side-by-side at `lg+` where the 901.33 + 426.66 px Figma geometry fits cleanly. Don't pin `lg:w-[901.33px]` on the left column; use `flex-1 min-w-0` + `lg:max-w-[901.33px]` (and `lg:w-[426.66px] lg:shrink-0` on the aside).
    CheckoutDetailsClient.jsx ← "use client" — owns the /checkout page (step 2). Imports the shared chrome from CartPageClient as named exports + adds the details-only sections inline: `ContactInformationSection`, `ShippingAddressSection` (mobile single card + ADD-NEW-CARD muted button; desktop multi-card radio list with Add More + per-card EDIT DETAILS), `DeliveryMethodSection` (3 radio rows — Standard / Express / Store Pickup; selected card 2px dark border on mobile, 1px dark border on desktop; pickup price renders as green "Free"), `OrderSummaryToggle` (mobile-only collapsed bar that scrolls to the items section), `MiniItemsList` + `DesktopOrderSummaryCard` (desktop right-column composite: ORDER SUMMARY heading + mini items list + ORDER TOTAL breakdown + Total row + Continue CTA + policy line + Encrypted & Secure pill). Owns `addressList` state + an `addressDrawer = { open, initial }` discriminator that drives the reused `AddAddressDrawer` from `customer/account/` — heading **Edit** (mobile), heading **Add More** (desktop), per-card **EDIT DETAILS** (desktop), and the mobile **ADD NEW CARD** muted button all open the drawer in the matching mode. `handleSaveAddress` rebuilds the flat display `line` via `buildAddressLine` from `mockCart.js`. Continue routes to `/checkout/payment` via `router.push`.
    PaymentPageClient.jsx ← "use client" — owns the /checkout/payment page (step 3). Reuses the same shared chrome + adds the payment-only sections inline: `PaymentMethodSection` (radio list with one row per method; selected card 2px dark border on mobile, 1px dark border on desktop; ADD NEW CARD muted button below opens the reused `AddCardDrawer` from `customer/account/`), `ShippingAddressDisplay` (read-only address card with an Edit button that opens the reused `AddAddressDrawer` in edit-mode pre-filled with the shipping address), `TermsCheckbox` (T&C agreement that gates the place-order CTA — pill-wrapped #fafafa on mobile, plain row on desktop), `PaymentOrderSummaryCard` (desktop right column composite — mini items list + totals + embedded T&C + `CONFIRM & PAY` CTA + policy line + Encrypted & Secure pill). Mobile sticky CTA reuses `StickyCheckoutBar` with `ctaText="PAY & CONFIRM ORDER"`. **The place-order CTA now `router.push("/checkout/success")`** — it used to fire a `window.alert` placeholder. `handleAddCard` appends to `paymentMethods` state + auto-selects the new card. **Brand tiles are inline SVG components** (`VisaTile` / `MastercardTile` / `ApplePayTile` / `GooglePayTile` / `CashOnDeliveryTile`) matching Figma exactly — e.g. Visa is a light-grey 40×40 outer tile holding a dark inner mini-card with white italic "VISA" text (reads as a credit card); Apple Pay is a BLACK 40×40 tile with just the white Apple silhouette (no "Pay" wordmark). Don't reach for the wordmark PNGs in `/public/cart/` for the row tiles — those are for the small "we accept" payment-logo row, not the radio cards.
    OrderSuccessClient.jsx ← "use client" — owns the /checkout/success page. Mobile (Figma 85:8692) stacks: 128px dark gradient-blur disc with white check → "Order Successful!" 24px bold + 16px condensed subtitle → coaching CTA card (dark teal gradient + 106×120 coach photo + APPLY FOR COACHING white pill) → CONTINUE SHOPPING dark CTA → order-info card (shadowed, white, #ORD-2847391 + 48px dark bag tile + Estimated Delivery / Total Amount meta rows) → items list (84×126 thumbs, Zalando Condensed variant labels) → shipping-address card. Desktop (Figma 119:6418) centers a flat 96px #11191f disc with red-tinted check + heading at top, then two columns — left (coaching card → items list with #f9fafb pill rows, 96×128 thumbs → bordered shipping-address card with circular #11191f icon) and right (white shadowed Order Info Card with stacked CONTINUE SHOPPING / TRACK ORDER, then help box "Have a question about your order?" → `Contact Support Team`). Order metadata (`ORDER_NUMBER` / `ORDER_TOTAL` / `ESTIMATED_DELIVERY` / `PAYMENT_METHOD`) is hardcoded against the seeded cart — swap to data returned from `myAppCheckout` when the mutation lands (resolver already exists in `orders.ts`). All SVGs (check, bag, home) inlined per the 7-day asset-URL expiry rule; the coaching photo lives at `/public/cart/coaching-model.png`.
    OrderFailedClient.jsx ← "use client" — owns the /checkout/failed page. Mobile (Figma 111:3587) matches the design exactly: 128px dark gradient-blur disc with white X → "Payment Declined" 24px bold + 16px condensed subtitle → Payment Error card (white, shadowed, with a 40×40 #ff6b6b tile holding a white bang icon, then 3 condensed meta rows — Transaction ID, Attempted Amount in #ff6b6b, Card Ending) → items list → 3-CTA stack (`TRY ANOTHER METHOD` filled #11191f, `RETRY PAYMENT` 2px dark outlined, `RETURN TO CART` text-only). Desktop (Figma 119:6627) flat 96px #1a1a1a disc with red `#ef4444` pulse ring + 36px ExtraBold heading. Two columns — left (Payment Error card with `#fee2e2` outer border and a red-tinted top section using a `rgba(239,68,68,0.1)` icon tile + white bottom section with the 3 meta rows; ●●●● 4532 split-render matches the Figma node 119:6666; then items list — **no shipping address on desktop**) and right (Action Card: "ACTION REQUIRED" red eyebrow + "Payment Failed" 24px heading + #fef2f2 card-icon tile + Attempted Method row with inline `VisaChip` + 24px Total Amount + 3 CTAs — `Try Another Method` filled #111827, `Retry Payment` outlined #d1d5db NOT 2px dark, `Return to Cart` underlined text #666; then help box "Think this is a mistake?" with pipe-separated `Contact Bank Support` | `Chat with Us` links). All routes: `TRY ANOTHER METHOD` and `RETRY PAYMENT` → `/checkout/payment`; `RETURN TO CART` → `/cart`. **`VisaChip` is an inline 26×18 #1a1f71 chip with italic white "VISA"** — same pattern as the row tiles in PaymentPageClient, just smaller. Transaction id / attempted amount / card last4 are seeded constants; replace with the rejection payload from `myAppCheckout` (or whatever payment-processor adapter lands).
  shared/
    Sidebar.jsx           ← "use client" — mobile slide-in menu w/ drilldown for items that have children. Consumed by Header AND ShopHeader (mobile sort/menu button). Bottom section is auth-aware: when `isAuthenticated` is false renders a `Sign in/Sign up` link to `signInHref`; when true renders a `Logout` button (red `#a50013`) that fires the parent-provided `onSignOut`. The two parent headers both wire the same logout flow — don't duplicate it inside Sidebar.
    StoreProvider.jsx     ← "use client" — mounts the Zustand store on the client AND owns every cross-cutting auth-lifecycle UX (proactive refresh, session-expired toast + `?next=` redirect, focus/online wake-ups, cross-tab BroadcastChannel relay). Wraps `{children}` in the root layout. **Three independent effects**:
      (1) **Rehydrate + initial sync** — `await`s `useRepairStore.persist.rehydrate()` (the await is load-bearing — Zustand's persist middleware applies rehydrated state inside a `.then()` microtask, even with synchronous localStorage, so a bare `rehydrate()` followed by a synchronous read sees the pre-rehydrate values), THEN calls `maybeProactiveRefresh()` BEFORE the initial `syncCart()` + `syncWishlist()` — a tab that's been asleep for hours might rehydrate a token that's about to expire, and refreshing first means the syncs don't burn a 401 + retry round-trip on every page load. Also starts `initRepairStoreListener()` for cross-tab + same-tab localStorage-deletion handling + the BroadcastChannel("repair-auth") receiver. A `cancelled` flag in the effect closure guards against React strict-mode double-mounting kicking off two redundant sync passes.
      (2) **Focus / online proactive refresh** — `visibilitychange` + `focus` + `online` listeners call `maybeProactiveRefresh()` whenever the tab comes back into view or the network reconnects. E-commerce sessions are bursty (browse → walk away 20 min → come back to check out); without this, the first click after returning hits a 401 → refresh → retry at the worst possible moment. The `online` handler also re-runs `syncCart` + `syncWishlist` because the server-side cart/wishlist may have moved during the outage from another device.
      (3) **Session-expired UX** — subscribes to `sessionExpired` via `selectSessionExpired`. When it flips true, renders an inline `SessionExpiredToast` (dark slab anchored top-center, same visual language as `CardAddedBanner` / `AddressDeletedBanner`) for ~1.4s, then `router.replace(buildSignInRedirect(here))` where `here` is read from `window.location` directly. **Why window, not `useSearchParams` / `usePathname`** — StoreProvider lives in the root layout, and either of those hooks would either force the whole app to opt out of static rendering or require a Suspense boundary the root layout doesn't currently have. The session-expired path runs client-side only so `window` is always defined. Auth routes are skipped (no redirect loop), and `toastVisible` is derived from `sessionExpired` directly (avoids `react-hooks/set-state-in-effect` lint). A `handledRef` guards against duplicate handling when a sibling tab broadcasts the same expiry within milliseconds.
  auth/                   ← shared auth UI (15 files)
    HeroPanel.jsx         ← desktop-only 60%-wide image panel w/ logo + quote, hidden on mobile
    AuthHeader.jsx        ← logo + "Don't have an account? Sign Up" style header, mobile/desktop variants
    AuthInput.jsx, PasswordInput.jsx
    PhoneInput.jsx        ← "use client" — floating-label box hosting the shared `customer/contact/CountryCodePicker` for the country dial code plus a digit-only `<input type="tel">` for the subscriber number. Emits two hidden inputs alongside the visible field so FormData carries the country: `${name}DialCode` and `${name}Iso2`. Digit-only enforcement: `onKeyDown` blocks non-digit single-character keys (Backspace/arrows/Ctrl-V pass through), `onChange` runs `/\D/g` strip + `slice(0, len.max + 1)` so paste survives but length stays bounded, plus `inputMode="numeric"` + `pattern="[0-9]*"` for the mobile keypad. `maxLength` is `phoneLengthFor(country.iso2).max + 1` — the +1 absorbs the national-trunk-zero (e.g. Jordan's "0791234567"). When country changes, the existing value is trimmed to the new country's `max + 1`. **Validation lives in the parent form, NOT here** — parents must strip the leading 0 (and any duplicated dial code) BEFORE measuring length against `phoneLengthFor(iso2)`; see SignUpForm/CompleteProfileForm/ContactForm for the canonical pattern.
    AuthButton.jsx        ← primary white-pill submit
    GoogleButton.jsx, Divider.jsx, TermsFooter.jsx
    AuthGuard.jsx         ← "use client" — gate for the role-group layouts (`(customer)`, `(admin)`, `(deliveryCompany)`, `(accountant)`). Renders `null` while the store is still rehydrating (so a returning user doesn't flash to /sign-in on every page reload — uses `useRepairStore.persist.hasHydrated()` + `onFinishHydration`), and once ready, if the user isn't signed in, `router.replace`'s to `buildSignInRedirect(here)` from `@/lib/authRedirect` — encoded as `/sign-in?next=<current path + search>`. Reads `window.location.pathname` / `window.location.search` directly to avoid pulling `useSearchParams` / `usePathname` into every guarded layout (each would need its own Suspense boundary). The `?next=` round-trip is honored by every auth form on the other side via `postAuthDestination`.
    GuestGuard.jsx        ← "use client" — inverse of `AuthGuard`. Wraps `(auth)/layout.js` so a returning, already-logged-in user is `router.replace`'d to their role's home via the shared `homeForRole()` helper from `@/lib/authRedirect` (`admin → /r3pr-console`, `delivery → /r3pr-dispatch`, `accounting → /r3pr-ledger`, `customer → /shop`). Same hydration-wait pattern as `AuthGuard` — `useRepairStore.persist.hasHydrated()` + `onFinishHydration` listener — so first paint on a hard reload doesn't see `isLoggedIn === false` and miss the redirect. Renders `null` while hydrating and while redirecting (no flash of auth form).
    SignInForm.jsx        ← "use client" — wired to `myAppLogin` (query). Reads `?reset=1` AND `?next=<path>` via `useSearchParams`: the former renders a green "Password updated. Sign in with your new password." banner above the form (surfaced after a successful reset confirmation), the latter is honored on success via `postAuthDestination({ searchParams, role: data?.user?.role })` so the user lands back at the page that bounced them through AuthGuard or the session-expired flow (falls back to the role's home — admin → `/r3pr-console`, delivery → `/r3pr-dispatch`, etc.). On success: `setAuthInfo(data)` + `syncCart()` + `syncWishlist()` + `router.push(postAuthDestination(...))`. Single-string "Invalid email or password" on every failure mode (matches the server's anti-enumeration posture). **Must be wrapped in `<Suspense>`** at the page level — `useSearchParams` requires it in App Router.
    SignUpForm.jsx        ← "use client" — wired to `myAppSignUp` (mutation, locked to `tableName: "users"`). Validates email format with `/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/`, strips the national-trunk-zero from the phone BEFORE measuring length against `phoneLengthFor(iso2)`, builds wire format as `+${dial}${localDigits}`. Hardcoded "customer" role lives on the server — never trust client-provided role. On success: same setAuthInfo + sync + `router.push(postAuthDestination(...))` as SignInForm so `?next=` is honored from a sign-up bounce too. **Page must be wrapped in `<Suspense>`** at the page level — `useSearchParams` requires it in App Router (already done in `(auth)/sign-up/page.js`).
    GoogleSignInButton.jsx ← "use client" — token-client flow (single popup, scopes `openid email profile`). On callback calls `myAppGoogleAuth`; new Google users get `{ needsPhone: true, signupToken }` back, sessionStorage-stash the signupToken AND the validated `?next=` value under `pendingGoogleSignup` (the next-value survives the /complete-profile hop so the user still lands at their original destination), route to `/complete-profile`. Returning Google users skip straight to `postAuthDestination({ searchParams, role })` with auth + sync. **No People API / no phone scope** — phone collection deferred to the post-OAuth form by design.
    CompleteProfileForm.jsx ← "use client" — reads `pendingGoogleSignup` from sessionStorage on mount (`{ signupToken, email, name, next? }`); missing → `router.replace("/sign-up")`. Submits `{ signupToken, phone }` to `myAppCompleteGoogleSignUp`. Uses the same trunk-zero-strip phone normaliser as SignUpForm. On success: setAuthInfo + sync + push to `next` (if `isSameOriginPath(pending?.next)` — validated again on the way out because the sessionStorage payload could have been tampered with) or `homeForRole(data?.user?.role)`; clears the sessionStorage entry.
    RequestPasswordResetForm.jsx ← "use client" — wired to `myAppRequestPasswordReset` (mutation). Email validated with same regex as signup. Server returns "Email is not registered" for unmatched / inactive accounts (deliberate trade-off against anti-enumeration — see Server CLAUDE.md). On success swaps to an inline "Check Your Inbox" state with: the submitted email, a Resend button that re-fires the same call (server invalidates prior unused tokens so resends are safe), and a "Use a different email" link that returns to the form.
    ResetPasswordForm.jsx ← "use client" — reads `?token=` via `useSearchParams`, validates new password (≥8 chars + match), calls `myAppConfirmPasswordReset`. Lazy token validation — form renders for everyone, server rejects bad/expired/used tokens with an inline error. On success: `router.push("/sign-in?reset=1")` so SignInForm flashes the green banner. **Must be wrapped in `<Suspense>`** at the page level for the `useSearchParams` read. Server-side bonus: every refresh token for the user is revoked inside the same resolver, so any active sessions on other devices are kicked at the same moment the password rotates.
  admin/                  ← admin console UI — all client-component-heavy because every page is interactive (forms, drawers, filters). Mock data only; no backend calls yet.
    layout/
      AdminShell.jsx      ← "use client" — sticky SideNav (lg+) + mobile off-canvas drawer + TopBar. Locks body scroll while the off-canvas is open. Single layout consumed by every admin page via (admin)/layout.js.
      SideNav.jsx         ← "use client" — dark #11191f sidebar with `SECTIONS` array (Overview / Catalog / Operations / Insight). Active route highlighted via `usePathname` exact-or-prefix match. **Add new admin pages by extending SECTIONS here** — don't recreate the navigation per page. Catalog now includes Categories / Products / **Taxonomies** / Discounts / Promo Codes. Insight now includes Reports / **Wishlist Insights** / **Storefront Content** / **Subscribers** / Broadcast Email. Imports `ORDERS`, `CONTACT_MESSAGES`, and `STOCK_ALERTS` from `mockAdmin.js` to compute live badge counts: `processingCount` on Orders, `unreadCount` on Contact Messages, `pendingAlertCount` on Stock Alerts — each renders a `#1d4ed8` pill badge when > 0. Swap to props from AdminShell when wiring to real data.
      TopBar.jsx          ← "use client" — sticky white bar with mobile menu button, search input (presentational), notification bell, profile dropdown. The bell button toggles `NotificationPanel` open/close; a `useRef` + `mousedown` listener closes the panel when clicking outside it. The bell badge shows the live total count (processing orders + unread messages + pending stock alerts + all low-stock variants); hidden when zero. Profile dropdown uses the same outside-click pattern.
      NotificationPanel.jsx ← "use client" — dropdown panel rendered below the bell button. Receives pre-filtered slices as props (`orders`, `messages`, `stockAlerts`, `lowStock`) — TopBar computes these at module level. Shows 4 grouped sections: **New Orders** (blue, links to `/r3pr-console/orders`), **Unread Messages** (indigo, links to `/r3pr-console/messages`), **Stock Alert Requests** (amber, links to `/r3pr-console/stock-alerts`), **Low Stock** (red, links to `/r3pr-console/products`). Each section has a coloured icon header with count chip + "View all" link, up to 3 item rows (primary text, truncated secondary, timestamp), and a "+N more" link when extras exist. All links call `onClose` so the panel dismisses on navigation. Empty state shows "All caught up" when total count is 0.
      PageHeader.jsx      ← shared `{ eyebrow, title, description, actions }` page-title block — every admin page renders one at the top before its manager component.
    shared/               ← admin design primitives (NOT to be promoted to /components/shared/ — they're admin-specific colors + radii)
      Button.jsx          ← 6 variants (primary / accent / secondary / ghost / danger / dangerSolid) × 3 sizes. Plus `<IconButton>`. Uppercase Zalando Expanded, 2px radius. Don't reach for the shop's GlassButton / AuthButton inside admin — they're tonally wrong.
      Drawer.jsx          ← right-side drawer, reuses the `.right-drawer` keyframes from globals.css (the shop's AddToCartDrawer animation). Owns its own `useDelayedUnmount` + body-scroll lock + Escape handler.
      Modal.jsx           ← centered confirmation dialog, reuses the `.filter-card-desktop` keyframes for the open/close animation.
      DataTable.jsx       ← "use client" — generic `{ columns: [{ key, label, align, width, render(row) }], rows, onRowClick, empty }`. Required to be a client component because column `render` callbacks are functions — see "Server→client function-prop boundary" in Conventions.
      StatusBadge.jsx     ← rounded-full pill, color comes from `STATUS_TONE` in mockAdmin.js keyed by `status` (order status / payment / role / active|inactive). Centralised tone map so a new status only needs one new entry.
      KpiCard.jsx         ← dashboard KPI tile. **Value font is `clamp(20px, 4.2vw, 28px)`** so long currency strings ("JOD 184,320.00") scale down on phones without media queries. Delta + value wrap on a flex row at narrow widths.
      Charts.jsx          ← **"use client"** — Recharts wrapper. Exports `LineChart`, `BarChart`, `Donut`. All admin pages import charts exclusively from here; never import from `recharts` directly so a future library swap stays in one file. `LineChart({ data, series, height, color, yFormatter })` — `data` is `[{ day|label, value }]` or a multi-key object when `series` is provided; `series?: [{ key, color, name }]` drives multi-line comparison (omit for single-series); dots suppressed when `data.length > 20`; X-axis labels auto-thin to ~10 visible ticks for 30/60/90-day ranges. `BarChart({ data, height, color, horizontal, yFormatter })` — vertical by default; pass `horizontal` for category comparisons or when label count > 7; per-bar `color` field on each datum overrides `color`. `Donut({ data, size, label })` — Recharts `PieChart` with center label overlaid via CSS absolute positioning (not SVG `<text>`) so the font tokens apply. All three expose a `yFormatter` prop for Y-axis/tooltip value formatting. `TooltipBox` renders on hover for all chart types. **Server components can still import and render these** — they're client components, and Next.js serialises their plain-data props at the server→client boundary (no function props allowed).
      Form.jsx             ← Field / Label / TextInput / NumberInput / TextArea / Select / Toggle / Chip / SearchInput / DateInput. Standard admin form atoms. Tailwind v4-safe (no arbitrary `aspect-[]` patterns).
      Icons.jsx            ← 30 single-stroke SVG icons (Dashboard / Box / Folder / Cart / Chart / Tag / Users / Percent / Mail / MessageSquare / Search / Bell / Plus / Close / Chevron* / Edit / Trash / Eye / EyeOff / Grip / Download / Filter / Check / ArrowUp/Down / Logout / Alert / Calendar / Send / Copy / Settings). Stroke uses `currentColor` so they inherit parent color.
    dashboard/RecentOrdersTable.jsx ← "use client" wrapper around DataTable so the dashboard server page can pass plain `rows={…}` without crossing the function-prop boundary.
    categories/CategoryManager.jsx  ← "use client" — full category tree. Row click toggles the major's accordion (chevron is a visual indicator only); inner action buttons + grip stopPropagation so they don't toggle. Reorder is **Pointer Events drag-and-drop** (mouse / touch / pen) on the grip handle (`touch-none` to suppress scroll), with up/down arrows kept as keyboard fallback. Drop targets are hit-tested via `data-drop-kind`/`data-row-id` + `getBoundingClientRect`; majors only drop on majors, subs only drop within their parent major (scoped by `data-major-id`). A 2px `#1d4ed8` line indicates the insertion point and the dragged row dims to 50%. Visibility toggle, edit, delete are per-row. `CategoryDrawer` handles BOTH major and sub creation/edit via an `editing = { kind: "major" | "sub", row }` discriminator. **No `slug` field** — major_categories / sub_categories on the server only have `id`/`name`/`is_visible`/`sort_order`, so the drawer only writes `name`/`visible`/`majorId` (mockAdmin.js still carries a `slug` field but it's ignored). **All `editing.X` and `drag.X` access uses optional chaining** — even inside `{open ? ... : null}` guards or after `drag?.kind === "..."` narrowing — because React Compiler can pre-evaluate both ternary branches during memoization analysis.
    products/ProductManager.jsx     ← "use client" — three panels + table + drawer. **ColorPanel** manages the global color palette (`COLORS` in mockAdmin.js — `{id, name, hex}`); colors can be added, edited (name + hex swatch), and deleted (with a confirmation guard if variants reference them). **SizePanel** manages the global size list (`SIZES` — `{id, label}`); same add/edit/delete pattern. Both panels feed the per-product selectors in the drawer. The **product table** has search, category, and visibility filters. Clicking a row or "New Product" opens **ProductDrawer** — a right-side Drawer with 4 tabs:
      - **Details** — Name (required), Subtitle (required in text mode; in Composition mode the subtitle is auto-built from `{pct, material}` rows that must each have a name and must sum to 100%), Sub-category, Price (required, >0), Description, **Filter facets card** (multi-chip `materialTags` selector sourced from `MATERIALS` in mockAdmin — manage on Taxonomies; the Product Type is the sub-category set above, NOT a separate select), Product details (minimum 3 entries, each needs a title — header shows a live `N/3` counter), Visible toggle. A **Storefront preview** panel mirrors the exact mobile and desktop `ProductCard` label-row rendering live. **The legacy hardcoded `materials` panel (recycledPolyester / premiumElastane / performanceGuaranteed) was removed** — flexible fabric content lives in the composition rows under the Subtitle field.
      - **Media** — **Available Colors** chip-row (subset of global palette → `draft.productColors`; shared state with Inventory tab). **Available Sizes** chip-row (`draft.productSizes`; shared with Inventory tab). Below the divider, a per-color **PhotoColorBlock** with real photo management: file picker (multi-select, `URL.createObjectURL` previews — no server upload, pure UI), thumbnail grid in 3:4 ratio cards, primary-star toggle (first upload auto-becomes primary; the `Primary` chip overlays the thumbnail), move-left/move-right reorder buttons, per-photo delete, plus an "Add photo" tile and a "Use sample" fallback that cycles `PLACEHOLDER_PHOTOS` (`/shop/model-1.png` … `model-5.png`). Existing products with the legacy `colorImages: { [colorId]: count }` counter shape are backfilled to `photos: { [colorId]: [{id, url, primary, name}] }` via `buildPhotosFromCounter()` at draft init, so older mock rows still render thumbnails when opened for edit.
      - **Inventory** — Color chip-row and Size chip-row (same `draft.productColors`/`draft.productSizes` state — changes reflect in Media tab and vice versa). Below the divider, a color × size qty grid **scoped to selected combinations only** (not the full global palette). Qty inputs show empty string instead of `0` (prevents "02" prepend issue), `min="1"`. Setting a cell to 0/empty prunes that variant from `draft.variants`; `totalStock` updates live.
      - **Labels** — Storefront label chips (multi-select from `LABEL_OPTIONS`) + **Featured-in-Collections checkbox list** (sourced from `COLLECTIONS` in mockAdmin — Sale / Just Dropped (Weekly) / Just Dropped (Monthly) / Featured on Discover All) + low-stock alert threshold.
      **Draft state shape**: `{ name, subtitle, subtitleMode ("text"|"composition"), composition [{pct,material}], subId, price, description, materialTags [materialId], collections [collectionId], details [{title,desc}], productColors [colorId], productSizes [sizeId], photos {[colorId]: [{id, url, primary, name}]}, variants [{color,size,qty}], totalStock, visible, labels, lowStockThreshold }`. When editing an existing product whose mock data has `variants` but no `productColors`/`productSizes`, the `useEffect` derives them from the variant list (via `??` coalescing) so the grid renders correctly. `toggleProductColor(id)` / `toggleProductSize(id)` add/remove from draft and prune orphaned variants atomically.
      **Validation** (`validate()` → `handleSave()`): name required; subtitle required (text mode); price > 0; composition rows complete + sum 100%; details ≥ 3 with titles; ≥ 1 color selected; ≥ 1 size selected; `photos[id].length > 0` for every selected color; every color × size cell ≥ 1. All errors collected at once; inline error text appears per field; tab buttons show a red dot while their tab has errors; Save auto-jumps to the first failing tab (Details → Media → Inventory priority). Individual field errors clear as the user fixes them via `clearError(key)`.
    taxonomies/TaxonomyManager.jsx  ← "use client" — left column: editable `MATERIALS` list (add/edit/delete inline with Enter-to-save / Escape-to-cancel). Right column: **read-only Sub-Category Types panel** grouping `SUB_CATEGORIES` by major with an "Edit in Categories" link — the panel reinforces that the storefront "type" filter is the sub-category, not a separate taxonomy.
    storefront/StorefrontManager.jsx ← "use client" — content editor for every hardcoded landing surface. Sections (each collapsible via `SectionCard`): **Hero** (eyebrow / title / subtitle / CTA label / CTA href / background image), **Colorways** (add/remove sections; each has `image` + `imageAlt` + `badge` + `reversed` toggle + `ctaLabel` + swatch list with color picker / name / tagline), **Stats**, **Browse Collection** tiles, **Product Page Sections** (per-row enable toggle + title + body), **Coaching Cross-Sell** (enable toggle + eyebrow/title/body/CTA + coach image), **Footer** (brand copy + social links + footer columns of links). **`ImageField`** helper renders the current image preview at the chosen aspect ratio, swaps between "Upload photo" (file picker → URL.createObjectURL) / "Edit path" / "Clear image" / "Use sample" affordances. Every section's `SaveBar` carries a **Show preview / Hide preview** toggle that reveals a `PreviewPanel` wrapped in a blue "PREVIEW — UNSAVED" eyebrow; the preview components (`HeroPreview` / `ColorwayPreview` / `StatsPreview` / `BrowseTilesPreview` / `ProductSectionsPreview` / `CoachingPreview` / `FooterPreview`) are styled to match the real landing — black bgs, Zalando display fonts, inline-rebuilt `GlassCtaPill` matching `GlassButton`, alternating two-column colorway layout, white footer with social bubbles. **When you change the live `homePage/*` components, mirror the change in the matching preview** so admins keep seeing an accurate demo before they Save.
    subscribers/SubscribersManager.jsx ← "use client" — status filter chips (All / Active / Unsubscribed with live counts), email search, CSV export (client-side `Blob` download), per-row Unsubscribe ↔ Re-subscribe and Delete (with confirmation modal). Wire to a future `myAppListNewsletterSubscribers` resolver pair when a `newsletter_subscribers` table lands on the server.
    wishlist-insights/WishlistInsightsManager.jsx ← "use client" — 4 KPIs at top (Total wishlisted / Added last 7 days / Out of stock / Low-stock variants). **Bulk "Notify all wishlist holders" card** above the table with a modal (subject + body textarea with `{{first_name}}` merge tag, validation, "Send to all" CTA → toast). Stock-filter chips (All / In stock / Out of stock / Low-stock variants), product search, ranked DataTable with per-row "Notify holders" (in stock) / "Notify on restock" (out of stock) button → per-product modal → toast. Unique-holder count is mocked at ~80% of the sum of wishlist counts (de-dup heuristic). Wire to a future `myAppListWishlistInsights` + `myAppNotifyWishlistHolders` resolver pair.
    orders/OrderManager.jsx          ← "use client" — status filter chips with live counts at the top, then search/export bar, table, and detail drawer. (The delivery-fee setting that used to live in a collapsible accordion here moved to `settings/SettingsManager.jsx` — keep all storefront-wide shipping/delivery rules on the Settings page, not bolted onto operational pages.) **Pipeline is 4 steps: Processing → Prepared → With Delivery → Delivered** (no Pending — orders enter at Processing). `PIPELINE` + `PIPELINE_LABEL` map live at the top of the file. Transition rules: (1) **sequential only** — the detail drawer shows a single "Mark as {next step}" button, never a skip-ahead list; (2) **confirmation modal** on the `handed_to_delivery → delivered` transition ("Are you sure?", Cancel / Yes, mark as delivered). Cancelled/returned are terminal — transition button is hidden. `OrderDetail` owns its own `confirmNext` state and imports `Modal` from admin shared.
    reports/ReportsView.jsx         ← "use client" — **date presets** (7D / 30D / 90D / YTD) above the date-range inputs; selecting a preset highlights it blue and fills both inputs. **Underline tab bar** (not chip pills) for the 7 tabs: Sales / Revenue / Promo usage / Inventory / Customers / Delivery / Returns. `MetricStat` shows a coloured delta arrow (green ▲ positive / red ▼ negative) + "vs prior period" hint. Charts use the Recharts-backed `Charts.jsx` primitives. Notable tab details: Sales and Customers tabs have a **Compare toggle** that adds a grey prior-period line to the LineChart (controlled by local `compare` state; data comes from the `current`/`prev` keys on `SALES_SERIES_30` / `SIGNUPS_SERIES_30`). Revenue and Inventory use horizontal `BarChart` for category comparisons. Delivery has both a daily bar chart and an outcome Donut. Returns has a horizontal reasons bar + Donut. The promo tab uses an inline `UsageBar` component (fill turns red when ≥ 90% of limit used). Export PDF / CSV buttons remain presentational.
    promo/PromoManager.jsx          ← "use client" — **WIRED TO BACKEND** (`myAppAdminListPromoCodes` + AdminCreate/Update/DeletePromoCode via `repairCall`). Table with usage-progress bars + copy-to-clipboard, drawer with code generator + type/amount/min-order/**redemptions-per-customer (usage_type single|multi)**/total-usage-limit/expiry/active. Field-map gotchas (centralized in toUiRow/toWirePayload/validateDraft): **usage_limit blank→null = unlimited** (NEVER 0 — backend rejects <1); **used_count never sent** (append-only via checkout, read-only in drawer); **delete refuses redeemed codes** (await + keep row + surface "deactivate instead"); **status from two independent fields** (`is_active` flag + `expires_at` date — expired derived client-side, badge shows "Expired" over the active flag); **usage_type single = once PER CUSTOMER** (via promo_code_usages), distinct from the global usage_limit cap.
    users/UserManager.jsx           ← "use client" — role chips (Admin / Delivery / Accounting / Customer) with counts, table with inline active toggle, drawer with welcome-email notice for new accounts.
    discounts/DiscountManager.jsx   ← "use client" — scope chips (product / sub-category / major-category), drawer that swaps its target-picker `options` based on the selected scope, validity window + active toggle.
    broadcast/BroadcastComposer.jsx ← "use client" — side-by-side composer + live email preview, audience selector with counts, merge-tag chips, confirm-modal before send, sent-history table.
    contacts/ContactManager.jsx     ← "use client" — status filter chips (All / Unread / Read / Replied / Archived with live counts), search bar, table with sender avatar + name + email + phone + message preview + date + status badge + view/delete actions. Opening an unread row auto-marks it as read. Detail drawer shows full message body + "Mark as Replied" / "Mark as Read" quick actions + Archive/Unarchive + Delete footer buttons. Delete confirmation modal. Wire to a future `myAppListContactMessages` / `myAppUpdateContactMessageStatus` / `myAppDeleteContactMessage` resolver set when the backend adds a `contact_messages` table.
    stock-alerts/StockAlertManager.jsx ← "use client" — status filter chips (All / Pending / Notified / Dismissed with live counts), search by customer name, email, or product name, table with customer avatar + name/email + product/variant + date + status badge + delete action. Row click opens detail drawer showing customer block, variant block (product · color · size), and contextual quick-action buttons: "Mark as Notified" + "Dismiss" when pending; "Dismiss" only when notified. Delete confirmation modal. Wire to a future `myAppListStockAlerts` / `myAppUpdateStockAlertStatus` / `myAppDeleteStockAlert` resolver set when the backend adds a `stock_alerts` table.
    settings/SettingsManager.jsx ← "use client" — storefront-wide commerce rules. **Six stacked `SettingsCard` blocks**: (1) **Delivery Fee** (JOD-prefixed standard fee, always on); (2) **Free Delivery** (toggle + threshold input that disables when toggle is off); (3) **Express Shipping** (toggle + extra-fee input); (4) **Shipping Methods** (per-method enable toggle + display-name + ETA-copy editor for Standard / Express / Pickup — drives `SHIPPING_METHODS` on mockAdmin; when Pickup is on, a **Pickup Locations** CRUD block lets admins add/remove `{name, address, hours}` rows); (5) **Tax Rate** (`PctInput` 0-100 + tax-inclusive toggle + apply-on-shipping toggle); (6) **Payment Methods** (per-method on/off toggles for Visa/MC, Apple Pay, Google Pay, Cash on Delivery; Save validates that at least one method stays enabled). Shared inline `JodInput` / `PctInput` / `SavedPill` helpers; validation skipped when the feature toggle is off (no point validating a disabled input). Mock-only — wire to future `myAppGetCommerceSettings` / `myAppUpdateCommerceSettings` (plus the existing `myAppGetShippingSettings` shape) resolvers when the `shipping_settings` / `tax_settings` / `payment_methods` / `pickup_locations` tables land on the server.
  accountant/             ← accountant ledger UI — forks the admin shell pattern with a finance-only nav. Reuses admin's `shared/` primitives (Button, DataTable, Charts, Form, Icons, PageHeader) — do NOT duplicate them under accountant/shared/. Mock data only.
    layout/
      AccountantShell.jsx ← "use client" — sticky SideNav (lg+) + mobile off-canvas drawer + TopBar. Body-scroll lock pattern copied from `AdminShell`. Single layout consumed by every accountant page via (accountant)/layout.js.
      SideNav.jsx         ← "use client" — dark `#11191f` sidebar with a single **Finance** section listing Overview, Export history, and Audit log. Footer carries a persistent **Read-only access** info block above Sign Out. Active route uses `usePathname` exact-or-prefix match, same as the admin SideNav. **Add new accountant pages by extending SECTIONS here** — don't recreate the navigation per page. (The `disabled: true` "Coming soon" pattern is still supported in the rendering branch even though no item currently uses it — keep that branch if you stage a future page.)
      TopBar.jsx          ← "use client" — slim sticky white bar with mobile menu button, breadcrumb cluster ("Finance Ledger / Read-only · Accountant"), and profile dropdown. **No notification bell + NotificationPanel** — the accountant role has no operational queue to action, so the bell was replaced with a permanent `Read-only` chip (blue `#eff6ff` / `#1d4ed8`). When wiring real data, surface signed-in user info via props rather than reintroducing the bell.
    overview/
      FinanceOverview.jsx ← "use client" — full finance dashboard. **Date-range filter bar** (presets 7D/30D/90D/YTD + DateInput From/To + Export PDF/CSV buttons — exports are presentational, same as admin Reports). **5 KpiCards** in a `grid-cols-1 sm:grid-cols-2 lg:grid-cols-5` row, each with a coloured 3px top stripe: Total revenue (blue) / Total orders (sky) / Discount impact (purple) / Avg. order value (amber) / Net revenue (green). All deltas computed via `pctDelta` against the prior-period column. **Revenue-over-time** LineChart with a Compare toggle that adds a grey prior-period series. **Breakdown tab bar** — Category / Product / Time period: Category renders a horizontal BarChart + Donut + a share-list aside; Product renders a ranked DataTable with rank chip, units, revenue, and an inline share-bar; Time period renders a Day/Week/Month granularity toggle that calls `rollupBy` then renders a BarChart + DataTable with orders/revenue/discount/net columns. Finally a **Discount impact by code** DataTable below the tabs covers the requirement-2 "discount impact" line item with per-code redemptions + JOD impact + active/expired badge. `_idx` is injected via `rows.map((r, i) => ({ ...r, _idx: i }))` so the rank column can read it inside `render(row)` (DataTable's render signature is row-only, no index — see Conventions). No hand-written `useMemo` per the React Compiler convention; filterDaily / aggregate / rollupBy run inline.
    exports/
      ExportHistory.jsx   ← "use client" — list of past exports backed by `EXPORT_HISTORY` in mockFinance. 4 summary tiles (Total / Ready / Generating / Expired) on a coloured top stripe, then a filter bar with a search-by-file/report/person `TextInput` (magnifying-glass icon inside) + format chips (All / PDF / CSV) + status chips (All / Ready / Generating / Expired). Table columns: File (red PDF / green CSV format tile + truncated filename + report subtitle) · Date range (`IconCalendar` + range string) · Generated by (name + email) · When (relative timestamp via `formatRelative` anchored to today = 2026-05-23) · Size (via `formatBytes`) · Status pill from `EXPORT_STATUS_TONE` · Action (`Button variant="secondary" size="sm" icon={<IconDownload />}` Download — only rendered when status === "ready"). Empty state has a "Reset filters" link that clears all three filter axes at once. Footnote points to `myAppFinanceListExports` / `myAppFinanceDownloadExport` resolvers.
    audit/
      AuditLog.jsx        ← "use client" — read-only audit trail backed by `AUDIT_LOG` in mockFinance. 5 summary tiles (Total events / Views / Exports / Filter edits / Sessions = sign-ins + sign-outs). Filter bar with search-by-user/target/IP/detail + From/To `DateInput`s (with a "Clear" button when either is set) + kind chips (All / Views / Exports / Filter changes / Sign-ins / Sign-outs). Table columns: Actor (dark circular initials avatar + name + email) · Action (coloured pill via `AUDIT_ACTION_KINDS` with an inline icon — `IconChart` for view, `IconDownload` for export, `IconFilter` for filter, `IconCheck` for signin, `IconLogout` for signout) · Detail (action description + target subtitle) · When (date + time via `formatAuditTime`) · IP (tabular-nums, right-aligned). **No mutation actions** — surface is intentionally read-only; do not add edit/delete columns when wiring to the backend. Footnote points to `myAppFinanceAuditLog({ from, to })`.
  delivery/               ← delivery role UI — forks the accountant shell pattern with a delivery-only single-item nav. Reuses admin's `shared/` primitives (Button, Modal, Icons) — do NOT duplicate them under delivery/shared/. Mock data only.
    layout/
      DeliveryShell.jsx   ← "use client" — sticky SideNav (lg+) + mobile off-canvas drawer + TopBar. Body-scroll lock pattern copied verbatim from `AdminShell` / `AccountantShell`. Single layout consumed by every delivery page via (deliveryCompany)/layout.js.
      SideNav.jsx         ← "use client" — dark `#11191f` sidebar with a single **Dispatch** section listing **Assigned orders** (with a live `#1d4ed8` count badge derived from `deliveryCounts(ASSIGNED_ORDERS)` — currently only the `active` count is surfaced, but `delivered` / `failed_delivery` counts are computed too in case a second item is added). Footer carries a persistent **Delivery account** info block ("You can update status on orders assigned to you. You cannot modify catalog or pricing.") above Sign Out. Active route uses `usePathname` exact-or-prefix match, same as the admin/accountant SideNavs. **Add new delivery pages by extending SECTIONS here** — don't recreate the navigation per page.
      TopBar.jsx          ← "use client" — slim sticky white bar with mobile menu button, **Dispatch / Delivery · <DRIVER.zone>** breadcrumb cluster, an **On shift** pill (blue, analogous to the accountant's Read-only pill — replaces the bell + notification panel because the driver's queue is the dashboard itself), and a profile dropdown keyed to `DRIVER` (initials chip + name + role + My profile / End shift / Sign out menu).
    dashboard/
      DeliveryDashboard.jsx ← "use client" — full delivery dashboard. **3 StatPills** in a wrapped flex row (Out for Delivery / Delivered / Failed) — each pill double-acts as a filter chip; clicking the active one toggles back to "all". **Search + chip row** with a magnifying-glass `TextInput` (filters by order id, customer name, address) and an "All" chip plus 3 status chips from `DELIVERY_FILTERS` with live counts. **Orders list** renders as a `md:hidden` stacked card list on mobile and a full-width table at `md+`. Mobile cards: order id + payment type + total + status badge in the header row, then customer name/phone, then address + items summary on a divided bottom row. Desktop table columns: Order (id + COD/Prepaid eyebrow) · Customer (name + phone) · Address (truncated + italic note if present) · Items (via `itemsSummary` from mockDelivery) · Total (with "Collect cash" amber subtitle on COD rows) · Status (StatusBadge) · Action (Update for `handed_to_delivery`, Open otherwise). Both variants link to `/r3pr-dispatch/orders/[id]`; the Update CTA stops the row click from re-firing.
    orders/
      OrderDetailPage.jsx ← "use client" — full `/r3pr-dispatch/orders/[id]` detail. Header carries the breadcrumb back to Assigned orders, the customer name as the page title, a StatusBadge + "Assigned <ts>" line, and (when status === `handed_to_delivery`) two CTAs — **Failed Delivery** (danger variant) and **Mark Delivered** (primary). 2-column grid at `xl+` — left has **Customer** (name + Call/Email CTAs that use `tel:` / `mailto:` deep links + phone/email MetaRows), **Delivery Address** (address line + optional customer note in a blue-tinted left-border callout + "Open in Maps" button that uses a `https://www.google.com/maps/search/?api=1&query=` link with the address URL-encoded), and **Items** (each row: `× N` quantity chip + product name + color/size eyebrow + line total; footer has subtotal + order total + a yellow "Collect on delivery" stripe rendering the COD amount when payment method is Cash on Delivery). Right column has **Contact**, **Order info** (Placed / Assigned / Delivered or Failed / Reason / Note as terminal-state rows kick in), and **Status history** (reverse-chronological timeline with colored dot per status). **Mark Delivered** opens a `Modal` confirm — when the order is COD, the modal surfaces a yellow "Confirm you collected JOD <codAmount>" callout. **Failed Delivery** opens a `FailedDeliveryModal` (radio list of the 6 `FAILED_DELIVERY_REASONS` + optional notes textarea, dangerSolid "Mark as Failed" CTA). On commit, both flows mutate the local `order` state AND call `mutateLocalStatus(id, patch)` to update `ASSIGNED_ORDERS` in place so the sidebar count badge refreshes on next navigation — this is mock-layer behavior, replace with a refetch after the mutation lands. Both flows fire a 3.2s `Toast` (green for delivered, red for failed); the Toast uses a `useEffect` keyed on `text` (not on the onDismiss callback, which is a new arrow each parent render — would restart the timer on every keystroke). Inline "Order not found" fallback (back-to-list CTA) when `findDelivery(id)` returns null.

public/
  splash/   ← splash-{1..4}.png + logo-re.png (splash overlay)
  home/     ← hero hoodie, colorway shots, social/cta icons, logo-re.png
  auth/     ← desktop + mobile hero art for the auth split layout, blackLogo.png
  shop/     ← model-1..5.png (mock product photos pulled from Figma), icon-add.svg, icon-bag.svg, icon-filter-remove.svg, icon-search.svg, icon-sort.svg, icon-timer.svg (used by ComingSoonOverlay; vuesax/linear/timer, 104x104 viewBox, downloaded from Figma)
  cart/     ← cart + checkout chrome icons (visa.svg + amex.svg clean from Figma; mastercard / discover / applepay / gpay as PNGs after the `Cnan nan` SVG export bug — see "Figma SVG export NaN bug" below), plus checkout-result imagery: coaching-model.png (Asaad Hamawi photo for the /checkout/success coaching CTA, downloaded from Figma node 128:6031 via get_screenshot)
```

All **four role route groups originally planned** (`(customer)`, `(admin)`, `(deliveryCompany)`, `(accountant)`) now exist. The public storefront landing stays at the app root (`src/app/page.js`) — it's intentionally outside `(customer)` because it's pre-auth marketing. The role-group plan still applies for everything past sign-in:

| Requirement area | Route group |
| --- | --- |
| Storefront browse, product detail, cart, wishlist, checkout, order history, customer account hub (profile + payment methods + addresses) | `(customer)` |
| Admin dashboard, category/product/variant/image CRUD, discounts/promo codes, order management, reports, broadcast email, user management | `(admin)` |
| Delivery dashboard: list assigned orders, update status to Out for Delivery / Delivered / Failed | `(deliveryCompany)` |
| Read-only financial overview, revenue breakdown, export | `(accountant)` |

There is **no top-level `src/app/components/` folder** — shared UI lives under `src/components/<domain>/` (currently `public/homePage/`, `customer/shop/`, `auth/`, and `shared/`). Add a new domain folder when a new surface starts; promote a component up to `src/components/shared/` only when a second domain needs it (`Sidebar.jsx` lives there because both `Header` on the landing and `ShopHeader` in `customer/shop/` consume it for the mobile menu).

## Canonical route map

These are the URLs the app commits to. Nav links, redirects, and any hand-built `<Link>`s should match this shape exactly — don't invent variants.

| Surface | Route |
| --- | --- |
| Landing / home | `/` |
| Shop — category picker (no `?category=`) | `/shop` |
| Major category | `/shop?category=<slug>` |
| Sub-category filter (stacks on top of major) | `/shop?category=<slug>&sub=<slug>` |
| Product detail | `/products/[slug]` |
| Cart | `/cart` |
| Checkout — Details (step 2) | `/checkout` |
| Checkout — Payment (step 3) | `/checkout/payment` |
| Checkout — Order confirmed | `/checkout/success` |
| Checkout — Payment declined | `/checkout/failed` |
| Account hub (profile + payment methods + addresses) | `/account` |
| Order history | `/account/orders` |
| Order tracking (per order) | `/account/orders/[id]` |
| Wishlist | `/account/wishlist` |
| Contact us | `/contact` |
| Admin console index (redirects to dashboard) | `/r3pr-console` |
| Admin dashboard | `/r3pr-console/dashboard` |
| Admin — categories / products / taxonomies / orders / stock-alerts / messages / reports / wishlist-insights / storefront / subscribers / promo-codes / users / discounts / broadcast / settings | `/r3pr-console/<section>` |
| Accountant ledger index (redirects to overview) | `/r3pr-ledger` |
| Accountant — finance overview (KPIs + revenue chart + category/product/time breakdowns + discount-by-code table) | `/r3pr-ledger/overview` |
| Accountant — export history (past PDF/CSV exports, filterable + downloadable) | `/r3pr-ledger/exports` |
| Accountant — audit log (read-only trail of views, filter changes, exports, sessions) | `/r3pr-ledger/audit` |
| Delivery index (redirects to dashboard) | `/r3pr-dispatch` |
| Delivery — assigned orders dashboard (filter chips + table) | `/r3pr-dispatch/dashboard` |
| Delivery — per-order detail (customer / address / items / contact / status update CTAs) | `/r3pr-dispatch/orders/[id]` |

Notes:
- **`/shop` never shows an all-products listing.** When `?category=` is absent, render `CategoryPicker` so the user always commits to a major category before seeing products. This is a deliberate product decision — don't add an "all products" page back.
- Browse and filter state lives in **query params on `/shop`**, not nested path segments. Adding facets later (color, size, price, sort) follows the same pattern — append query params, don't add new path segments.
- Current filter query params consumed by `shop/page.js` → `parseFilters`: `category`, `sub`, `sizes`, `colors`, `materials`, `types` (all CSV), `min`, `max`, plus debug flags `empty=1` (force empty state) and `comingSoon=1` (force the coming-soon overlay over the grid). The filter group was originally `fits`; it was renamed to `materials` to match the Figma mobile filter sheet — don't reintroduce `fits` without updating both `FILTER_OPTIONS` and `ACTIVE_FILTER_COUNT` in `mockShop.js`.
- **Coming-soon overlay triggers** (handled in `shop/page.js`, on top of the `?comingSoon=1` debug flag): (1) unknown major category — `getCategory(slug)` returns falsy, (2) unknown sub-category — `?sub=<slug>` doesn't match any entry in `cat.subs`, (3) **hidden sub-category** — the matching entry has `visible: false` on its record. The page falls back to `listProducts({ category: "discover-all" }).slice(0, 8)` to populate the blurred grid behind the overlay when the real category returns nothing. Breadcrumb falls back to a humanised label via `slugToLabel(slug)`. Don't drop the `visible` check — it's the contract the admin Categories surface writes against (the field is also called `visible` in `MAJOR_CATEGORIES` / `SUB_CATEGORIES` on mockAdmin, and `is_visible` on the server schema; an earlier draft used `active` here — keep them aligned).
- **Shop page top padding** is `md:pt-24` (96px) — matches Figma 119:3858 desktop spacing below the sticky header. Don't reduce; the breadcrumb and grid both rely on it for vertical breathing room.
- `/account` is the profile + payment-methods + addresses hub (single page, not a redirect). Sibling routes `/account/orders` and `/account/wishlist` exist; there is **no** separate `/account/addresses` route — addresses are managed inline in the hub.
- `Header.jsx` slugifies major/sub names client-side for URLs. Once the backend exposes a stable `slug` column on `major_categories` / `sub_categories`, switch to that to survive renames.
- **The admin URL prefix `/r3pr-console` is intentional, not a placeholder.** It's a security-through-obscurity layer on top of the role gating that will eventually live behind these routes — don't rename to `/admin` for "tidiness". Add new admin pages under `src/app/(admin)/r3pr-console/<feature>/page.js` AND extend `SECTIONS` in `src/components/admin/layout/SideNav.jsx` so the nav picks them up.
- **The accountant URL prefix `/r3pr-ledger` follows the same obscurity pattern.** Add new accountant pages under `src/app/(accountant)/r3pr-ledger/<feature>/page.js` AND extend `SECTIONS` in `src/components/accountant/layout/SideNav.jsx`. The three current routes — Overview, Export history, Audit log — are all active; the SideNav still renders a `disabled: true` "Soon" branch if a future page needs to be staged.
- **The delivery URL prefix `/r3pr-dispatch` follows the same obscurity pattern.** Add new delivery pages under `src/app/(deliveryCompany)/r3pr-dispatch/<feature>/page.js` AND extend `SECTIONS` in `src/components/delivery/layout/SideNav.jsx`. The driver role's working surface is intentionally minimal — a dashboard + per-order detail. The dashboard's status filter chips (Out for Delivery / Delivered / Failed) collapse what would have been a separate "History" page, so resist adding one back unless the surface genuinely splits in two.

## Commands

Run from `repair/`:

- `npm run dev` — Next.js dev server (default port 3000). The `Server/` dev server also defaults to port 3000, so when running both locally start the backend first and set the frontend on another port (`PORT=3001 npm run dev`), or move the backend via its `PORT` env var.
- `npm run build` — production build.
- `npm start` — serve the built app.
- `npm run lint` — ESLint flat config (`eslint-config-next/core-web-vitals`).

There is no test runner wired up yet.

**Environment variables** (loaded from `.env.local` in dev, from the Vercel project env in staging/prod). `.env.local.example` is committed as a template — copy it to `.env.local` before first run. `.env.local` itself is gitignored via `.gitignore`'s `.env*` pattern with a `!.env.local.example` negation so the template stays tracked.

| Var | Side | Purpose |
| --- | --- | --- |
| `NEXT_PUBLIC_REPAIR_GRAPHQL_URL` | client | Where `repairClientApi.js` POSTs from the browser. Falls back to `/repair/graphql` (relative) when unset. Set to a full origin (`https://api.example.com/repair/graphql`) when the frontend and backend live on different domains. |
| `NEXT_PUBLIC_STORAGE_SECRET_KEY` | client | AES key used to encrypt the Zustand store snapshot in localStorage. **Change per environment** so a dev snapshot can't be decrypted in prod and vice versa. Generate with `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`. Public (ships in the bundle); this is obfuscation, not real secrecy. |
| `REPAIR_API_URL` | server | Where `repairApi.js` POSTs from server components. No `NEXT_PUBLIC_` prefix — never reaches the client. Defaults to `http://localhost:3000/repair/graphql`. |

Local dev tip: when the `Server/` dev server runs on port 3000 and you start the frontend with `PORT=3001 npm run dev`, set `NEXT_PUBLIC_REPAIR_GRAPHQL_URL=http://localhost:3000/repair/graphql` and `REPAIR_API_URL=http://localhost:3000/repair/graphql` so both helpers point at the backend instead of the frontend port.

## Conventions

- **JavaScript, not TypeScript.** `jsconfig.json` is the only config; do not introduce `.ts`/`.tsx` files or a `tsconfig.json` without discussing it. (Note: the sibling `client/` app is TypeScript — do not copy its patterns wholesale.)
- **Path alias `@/*` → `./src/*`** (`jsconfig.json`). Use it for cross-folder imports instead of long relative paths.
- **React Compiler is enabled** (`next.config.mjs` → `reactCompiler: true`). Don't hand-write `useMemo` / `useCallback` for things the compiler can hoist; reach for them only when profiling shows a real issue.
- **React Compiler + ternary guards — use optional chaining defensively.** The compiler's memoization analysis can pre-evaluate both branches of a ternary expression, so an "obviously guarded" access like `{state ? <X title={state.kind} /> : null}` can still throw `Cannot read properties of null (reading 'kind')` at runtime when `state` is null. **Use `state?.kind` (or hoist a guard `if (!state) return …`) even inside branches you think are protected.** This bit `CategoryManager` on initial render and required the same pattern across every admin manager component — see `src/components/admin/categories/CategoryManager.jsx` for the canonical fix (guard at the top of the save handler, `?.` on every `editing.X` access, and `{open && editing ? …}` instead of `{open ? …}` for the drawer body).
- **Server→client function-prop boundary.** Server components can't pass functions to client components — Next.js production builds throw `Functions cannot be passed directly to Client Components` and the page fails to prerender. The dashboard hits this with `DataTable`'s `columns` array (each column has a `render: (row) => …` callback). **Pattern:** when a server page needs a `DataTable`, render a tiny `"use client"` wrapper component that owns the column definitions and accepts plain `rows={…}` from the server — see `src/components/admin/dashboard/RecentOrdersTable.jsx`. Manager pages (Products, Orders, etc.) don't need this because they're already client components.
- **Tailwind v4** via `@tailwindcss/postcss`. There is no `tailwind.config.js` — theme tokens live in `globals.css` under `@theme inline { ... }`. Add new design tokens there, not in a JS config file. There is **no animation library** — splash uses `@keyframes` in `globals.css` plus inline `animation:` styles, and that's the established pattern. Don't add framer-motion / gsap / etc. without discussing.
- **Tailwind v4 + Turbopack gotcha:** arbitrary `aspect-[w/h]` classes (e.g. `aspect-[176/264]`) silently don't apply under the current Turbopack content scanner — the parent collapses to zero height and `next/image` with `fill` renders nothing. Use inline `style={{ aspectRatio: "176 / 264" }}` for image card containers. `bg-[#hex]` / `text-[#hex]` arbitrary values work fine for non-toggled classes, but conditional arbitrary classes inside template literals can also miss the scanner — when a sale price chip or strikethrough needs to toggle, prefer inline `style` (`backgroundColor`, `textDecoration`) over a conditional `bg-[#1d4ed8]` / `line-through` class. See `customer/shop/ProductCard.jsx` for the pattern. **The same scanner gotcha extends to positioning/sizing/outline arbitraries** — at least these forms have all been observed being silently dropped in `customer/cart/CartPageClient.jsx`: `top-[Npx]`, `left-[Npx]`, `right-[Npx]`, `w-[Npx]`, `h-[Npx]`, `outline-[#hex]`, `-outline-offset-1`, and even non-arbitrary classes like `w-64` in some contexts. Symptoms: an absolutely-positioned line collapses to width 0, an outlined card shows the default text color as its outline, a stepper connector lines up at `top: 0` instead of the intended Y. **Pattern:** for any absolutely-positioned divider, decorative element, or outlined surface, default to inline `style` for `left/right/top/width/height/outlineColor/outlineOffset` from the start — don't trust arbitrary positioning/outline classes here. See the multiple `style={{ left, top, width, height }}` and `style={{ outlineColor: "#…", outlineOffset: "-1px" }}` usages in `CartPageClient.jsx` for the canonical fix.
- **Responsive widths between `md` and Figma design width — flex, don't pin `lg:` widths.** Figma desktop frames are ~1376–1440 wide; pinning a child to `lg:w-[508px]` / `lg:w-[545.33px]` / etc. only works once the viewport reaches the Figma width plus padding, so every viewport between `lg` (1024) and ~1500 overflows. The pattern that works is `flex-1 min-w-0` on the flex children plus `lg:max-w-[<figma-width>]` as a ceiling — the layout fluidly fills whatever space the section's `max-w-[1376px]` and section-level padding leave, and only locks to the Figma proportions at the widest viewports. This caused the original `/products/[slug]` hero to overflow by 269px at `lg` and 109px at `xl`; the fix is in `customer/product/ProductPageClient.jsx`. Apply the same pattern when porting any Figma desktop hero into code.
- **Responsive typography ramps `md → lg → xl`, don't jump straight to Figma sizes at `md`.** When a section has very different mobile vs desktop type scales (e.g. 24px → 48px heading, 36px → 72px stat numbers), use an intermediate `md:` size, an `lg:` size, and the Figma value only at `xl:`. Otherwise the section reads as "shrunken Figma" between 768 and ~1280. The "Crafted to Last" section in `customer/product/ProductPageClient.jsx` is the worked example — see the heading + stat-value class lists for the ramp shape.
- **App Router only.** No `pages/` directory; don't add one.
- **`viewport` export in `src/app/layout.js` is load-bearing.** Without it, the page is missing `<meta name="viewport" content="width=device-width, initial-scale=1">` and mobile browsers (plus any headless screenshot tool) lay out at the legacy 980px default — every storefront page then renders content wider than the visual viewport and clips on the right. The fix is the App Router idiomatic `export const viewport = { width: "device-width", initialScale: 1 }` already in the root layout; don't remove it.
- **Figma SVG export NaN bug.** When the Figma MCP `get_design_context` API returns SVG assets, complex paths occasionally serialize a degenerate cubic curve as the literal text `Cnan nan` inside the `d` attribute. That's invalid SVG path syntax, so the renderer stops at the first invalid command — the icon ends up as a fragment of the original mark (Mastercard rendered as just two dots, Apple Pay cut off mid-"Pay", etc.). **Pattern:** before trusting any SVG downloaded from Figma's MCP, `grep -c nan` it. If it has `nan`, fall back to the PNG via the `get_screenshot` MCP API on the same node id — the screenshot pipeline rasterizes correctly because it bypasses path serialization. The 6 payment icons in `repair/public/cart/` are the worked example: `visa.svg` + `amex.svg` are clean Figma SVGs, the other 4 (mastercard / discover / applepay / gpay) are PNGs from `get_screenshot`. See `PAYMENT_BRANDS` in `CartPageClient.jsx` for the per-icon `{ name, ext, w }` map.
- **Two GraphQL paths: server-side vs. client-side, never mix.**
  - **Server components** read through `src/lib/repairApi.js` → `repairQuery(operationName, variables, { revalidate })`. ISR-cached via `next.revalidate`, no auth header. Endpoint from `REPAIR_API_URL` env (server-side, no `NEXT_PUBLIC_` prefix), defaulting to `http://localhost:3000/repair/graphql`. Used by `HeaderShell.jsx` for `myAppListCategoriesTree`. Don't reach for this from a client component — the `next.revalidate` option is server-only and the helper is unaware of auth tokens.
  - **Client components** call `repairCall(operationName, variables, { isQuery })` from `src/lib/repairAuthedApi.js`. It pulls the access token from the Zustand store, sends `Authorization: Bearer <jwt>`, and on a 401 (or the synthetic 401 we attach to `"Un Authenticated User"` application failures — see `repairClientApi.js`) it calls `useRepairStore.getState().refreshAuth()` and retries once. Endpoint from `NEXT_PUBLIC_REPAIR_GRAPHQL_URL` env (client-side, must have the prefix). Don't import `repairApi.js` from a client component, and don't roll your own fetch — every authed call goes through `repairCall` so the 401-refresh-retry behaviour is uniform.
- **Client state: Zustand + AES-encrypted localStorage.** The store lives at `src/lib/useRepairStore.js` — five persisted slices (`authInfo`, `cartInfo`, `wishlistInfo`, `lastOrder`, `checkoutInfo` — the last is in-memory only) plus one transient (`sessionExpired`). Persistence uses `createJSONStorage(() => secureStorage)` with CryptoJS AES-256, key from `NEXT_PUBLIC_STORAGE_SECRET_KEY`. The key ships in the JS bundle — treat the encryption as obfuscation; real session security is server-side refresh-token rotation. The store is mounted via `<StoreProvider>` in the root layout, which `await`s `rehydrate()` client-side, calls `maybeProactiveRefresh()` to top up a near-expired token, then fires `syncCart()` + `syncWishlist()` if the rehydrated `authInfo.isLoggedIn` is true.
- **Explicit logout vs. forced session-expiry are different code paths — keep them split.** `clearAuth()` is the **explicit-logout** path (Sign Out button) — wipes EVERYTHING user-scoped (auth + cart + wishlist + lastOrder + checkout) because the next person on this device may be a different user. `handleSessionExpired()` is the **forced-sign-out** path (refresh failed, token reuse detected server-side, account deactivated, or 30-day refresh window finally elapsed) — wipes only the auth slice and **preserves cart/wishlist/lastOrder so the user signs back in and their basket is intact**, then sets `sessionExpired: true` so StoreProvider surfaces the toast and `?next=` redirects them back to where they were. **`refreshAuth()` on failure calls `handleSessionExpired`, NOT `clearAuth`** — if you ever revert this, the user loses their cart on every 30-day rotation timeout. Sign-out buttons should call `clearAuth()` and also call `myAppLogout` on the wire to revoke the server-side refresh token.
- **Refresh token rotation is single-flight-locked AND sliding.** Access tokens expire every 30 minutes (server-side `ACCESS_TOKEN_TTL = "30m"`); refresh tokens get a fresh 30-day window on every successful rotation (server slides `expires_at` to `now + 30d`), so any user who returns within 30 days of their last visit is never logged out. A module-level `_refreshInFlight` Promise in `useRepairStore.js` ensures concurrent callers share one refresh request; without the lock, a second in-flight refresh would reuse an already-rotated token and the server's rotation-detection logic would revoke the entire token family. Don't replicate refresh logic elsewhere — call `useRepairStore.getState().refreshAuth()` (or rely on `repairCall`, which calls it for you).
- **Proactive refresh hides the 30-min access TTL from the UX.** `useRepairStore.maybeProactiveRefresh()` peeks at the JWT `exp` claim (read-only — server is the only place that can trust the signature) and rotates the token only when fewer than `PROACTIVE_REFRESH_WINDOW_SEC` (5 min) remain. StoreProvider wires it to `visibilitychange` / `focus` / `online` and to post-rehydrate; **before a sensitive flow** (opening `/checkout/payment`, placing an order, etc.) call it manually so the user never sees a 401 → refresh → retry round-trip mid-action. Cheap to call — no-op when the token is still fresh.
- **Cross-tab auth sync via `BroadcastChannel("repair-auth")`.** Every `setAuthInfo` / `clearAuth` / `handleSessionExpired` posts a message; sibling tabs receive it through `initRepairStoreListener` and apply via `applyAuthInfoFromBroadcast` / `applySessionExpiredFromBroadcast` (the applier paths deliberately do NOT rebroadcast, preventing echo storms in 3+ tab setups). Without this, two open tabs each refresh independently with their own copy of the refresh token — the second one reuses an already-rotated token, the server's reuse-detection fires family revocation, and the user is silently logged out everywhere. The single-flight lock alone doesn't help here because tabs don't share JS scope; the channel is what keeps them coherent. **If you ever add a new auth-mutating action to the store, broadcast it.**
- **`?next=` round-trip + open-redirect rules are centralized in `@/lib/authRedirect`.** AuthGuard pushes `/sign-in?next=<path+search>` when the user isn't signed in; the session-expired flow does the same. All four sign-in paths (SignInForm, SignUpForm, GoogleSignInButton, CompleteProfileForm) call `postAuthDestination({ searchParams, role })` on success — honors `?next=` if it passes `isSameOriginPath` (single leading `/`, no `//evil.com`, no `/\\evil.com`), otherwise falls back to `homeForRole(role)` (admin → `/r3pr-console`, delivery → `/r3pr-dispatch`, accounting → `/r3pr-ledger`, customer → `/shop`). **Never reach for `router.push("/shop")` directly from an auth form** — go through `postAuthDestination` so the round-trip works AND the role-routing stays in one file. GoogleSignInButton stashes the validated `next` value alongside the `signupToken` in sessionStorage so it survives the `/complete-profile` hop. CompleteProfileForm re-validates with `isSameOriginPath` on the way out because the sessionStorage payload could have been tampered with.
- **Login flows are responsible for the initial sync AND for honoring `?next=`.** `StoreProvider` only triggers `syncCart()` + `syncWishlist()` on page mount + post-rehydrate. When the user actively signs in / signs up / completes OAuth, the handler must `setAuthInfo(data)` AND fire both syncs AND push to `postAuthDestination(...)` so the header badge populates without waiting for the next navigation and the user lands at the page that bounced them:
    ```js
    const data = await repairCall("myAppLogin", { email, password });  // Query
    const store = useRepairStore.getState();
    store.setAuthInfo(data);
    store.syncCart();
    store.syncWishlist();
    router.push(postAuthDestination({ searchParams, role: data?.user?.role }));
    ```
- **Component state stays in `useState`** for things that don't span pages (drawer open/close, filter form values, banner timeouts). `ShopPageClient.jsx` keeps its drawer / cart-banner state in `useState` and lifts filter changes into URL query params via `useRouter`/`useSearchParams` from `next/navigation` — keep that pattern. Don't reach for the Zustand store for ephemeral UI state. Don't add Redux / SWR / TanStack Query without discussing — the existing two-layer split (server reads via `repairApi`, client reads + mutations via `repairCall`, shared state in Zustand) covers the surface.
- **`"use client"` only when actually needed.** Server-component-first. Current client components: `SplashSection.jsx` (drives the fade and toggles `body.style.overflow`), `Header.jsx` (menu state + `usePathname` + hover dropdown), and `shared/Sidebar.jsx` (drilldown state + `Escape` key handling). Everything else — auth pages, footer, colorway sections — is a server component. Keep it that way: when a form needs `onSubmit` or state, isolate just the interactive piece into a client component rather than flipping the whole page. For data-fetching surfaces, prefer the `HeaderShell` pattern: a thin async server component that calls `repairApi` and forwards plain props to a client child.
- **Dynamic-route client-component escape hatch for new `[param]` routes.** Next 16 server components unwrap dynamic params with `const { id } = await params`. That works, but the very first cold load of a freshly-added dynamic route was hitting 404 even with the file present — restarting the dev server cleared it. When a new dynamic route needs to work reliably from the moment the file lands (e.g. `account/orders/[id]`), make the page a `"use client"` shell that reads `use(params)` and renders an inline "not found" fallback when the lookup fails, rather than calling `notFound()`. This avoids both the cache quirk and the heavier `notFound()` boundary for what's effectively presentational mock data. See `(customer)/account/orders/[id]/page.js` for the worked example.
- **Storefront content editor — preview parity.** `/r3pr-console/storefront` ships a Show preview / Hide preview toggle next to every section's Save bar. The preview components (`HeroPreview` / `ColorwayPreview` / `StatsPreview` / `BrowseTilesPreview` / `ProductSectionsPreview` / `CoachingPreview` / `FooterPreview`) are visual approximations of the real landing — black bg with the same gradient as `HeroSection`, the inline-rebuilt `GlassCtaPill` matching `GlassButton`'s inset shadow stack, alternating two-column colorway layout matching `ColorwaySection`, white footer with circular dark social bubbles matching the real `Footer.jsx`. **When you change the live `public/homePage/*` components (Hero/Colorway/Stats/Browse/Footer), mirror the change in the matching preview** so the "PREVIEW — UNSAVED" panel keeps showing an accurate demo before Save. Don't extract the previews into the real `homePage/*` components — they're deliberately self-contained inside `StorefrontManager.jsx` so an admin-only change can't accidentally break the customer landing.

## Design system in play

Figma is the source of truth for the visual design (file `pLwxbw3UUexnNKG7lhFotU`, landing root node `2026:3962`, shop desktop set `2065:4344` (frames `119:3858` default / `126:3792` filter / `126:4397` empty / `198:5080` coming-soon / `120:6999` add-to-cart drawer / `120:7466` added-to-cart banner / `120:5217` search overlay), shop mobile set `15:810` (frames `2:12` default / `2:561` newly dropped / `2:709` category / `2:857` scroll / `5:217` filter sheet / `7:244` sale / `7:1358` add-to-cart sheet / `39:1525` search overlay) — see auto-memory `reference_repair_landing_figma.md` for per-section node IDs). Translate from Figma, don't invent.

Tokens already wired:
- `font-display` → Zalando Sans Expanded (uppercase display type — h1s, CTAs, nav)
- `font-body` → Zalando Sans (with inline `style={{ fontStretch: "75%" }}` to fake the "Zalando Sans Condensed" weight Figma uses for product names / chips on mobile — Next/font/google doesn't ship a separate Condensed family)
- `font-sans` / `font-mono` → Geist (Next default, mostly unused on these surfaces)

The four font CSS vars (`--font-geist-sans`, `--font-geist-mono`, `--font-zalando-sans`, `--font-zalando-expanded`) are wired in `src/app/layout.js` and exposed via `@theme inline` in `globals.css`. Use `font-display` / `font-body` class utilities; don't reach for the Geist vars on visual surfaces.

Color + surface conventions established by the existing pages:
- **Landing** is a black surface (`bg-black text-white`) with a deliberately white footer for visual relief.
- **Shop** (`(customer)/shop`) is a white surface (`bg-white text-[#11191f]`) with a sticky white header (`ShopHeader`) and a dark `#11191f` footer (`ShopFooter`). **The footer is desktop-only** — wrapped in `hidden md:block` at the `(customer)/layout.js` level because the Figma mobile shop screens (frame set 15:810) have no footer; do not re-render `ShopFooter` per-page or remove the `hidden md:block` wrapper without checking the mobile design. The shop has its OWN header / footer pair — do not reuse the landing's `Header.jsx` (translucent black) or `Footer.jsx` (white) inside customer surfaces.
- **Auth** is a white surface (`bg-white text-[#11191f]`) split 60/40 desktop with a hero photo on the left (`HeroPanel`, hidden under `md`) and form column on the right.
- **Admin** (`/r3pr-console/*`) is a `#f7f7f8` content panel with a dark `#11191f` sticky sidebar (`SideNav`, `lg:flex` — collapses to an off-canvas drawer below `lg`) and a white sticky `TopBar`. Cards are white with `#e5e7eb` borders, `[2px]–[4px]` radii (NOT the shop's larger radii). Accent color is `#1d4ed8` (matches the storefront sale chip), with semantic colors for status badges: success `#16a34a`, warning `#f59e0b`, danger `#dc2626`, info-purple `#a855f7`. Add new status tones to `STATUS_TONE` in `mockAdmin.js` — don't inline new hex values across components.
- **Hero CTA** on the landing → dark glass (`GlassButton`).
- **Action CTA** in auth forms → solid white pill (`AuthButton`).
- **Sale price treatment** (used on product cards in the Sale category): original `JOD 30` rendered with `textDecoration: "line-through"` + `color: "rgba(17,25,31,0.5)"`; discounted price below it as a solid `backgroundColor: "#1d4ed8"` chip with white bold text. Applied via inline `style` rather than Tailwind arbitrary classes — see the Tailwind v4 gotcha in Conventions.
- Body brand text color is `#11191f` (not pure black) — use this when matching.

When building a new landing section, follow the existing pattern: presentational server component under `src/components/public/homePage/`, sized via Tailwind, imported into `src/app/page.js` in display order. New customer-storefront surfaces (cart, checkout, account/*, products/[slug]) should mount under `(customer)/` so they pick up the `ShopHeader` + `ShopFooter` layout automatically — don't re-create the header/footer per page. Do not introduce a CMS or content layer for marketing copy yet.

## Shop drawers / overlays — animation + scroll conventions

The drawers and full-screen overlays under `customer/shop/` (FilterDrawer, AddToCartDrawer, SearchOverlay, ComingSoonOverlay) share a small animation system defined in `globals.css`. **Reuse it instead of inventing new keyframes.**

**Delayed-unmount hook.** Open/close animations only work if the element stays mounted through the exit. Each drawer file inlines its own `useDelayedUnmount(open, exitMs)` (15-line hook — duplicated, not shared) that holds `render=true` and toggles `data-state="open" | "closing"` on the aside + backdrop. CSS picks the enter / exit keyframe off the data-state, and the hook unmounts only after `exitMs`. The hook is now copied into **five** files (FilterDrawer, AddToCartDrawer, OrderFilterDrawer, WishlistFilterDrawer, admin Drawer) — the original "abstract on the fourth drawer" threshold has been crossed. Promoting to `customer/shared/useDelayedUnmount.js` (or a shared `BottomSheet` / `RightDrawer` primitive that bundles the hook + Escape handler + backdrop) is now reasonable next-cleanup work; until that lands, keep copying.

**CSS classes by surface type:**

| Class | Surface | Open animation | Exit animation |
| --- | --- | --- | --- |
| `.drawer-backdrop` | Backdrop scrim (rgba(17,25,31,0.50)) shared by all drawers | 280ms linear fade in | 280ms linear fade out |
| `.bottom-card` | Mobile bottom-anchored card — FilterDrawer mobile + AddToCartDrawer mobile + OrderFilterDrawer mobile + WishlistFilterDrawer mobile | 320ms linear slide up from `translateY(calc(100% + 24px))` (pure vertical) | 320ms linear slide back down |
| `.right-drawer` | Desktop right-side floating card — AddToCartDrawer desktop + FilterDrawer desktop + OrderFilterDrawer desktop + WishlistFilterDrawer desktop + admin Drawer | 300ms linear slide from `translateX(100%)` | 300ms linear slide back right |
| `.search-overlay` | SearchOverlay full-screen | 240ms linear fade in | 240ms linear fade out |

**All timings are `linear`** (deliberate — the previous spring/eased curves were rejected in favor of consistent mechanical motion). Don't reintroduce cubic-bezier for these.

**Mobile bottom-card centering.** `.bottom-card` is anchored at `left-0 right-0 mx-auto` (NOT `left-1/2 -translate-x-1/2`) because Tailwind v4's translate utilities use the `translate` CSS property while the keyframe uses `transform: translateY(...)` — combining them produces diagonal motion. Keep the centering off of transform.

**Reduced motion.** `globals.css` has a `@media (prefers-reduced-motion: reduce)` block that disables all four classes + `.splash-anim` + `.sidebar-view-anim`. Add any new animated drawer class to that block.

**Custom thin scrollbar.** `.drawer-scroll` gives a 4px transparent-track / `rgba(17,25,31,0.20)` thumb scrollbar (35% on hover) — Chromium via `::-webkit-scrollbar*`, Firefox via `scrollbar-width: thin` + `scrollbar-color`. Apply on any drawer/aside with `overflow-y-auto` to keep the design clean. AddToCartDrawer desktop uses it; FilterDrawer desktop uses it. Don't apply to the page itself.

**SearchOverlay placeholder typography.** `globals.css` includes `.search-overlay-input::placeholder` to set Zalando Sans Expanded Regular 12px @ `rgba(17,25,31,0.5)`. The input's resting text style is Bold 12px `#11191F` so typed content reads heavier than the placeholder, matching Figma 39:1953.

**ComingSoonOverlay sticky overlay.** When the blurred background grid exceeds viewport height (mobile, 8 cards in 2 cols = 4 rows), the centered icon+text uses `sticky top-1/2 -translate-y-1/2` so it stays visually centered as the user scrolls through the blur. On desktop where the grid fits the viewport, sticky behaves like regular centered absolute positioning. Don't replace with plain `absolute inset-0 justify-center` — that would push the overlay below the fold on tall mobile grids.

## Backend contract you are coding against

Every request from this app should go to `POST /repair/graphql`. The wire shape is the same `MyAppInput → MyAppResponse` envelope documented in the root CLAUDE.md, but in `repair` there is **no generic CRUD path** — each operation is a named resolver with its own auth + role check. The full inventory of named resolvers lives under `Server/servers/repair/src/graphql/resolvers/` (auth, catalog, adminCatalog, adminPricing, addresses, cart, orders, reports, admin). If a feature you are building has no matching resolver, the resolver needs to be added on the server side first; do not attempt to use a generic `myAppFindAll` / `myAppAddData` style call — those were removed in the May 2026 audit and a regression test fails fast if they reappear.

Auth header is `Authorization: Bearer <jwt>`. **Access tokens expire every 30 minutes** (server-side constant `ACCESS_TOKEN_TTL = "30m"` in `Server/servers/repair/src/graphql/resolvers/auth.ts`). Pair with the opaque refresh token (30-day TTL, single-use rotation — **sliding**: every successful rotation issues a fresh 30-day window, so an active user is never logged out and only genuinely abandoned sessions ever hit a hard expiry) returned alongside; exchange via `myAppRefreshToken` (a **Mutation**, not a Query — the server's `safeParse` expects camelCase `{ refreshToken }` as input).

**The client already handles all of this** — `repairCall` from `repairAuthedApi.js` auto-refreshes on 401 and retries; `useRepairStore.refreshAuth()` is single-flight-locked; `useRepairStore.maybeProactiveRefresh()` proactively rotates whenever the access token has under 5 minutes left (fired by StoreProvider on tab focus / network reconnect / post-rehydrate); `BroadcastChannel("repair-auth")` keeps every open tab's token pair in lock-step. When refresh ultimately fails (30-day window elapsed, token revoked, account deactivated, reuse detected), `handleSessionExpired()` clears the auth slice but **preserves cart + wishlist + lastOrder** and trips `sessionExpired` so StoreProvider shows a toast + redirects to `/sign-in?next=<here>`. Logout calls `myAppLogout` (single token) or `myAppLogoutAll` (all sessions for the user); both are Mutations and the client's `clearAuth()` should be called alongside so the access JWT is also dropped locally.

The `isAuth` middleware in the repair sub-server is **non-blocking** — an expired / missing / invalid token leaves `req.isAuth = false` and the request still reaches the resolver. Auth-gated resolvers then return `UNAUTH = fail("Un Authenticated User")` with **HTTP 200** + `blnRequestSuccessful: false`. The client's `graphqlFetch` recognises that exact string and synthesises `.status === 401` on the thrown error so the retry layer (`graphqlFetchWithRetry`, also used by `repairCall`) treats it the same as a real network 401. If a new server-side auth-failure code path is added later, either reuse the `UNAUTH` constant or update the message-match in `repairClientApi.js`.

## Alignment with the project requirements

Backend covers most of the feature surface (customer auth + Google OAuth + password reset + refresh rotation, catalog browsing with filtering, cart + wishlist, addresses, checkout with promo codes + per-variant `FOR UPDATE` locking, order status state machine, admin CRUD for categories/products/variants/images/discounts/promo codes, 8 aggregated reports, admin broadcast email, queued transactional emails for every status transition).

Frontend progress:
- **Public marketing landing** — built, dark theme. 10 sections composed in `src/app/page.js`. Splash overlay → fixed translucent header → hero → three colorway sections → "Crafted to last" → stats → browse-collection grid → white footer.
- **Header / nav** — `HeaderShell.jsx` fetches `myAppListCategoriesTree` server-side and forwards a shaped tree to `Header.jsx`. Desktop nav is built from the fetched categories with a hover dropdown for majors that have sub-categories; the mobile `Sidebar` uses the same tree plus static items (Home, Shop, Orders, Wishlist, Addresses, Cart). If the backend is unreachable the helper falls back to a dummy Women/Men/Sale tree so the nav still renders.
- **Auth flow** — fully wired end-to-end across all 5 routes under `(auth)/`. Layout wraps every page in `<GuestGuard>` so returning users skip the form entirely. All four sign-in paths honor `?next=<path>` via `postAuthDestination` from `@/lib/authRedirect` — they push to the bounced-from destination on success, or fall back to `homeForRole(role)` (admin → `/r3pr-console`, delivery → `/r3pr-dispatch`, accounting → `/r3pr-ledger`, customer → `/shop`).
    - **Sign-in (`/sign-in`)** → `SignInForm` calls `myAppLogin` (Query), single-string `"Invalid email or password"` on any failure (preserves the server's anti-enumeration posture), `setAuthInfo` + `syncCart` + `syncWishlist` + `router.push(postAuthDestination({ searchParams, role: data.user.role }))` on success. Reads BOTH `?reset=1` (renders the green "Password updated" banner) AND `?next=<path>` via `useSearchParams`.
    - **Sign-up (`/sign-up`)** → `SignUpForm` calls `myAppSignUp` (mutation, locked to `tableName: "users"`), validates email + per-country phone length, then same auth + sync + push. Page wraps the form in `<Suspense>` for the `useSearchParams` read (`?next=` support).
    - **Google sign-in** → `GoogleSignInButton` token-client flow (single popup, `openid email profile`). Returning Google user → straight to `postAuthDestination(...)` with auth + sync. Brand-new Google user → server returns `{ needsPhone: true, signupToken, email, name }` (no user row created yet); button stashes `{ signupToken, email, name, next }` in sessionStorage under `pendingGoogleSignup` (the `next` value survives the hop so the user lands at their original destination after finishing) and routes to `/complete-profile`.
    - **Complete profile (`/complete-profile`)** → `CompleteProfileForm` collects phone, calls `myAppCompleteGoogleSignUp` with the stashed signupToken, server creates the user row + issues access/refresh tokens, frontend auths + sync + pushes to the stashed `next` value (re-validated with `isSameOriginPath` on the way out — the sessionStorage payload could have been tampered with) or `homeForRole(role)` as fallback.
    - **Forgot password (`/email-sent`)** → `RequestPasswordResetForm` calls `myAppRequestPasswordReset`. Server returns `"Email is not registered"` for missing OR inactive accounts (single message — deliberate enumeration trade-off, documented in Server CLAUDE.md). On success the form swaps inline to a "Check Your Inbox" state with Resend + "Use a different email" affordances.
    - **Reset password (`/reset-password?token=…`)** → `ResetPasswordForm` reads `?token=` via `useSearchParams`, calls `myAppConfirmPasswordReset`, on success routes to `/sign-in?reset=1`. Server-side bonus: every refresh token for the user is revoked inside the same resolver — any active sessions on other devices are kicked at the same moment the password rotates.
    - **`<Suspense>` requirement** — `SignInForm` and `ResetPasswordForm` both read `useSearchParams`, so the pages that mount them (`/sign-in` and `/reset-password`) wrap the form in `<Suspense fallback={null}>`. Don't forget this for any new auth route that reads search params.
    - **Phone validation contract** (applies to `SignUpForm`, `CompleteProfileForm`, `ContactForm`): strip non-digits, strip the leading `0` trunk prefix, strip any duplicated dial code, THEN measure length against `phoneLengthFor(iso2)`. Build the wire format as `+${dial}${localDigits}`. The PhoneInput's `maxLength` allows one extra digit so the natural `0XXXXXXXXX` form types cleanly without the browser swallowing the last keystroke.
    - **Server uniqueness behavior**: `myAppSignUp` + `myAppCompleteGoogleSignUp` both check email + phone before insert (friendly "X is already in use" message) AND translate `SequelizeUniqueConstraintError` in the catch block so concurrent submissions that race past the pre-check still get the friendly error instead of the generic "Something went wrong".
- **Customer storefront — `/shop`** — built end-to-end as presentational UI against `src/lib/mockShop.js`. Every state from Figma is implemented: category picker (bare `/shop`), default 4-col desktop / 2-col mobile grid, sale variant with strikethrough + blue chip, **filter drawer** (bottom-anchored floating card on mobile, full-height right-side floating drawer on desktop — Figma 11:1797 + 126:3792, with stacked APPLY ALL / CLEAR ALL buttons on desktop), **add-to-cart drawer** (bottom-anchored card on mobile, floating right-side card 387px/32px-margins/radius-8 on desktop — Figma 7:1358 + 120:6999), **search overlay** (mobile full-screen + desktop fixed below the sticky header, both triggered by the ShopHeader magnifying-glass; live-filters mock catalogue with no-results "RESULTS / YOU MIGHT LIKE" state — Figma 39:1525 + 39:1953 mobile, 120:5217 desktop), "item added" banner, empty filter state (Figma 126:4397), and "more products coming soon" overlay (Figma 198:5080 — triggered by unknown major / unknown sub / `active: false` sub, plus the `?comingSoon=1` debug flag; hides FilterBar so the blur covers the full content area). **Not yet wired to the backend** — when the storefront resolvers land, swap `listProducts` / `getCategory` in `mockShop.js` for `repairQuery("myAppListProducts", { category, sub, ... })` calls in the server component (`(customer)/shop/page.js`). The mobile add-to-cart sheet deliberately drops the product image to match Figma 7:1358; the desktop floating card keeps it.
- **Product detail — `/products/[slug]`** — built end-to-end as presentational UI against an inline `MOCK_PRODUCT` in `(customer)/products/[slug]/page.js`. Mobile (below `md`): swipeable image carousel + product info + qty/CTA row. Desktop (md+): hero with vertical thumb strip (lg+) + main image + info column, then 8 stacked marketing sections (Crafted to Last → … → Complete Your Set). Fully responsive between mobile and the Figma 1376 design width — widths flex with `lg:max-w-[…]` caps rather than pinning to Figma px values, and typography ramps `md → lg → xl` rather than jumping at `md` (see the two new Conventions bullets). **Not yet wired to the backend** — swap the inline `MOCK_PRODUCT` for `repairQuery("myAppGetProductDetail", { slug })` once that resolver lands on `catalog.ts`. The `itemsLeft` / `lowStockLimit` shape maps to `variant.quantity` / `variant.low_stock_threshold`.
- **Order history — `/account/orders`** — built end-to-end as presentational UI against `src/lib/mockOrders.js`. Server component reads `?status=<csv>&date=<slug>` from the URL, runs `filterOrders(ORDERS, …)`, and forwards the filtered list + parsed filters to `OrdersPageClient`. Filter drawer mirrors the shop's exactly (mobile bottom card / desktop right drawer, same animations + chip primitives) with two axes — STATUS (multi: Delivered / On the way / Cancelled / Returned; `on-the-way` collapses processing+prepared+handed_to_delivery, plus legacy pending+dispatched) + PURCHASE DATE (single: All time / Last 30 days / Last 6 months / This year — Figma mobile 41:1420 + desktop 119:4406). EmptyOrders state when filters yield no rows. Each card carries a Track Order CTA → `/account/orders/${id}` while the order is in flight. **Not yet wired to the backend** — swap `ORDERS` for `repairQuery("myAppListMyOrders", { filters })` once the customer-scoped order-list resolver lands on `orders.ts` (admin + detail readers already exist there).
- **Order tracking — `/account/orders/[id]`** — built end-to-end as presentational UI against `src/lib/mockOrders.js` (`findOrder(id)` / `TRACKING_PIPELINE` / `trackingProgress`). Client component (`use(params)` shell — see the dynamic-route convention) renders `OrderTrackingPage`: status badge, breadcrumb back to orders, 4-step pipeline (Processing → Prepared → With Delivery → Delivered) with desktop horizontal step line + mobile vertical list, terminal-state cards for cancelled/returned, an item summary + courier/tracking card row, a reverse-chronological history list, and Inquire / Buy Again CTAs at the bottom. **Not yet wired to the backend** — swap `findOrder` for `repairQuery("myAppGetMyOrder", { id })` once the customer-scoped detail resolver lands on `orders.ts`.
- **Wishlist — `/account/wishlist`** — built end-to-end as presentational UI against `src/lib/mockWishlist.js`. Server component reads `?type=<csv>&price=<slug>` from the URL, runs `filterWishlist(WISHLIST, …)`, and forwards the filtered list + `totalCount` + parsed filters to `WishlistPageClient`. Card shape (mobile drop-shadow / desktop bordered + shadow) and split-by-variant pattern copied from OrderCard — Figma mobile 41:1613 + desktop 119:4743. Filter drawer also copied from OrderFilterDrawer with axes **TYPE** (multi, sourced from `FILTER_OPTIONS.types` in mockShop) + **PRICE** (single-select preset). **Both axes are invented — no Figma drawer spec for wishlist; swap when one lands.** EmptyWishlist branches "filters → zero rows" vs "wishlist is genuinely empty". **Not yet wired to the backend** — swap `WISHLIST` for `repairQuery("myAppListMyWishlist", { filters })` once the customer-scoped wishlist list resolver lands on `cart.ts` (toggle/clear primitives already exist there). Card heart button is presentational; wire it to `myAppToggleWishlist` when wiring.
- **Account hub — `/account`** — built end-to-end as presentational UI against `src/lib/mockAccount.js`. Single page that wraps three sections (Personal Information / Payment Methods / Address Information) and every interactive flow attached to them: star-toggle a row as default (radio semantics — at most one default per list); ADD MORE on Payment Methods opens `AddCardDrawer`; trash on a card opens `DeleteCardDrawer` (or fires the red `CannotDeleteDefaultBanner` if it's the default); successful add / delete each fire a dark slide-down toast (`CardAddedBanner` / `CardDeletedBanner`). Addresses mirror the same flows: ADD MORE / EDIT DETAILS open `AddAddressDrawer` (edit mode prefills from the structured `country`/`city`/etc. fields plus `label` + `kind`); trash opens `DeleteAddressDrawer` (or fires the red banner with `kind="address"` if default); `AddressAddedBanner` / `AddressDeletedBanner` confirm. The Address form includes an "Address Title" input + a 3-tile KindPicker (Home / Office / Other → `LocationIcon` pin) so customers can label rows freely. All state is local in `AccountClient.jsx`. **Not yet wired to the backend** — the address model already exists server-side under `addresses.ts`; the payment-methods backend is unresolved (Stripe-managed vs. local PCI-scoped table is still an open decision — see Gaps).
- **Contact us — `/contact`** — built end-to-end as presentational UI against Figma mobile 66:2449. Five-field form (first name, last name, email, country-code + phone, message) + black SEND MESSAGE CTA, hosted by a thin centered shell under `(customer)/` so it inherits ShopHeader + ShopFooter. Country picker (`CountryCodePicker.jsx`) drives a 245-country dial-code list from `src/lib/countryCodes.js` with search-by-name / dial / iso2 and emoji flags computed from regional-indicator codepoints; default is Jordan / +962. **Validation is wired**: name + email (regex) + per-country phone length (via `phoneLengthFor(country.iso2)`, applied AFTER stripping the leading 0 + duplicated dial code — same contract as the auth forms) + non-empty message; errors surface in a red inline alert above the Send Message button. **Phone input is digit-only**: `onChange` strips non-digits + clamps to `phoneLen.max`, `onKeyDown` blocks non-digit single-character keys, switching country trims the existing value to the new country's max. **Submit-side backend wiring still pending** — after validation the handler does nothing; wire to the now-existing `myAppSendContactMessage` resolver (Server CLAUDE.md confirms it's live under `resolvers/contact.ts` with an auto-acknowledgement email) and surface a "Message sent" toast on success. Reuse `countryCodes.js` for any other phone input.
- **Cart — `/cart`** — built end-to-end as presentational UI against `src/lib/mockCart.js` (Figma mobile 83:5144 + desktop 119:5240). Single `CartPageClient.jsx` owns both breakpoints via per-section `variant: "mobile" | "desktop"` rendering paths. Seeded with `DEFAULT_APPLIED_PROMO = PROMO_CODES.SUMMER25` so the Order Total renders with the green discount pill and `JOD 192.86` total out of the box. Tax formula is **9% on `subtotal + shipping`** (not `subtotal + shipping − discount`) so the seeded numbers match Figma's `JOD 17.46` / `JOD 192.86` exactly. **Not yet wired to the backend** — swap `CART_ITEMS` / `calcTotals` for `repairQuery("myAppGetMyCart", …)` once the storefront cart-read resolver lands on `cart.ts` (toggle/add/remove primitives already exist there).
- **Checkout — Details `/checkout`** (step 2) — built end-to-end as presentational UI against `CHECKOUT_CONTACT` / `CHECKOUT_ADDRESSES` / `SHIPPING_METHODS` in `mockCart.js`. Mounted under (customer)/, inherits ShopHeader + ShopFooter. Contact form, shipping-address radio list (with Add More + per-card Edit Details on desktop; mobile shows the default address + ADD NEW CARD button), delivery-method radio list (Standard / Express / Pickup), free-shipping progress banner, promo + order totals reused from cart, sticky CTA on mobile. Continue routes to `/checkout/payment`. Heading **Edit**, heading **Add More**, per-card **EDIT DETAILS**, and the mobile **ADD NEW CARD** muted button all open the same `AddAddressDrawer` from `customer/account/` (edit-mode when given an `initial` row). **Not yet wired to the backend** — addresses model already exists server-side under `addresses.ts`; swap `addressList` to `repairQuery("myAppListMyAddresses")` once a customer-scoped read lands. Shipping-rate selection is local-state only; selected method is NOT yet plumbed into `calcTotals` (the cart's flat-fee shipping math still wins).
- **Checkout — Payment `/checkout/payment`** (step 3) — built end-to-end as presentational UI against `PAYMENT_METHODS` + `CHECKOUT_ADDRESSES` in `mockCart.js`. Payment-method radio list (Visa default + Mastercard + Apple Pay + Google Pay + Cash on Delivery; brand icons are inline SVG matching Figma — see `PaymentPageClient.jsx`), read-only shipping-address display with Edit affordance, T&C checkbox gating the CTA, and the right-column composite `PaymentOrderSummaryCard` on desktop / sticky CTA "PAY & CONFIRM ORDER" on mobile. **ADD NEW CARD** opens the reused `AddCardDrawer` from `customer/account/`; Shipping Address **Edit** opens the reused `AddAddressDrawer` in edit-mode pre-filled with the displayed address. `handleAddCard` appends + auto-selects the new card; `handleSaveAddress` rebuilds the flat `line` via `buildAddressLine`. The place-order CTA now routes to `/checkout/success` via `router.push`. **Not yet wired to the backend** — the payment-methods model is still pending (Stripe-managed vs. local PCI-scoped table — see Gaps); the navigation is a placeholder until the `myAppCheckout` mutation is wired through to gate success vs. `/checkout/failed`.
- **Checkout — Order confirmed `/checkout/success`** — built end-to-end as presentational UI against `CART_ITEMS` + `CHECKOUT_ADDRESSES` in `mockCart.js` plus hardcoded order metadata constants. Matches Figma mobile 85:8692 + desktop 119:6418. Owns `OrderSuccessClient.jsx`. Inherits ShopHeader + ShopFooter from `(customer)/layout.js`. CONTINUE SHOPPING routes to `/shop`; desktop TRACK ORDER routes to `/account/orders`. **Not yet wired to the backend** — the displayed order number / total / estimated delivery / payment-method label are seeded constants; swap to the response from `myAppCheckout` (resolver already exists in `orders.ts` and returns the created order + totals).
- **Checkout — Payment declined `/checkout/failed`** — built end-to-end as presentational UI against `CART_ITEMS` in `mockCart.js` plus hardcoded transaction-failure constants. Matches Figma mobile 111:3587 + desktop 119:6627. Owns `OrderFailedClient.jsx`. Inherits ShopHeader + ShopFooter. TRY ANOTHER METHOD and RETRY PAYMENT route to `/checkout/payment`; RETURN TO CART routes to `/cart`. **Not yet wired to the backend** — `myAppCheckout` doesn't currently distinguish payment-processor rejection from validation errors, so the failure branch is unreachable until a payment adapter lands. Wire by selecting between this page and `/checkout/success` based on the mutation's `payment_status` field.
- **Other storefront pages** — none remaining past auth + landing + shop + product + cart + checkout + checkout/payment + checkout/success + checkout/failed + account hub + orders + wishlist + contact.
- **Admin console** — scaffolded end-to-end as presentational UI under `(admin)/r3pr-console/*`, **16 pages** backed by `src/lib/mockAdmin.js`. Dashboard (KPIs / line + donut charts / recent orders / top products / low stock), Categories (major+sub tree with reorder + visibility), Products (filter table + tabbed drawer with real per-color photo manager, material-tag chips, curated-collection checkboxes, color×size inventory grid), **Taxonomies** (editable Materials list + read-only Sub-Category Types panel that points to Categories), Orders (status pipeline + detail drawer + transitions), Stock Alerts (pending notify-when-available requests; status filter + search + table + detail drawer with Mark as Notified / Dismiss actions), Contact Messages (status filter chips + search + table + detail drawer with status transitions + archive/delete), Reports (7 tabs × stat cards + charts, with PDF/CSV export buttons still presentational), **Wishlist Insights** (KPIs + ranked table + per-product + bulk Notify-all flows), **Storefront Content** (editable Hero / Colorways / Stats / Browse tiles / Product page sections / Coaching CTA / Footer with image upload + per-section preview toggle that mirrors the live landing), **Subscribers** (newsletter list + CSV export + per-row Unsub/Re-sub/Delete), Promo Codes (usage progress + drawer + generator), Users (4 roles, inline active toggle, drawer), Discounts (product/sub/major scope + validity window), Broadcast (composer + live preview + sent history), Settings (6 cards — Delivery Fee / Free Delivery / Express Shipping / Shipping Methods + Pickup Locations / Tax Rate / Payment Methods). The TopBar bell icon opens `NotificationPanel` — a live dropdown aggregating processing orders + unread messages + pending stock alerts + low-stock variants into 4 coloured sections with per-item links to the relevant page. **Fully responsive**: KPI grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`; chart + secondary-column rows stay stacked until `xl:grid-cols-3` because the donut needs ≥250px for its legend; KpiCard value uses `clamp(20px, 4.2vw, 28px)` so long currency strings never overflow. **Not yet wired to the backend** — every manager is a client component holding mock data in `useState`. Swap section-by-section to `repairQuery("myAppAdmin…", …)` once the client-side auth/mutation story lands; admin resolvers already exist server-side under `Server/servers/repair/src/graphql/resolvers/{adminCatalog,adminPricing,orders,reports,admin,auth}.ts`. The contact messages, stock alerts, newsletter subscribers, wishlist insights, taxonomies (materials), curated collections, storefront content, tax settings, payment-method toggles, shipping-methods + pickup-locations surfaces all need new resolver files / tables on the server.
- **Accountant ledger — `/r3pr-ledger/{overview,exports,audit}`** — scaffolded as **3 read-only pages** backed by `src/lib/mockFinance.js`. Forks the admin shell shape (`AccountantShell` + `SideNav` + `TopBar` under `src/components/accountant/layout/`) with a finance-only nav, a slimmer TopBar (no NotificationPanel — read-only role has nothing to action; replaced with a permanent `Read-only` chip), and a SideNav footer carrying a "Read-only access" info block above Sign Out. **Overview** owns the date-range filter (7D/30D/YTD/90D presets + custom From/To inputs that filter `FINANCE_DAILY` by the implicit-2026 day labels), a 5-KPI row (Total revenue / Total orders / Discount impact / AOV / Net revenue — each with delta vs prior period and a coloured top stripe), a Revenue-over-time LineChart with Compare toggle, a Category/Product/Time-period tabbed breakdown (category = horizontal bar + donut + share list; product = ranked DataTable with inline share-bars; time = day/week/month granularity toggle that calls `rollupBy` and renders both a bar chart and a per-bucket DataTable with orders/revenue/discount/net columns), and a Discount-impact-by-code DataTable below the tabs. Export PDF + Export CSV buttons match the admin Reports posture — still presentational (see Gaps for the export-decision note). **Export history** lists past exports (file name + format tile + report + range + generator + relative timestamp + size + status + Download CTA) filterable by search / format / status. **Audit log** lists every view / filter change / export / sign-in / sign-out (actor avatar + action pill + detail + when + IP) filterable by search / kind / From/To date range — intentionally has no mutation actions. **Not yet wired to the backend** — swap `FINANCE_DAILY` / `DISCOUNT_BY_CODE` / `REVENUE_BY_PRODUCT` / `EXPORT_HISTORY` / `AUDIT_LOG` for `repairQuery("myAppFinance…", { from, to })` / `myAppFinanceListExports` / `myAppFinanceAuditLog` calls when the finance resolvers land. `Server/servers/repair/src/graphql/resolvers/reports.ts` already returns aggregated JSON close to the shape mockFinance.js exposes for the Overview slice — the exports + audit endpoints will need new resolver files (no `finance_exports` or `finance_audit_log` tables exist yet on the server; see Gaps). Wiring is mostly renaming fields + gating by `hasRole("accounting")`. The existing `USERS` mock in `mockAdmin.js` already carries an `accounting` role; backend role-check helpers will need it added too (currently `hasRole` is checked against `"admin"` in adminCatalog/adminPricing — the finance resolvers should accept either `accounting` or `admin`).
- **Delivery surface — `/r3pr-dispatch/{dashboard,orders/[id]}`** — scaffolded as **2 pages** backed by `src/lib/mockDelivery.js`. Forks the accountant shell shape (`DeliveryShell` + `SideNav` + `TopBar` under `src/components/delivery/layout/`) with a delivery-only single-item nav, a slimmer TopBar carrying an "On shift" pill (analogous to the accountant's Read-only pill), and a SideNav footer carrying a "Delivery account" info block above Sign Out. **Dashboard** owns 3 KPI stat-pills that double as filter chips (Out for Delivery / Delivered / Failed — clicking the active chip toggles back to "all"), an order id / customer / address search, an "All" + 3 status chips with live counts, and a responsive orders list (stacked cards on mobile + table at `md+`) — table columns are Order id + COD/Prepaid eyebrow / Customer (name + phone) / Address (with note italics) / Items summary / Total (with amber "Collect cash" subtitle on COD rows) / Status / Update CTA. **Per-order detail** owns the full Customer + Delivery Address (with Maps deep link) + Items + Contact + Order info + Status history layout, plus the **Mark Delivered** confirm modal (surfaces a yellow "Confirm you collected JOD <amount>" callout on COD orders) and the **Failed Delivery** reason picker modal (radio list of 6 `FAILED_DELIVERY_REASONS` + optional notes; on commit, writes `failedReason` + `failedNote` to the row + a `reason`-tagged history entry). Both flows fire a 3.2s color-coded Toast and mutate `ASSIGNED_ORDERS` in place via `mutateLocalStatus` so the SideNav count badge updates on next navigation. **Not yet wired to the backend** — there is no `delivery.ts` resolver file on the server and the new `failed_delivery` status doesn't exist in the server enum / `STATUS_TONE` map yet. See Gaps below for the wiring shopping list.

Treat anything past auth as greenfield UI work against an existing GraphQL surface. The auth pages exist and can be wired up to `myAppLogin` / `myAppSignUp` / `myAppGoogleAuth` / `myAppRequestPasswordReset` / `myAppConfirmPasswordReset` without redesigning — the forms already have the right fields. **The client-side data layer is now in place** — `repairCall` (auth + auto-refresh-retry), the Zustand store (`useRepairStore`), `StoreProvider` (rehydrate + initial sync), and AES-encrypted localStorage persistence. See the "Two GraphQL paths" + "Client state" + "Refresh token rotation" bullets under Conventions for the full pattern; every new authed surface from this point on should follow it rather than inventing its own.

Gaps worth flagging when planning work:
- The sign-up form **collects a phone number** which is now stored — `users.phone` exists on the server model, and `myAppSignUp` reads `phone` off the payload and persists it. No remaining work here; this gap line is kept for context only.
- The shop UI assumes the backend will eventually return a `salePrice` field per product (and a "Sale" major category). The current resolvers under `Server/servers/repair/src/graphql/resolvers/catalog.ts` don't surface a discounted price separately — products carry one `price` and discounts get applied at the cart/checkout boundary via `applyDiscount`. Decide whether the storefront list endpoint should pre-compute and return both `price` + `salePrice` per variant, or whether the frontend should compute it from the active discount window. The current mock matches the former shape.
- The shop UI's filter set is `sizes` / `colors` / `materials` / `types` / `priceMin..priceMax` (URL query params). There's no backend list resolver that accepts this filter shape yet — design it to match these param names so the frontend can switch over without renaming.
- The brief lists **report export as "document"**; the backend's `reports.ts` returns aggregated JSON. The admin Reports page renders presentational "Export PDF" / "Export CSV" buttons but they're no-ops — pick PDF (jsPDF / pdfmake / server-side render?) and CSV (client `Blob` is enough) before wiring.
- **Chart library: Recharts is installed** (`recharts` in `repair/package.json`). The SVG stand-ins in `Charts.jsx` have been replaced with Recharts-backed components. All charts now have hover tooltips, responsive containers, and auto-thinning X-axis labels for dense data. If the library is ever swapped, the change lives entirely in `Charts.jsx` — no other file imports from `recharts` directly. Mock data for 30-day series lives in `SALES_SERIES_30` and `SIGNUPS_SERIES_30` in `mockAdmin.js` (each row carries `current` + `prev` keys for comparison charts). The reports page PDF/CSV export is still a no-op — see the export-decision note above.
- **Payment methods has no server model yet.** The account hub renders payment-method rows from `mockAccount.js` (`{id, brand, last4, expiry, isDefault}`). Decide before wiring whether to (a) store payment methods as a local DB table (PCI-scoped) and surface customer-scoped CRUD resolvers, or (b) keep payment methods in Stripe (token-only references on the user row, all CRUD through Stripe APIs). The chosen shape determines what `myAppListMyPaymentMethods` / `myAppAddPaymentMethod` / `myAppDeletePaymentMethod` / `myAppSetDefaultPaymentMethod` return.
- **Admin → backend wiring is the next concrete data-layer step alongside the client-side auth/mutation story.** Every admin manager is currently a client component that holds mock data in `useState`. When wiring, choose whether each manager fetches its own slice or whether a higher-level layout provides a shared admin context — and pick the strategy once so all 11 surfaces look the same. `NotificationPanel` in `TopBar.jsx` currently pre-filters module-level constants; once real data lands it should derive counts from the same admin context or dedicated hooks.
- **Contact messages — backend is now wired.** Server migration 0003 adds the `contact_messages` table; resolvers `myAppSendContactMessage` (public — also queues an auto-acknowledgement email via the `contact_message` notification type), `myAppAdminListContactMessages`, `myAppAdminUpdateContactMessageStatus`, `myAppAdminDeleteContactMessage` live in `Server/servers/repair/src/graphql/resolvers/contact.ts`. Remaining frontend work: wire `/contact`'s `onSubmit` to `myAppSendContactMessage` and swap `CONTACT_MESSAGES` mock reads for `myAppAdminListContactMessages` on `/r3pr-console/messages`.
- **Stock alerts — backend is now wired.** Migration 0003 adds the `stock_alerts` table (unique on `(user_id, product_variant_id)`); resolvers `myAppRequestStockAlert` (customer — refuses if the variant is already in stock; revives `dismissed`/`notified` rows back to `pending`), `myAppAdminListStockAlerts` (enriched with customer/product/color/size + current `in_stock_now` flag), `myAppAdminUpdateStockAlertStatus` (sends `stockBackInStockEmail` on `pending → notified`), `myAppAdminDeleteStockAlert` in `resolvers/stockAlerts.ts`. Remaining frontend work: storefront product detail page needs a "Notify me when available" button on out-of-stock variants that calls `myAppRequestStockAlert`, and `/r3pr-console/stock-alerts` should swap to `myAppAdminListStockAlerts`.
- **Finance ledger — backend is now wired.** `resolvers/finance.ts` exposes `myAppFinanceDailySeries({from,to})` (per-day revenue/orders/discount/net + prior-period comparison columns), `myAppFinanceDiscountByCode({from,to})`, `myAppFinanceRevenueByProduct({from,to,limit})`, `myAppFinanceListExports`, `myAppFinanceCreateExport`, `myAppFinanceDownloadExport`, and `myAppFinanceAuditLog({from?,to?,kind?})`. **All read paths accept BOTH `admin` and `accounting` roles** (via `requireLedgerRole`) so admins can verify the ledger in QA. The Audit Log surface reuses the existing `audit_logs` table — no separate table — with `kind` chips mapping to action prefix (`finance.view*` / `finance.export` / `finance.filter*` / `auth.signin` / `auth.signout` / `finance.download_export`). The actual PDF/CSV generation is left to a worker — `myAppFinanceCreateExport` records `status: "generating"`; the worker should flip to `ready` + stamp `file_path` + `size_kb`. Migration 0003 adds the `finance_exports` table.
- **Commerce settings — backend is now wired and applied at checkout.** Migration 0003 adds `shipping_settings` (singleton), `tax_settings` (singleton), `shipping_methods` (seeded Standard/Express/Pickup, `removable: false`), `pickup_locations`, and `payment_method_settings` (seeded Visa/MC, Apple Pay, Google Pay, COD). `resolvers/commerceSettings.ts` exposes public `myAppGetCommerceSettings` (one-shot bundle for cart/checkout) and admin patches `myAppAdminUpdateShippingSettings` / `myAppAdminUpdateTaxSettings` / `myAppAdminUpdateShippingMethod` / `myAppAdminCreate|Update|DeletePickupLocation` / `myAppAdminUpdatePaymentMethodSettings` (refuses to leave zero methods enabled). **`myAppCheckout` now reads `shipping_settings` + `tax_settings` inside the transaction** and writes `shipping_method_key` + `shipping_amount` + `tax_amount` to the order (migration 0004 adds the columns). It accepts a `shippingMethodKey` input (`"standard"|"express"|"pickup"`, defaults to `"standard"`), rejects disabled methods, waives standard shipping above the free-delivery threshold, and computes tax on `(afterPromo + shipping)` when `applies_to_shipping` is true. Remaining frontend work: swap `SHIPPING_METHODS` / `PICKUP_LOCATIONS` / `TAX_SETTINGS` / `PAYMENT_METHOD_SETTINGS` mock reads on `/r3pr-console/settings` and `mockCart.js`'s hardcoded `FREE_SHIPPING_THRESHOLD` / `SHIPPING_FEE` / 9% tax for the live values returned by `myAppGetCommerceSettings` so the storefront preview matches what the server will actually charge.
- **Taxonomies — backend is now wired.** Migration 0003 adds `materials` (seeded Cotton/Polyester/Nylon/Spandex/Elastane) + `product_materials` join. Public `myAppListMaterials` + admin `myAppAdminCreate|Update|DeleteMaterial` + `myAppAdminSetProductMaterials({productId, materialIds})` live in `resolvers/taxonomies.ts`. The storefront `myAppListProducts` accepts `materialIds[]` and filters via the join. Product Type intentionally remains the sub-category — no separate taxonomy table.
- **Curated collections — backend is now wired.** Migration 0003 adds `collections` (seeded `sale`, `just-dropped-weekly`, `just-dropped-monthly`, `discover-all`) + `product_collections` join. Resolvers `myAppListCollections` (public, visible-only) + admin CRUD + `myAppAdminSetProductCollections` in `resolvers/taxonomies.ts`. The storefront `myAppListProducts` accepts both `collectionSlug` and `collectionIds[]` for scoping a curated tab.
- **Newsletter subscribers — backend is now wired.** Migration 0003 adds `newsletter_subscribers` (email unique, status `active|unsubscribed`, source `footer|checkout|popup|manual`). Public `myAppSubscribeNewsletter` (idempotent; revives unsubscribed rows; queues `newsletterWelcomeEmail`) + public `myAppUnsubscribeNewsletter` (constant-response, no enumeration) + admin list/update/delete in `resolvers/newsletter.ts`. Helper `audienceForSubscribers(source?)` is exported so the broadcast composer's "Newsletter subscribers" audience option resolves to the live active list.
- **Wishlist insights — backend is now wired.** `resolvers/wishlistInsights.ts` exposes `myAppAdminListWishlistInsights` (per-product wishlist counts + 7-day adds + stock state; KPIs across the table) and `myAppAdminNotifyWishlistHolders({ scope: "all"|"product", productId?, subject, bodyHtml })` — fans out one `wishlist_restock` email per unique active wishlist holder via `queueEmail`. Body supports the `{{first_name}}` merge tag (currently derived from the email local-part until a real `first_name` column lands on `users`).
- **Storefront content (CMS) — backend is now wired.** Migration 0003 adds the `storefront_content` key→JSON table. Public `myAppGetStorefrontContent` (returns the full section map + convenience top-level keys `hero` / `colorways` / `stats` / `browse_tiles` / `product_sections` / `coaching` / `footer`) + admin `myAppAdminUpdateStorefrontContent({ section, value })` live in `resolvers/storefrontContent.ts`. Image uploads still use `URL.createObjectURL` previews on the admin side; the chosen upload path (S3 presigned URL vs. uploadthing) is still TBD and the values returned in `value.image` are currently expected to be hosted-image URLs the admin pastes in.
- **Order pipeline alignment — done server-side.** New orders default to `status: "processing"` (the admin pipeline UI's first step) instead of `pending`. Customers can self-cancel from `pending` OR `processing` (so the customer cancel flow works against the new default). The `dispatched` / `out_for_delivery` enum values remain — the admin UI maps them to "Prepared" / "With Delivery" labels at render time.
- **Delivery surface — backend is NOT yet wired.** The `/r3pr-dispatch/*` UI is presentational against `src/lib/mockDelivery.js`. Wiring requires several pieces:
  1. **New `failed_delivery` status.** Add to the server `order_status` enum, to `ORDER_STATUSES` + `STATUS_TONE` in `src/lib/mockAdmin.js` (red tone `#fef2f2` / `#b91c1c` / `#ef4444` matching `DELIVERY_TONE` in mockDelivery), and to `canTransition` in `Server/servers/repair/src/graphql/resolvers/helpers.ts` (allow `handed_to_delivery → failed_delivery`; consider whether admin can re-dispatch a `failed_delivery` row back to `handed_to_delivery` or whether it terminates). Add a migration that extends the enum and updates the `STATUS_TONE` consumers (admin OrderManager label map).
  2. **Driver assignment.** The current schema has no `assigned_driver_id` on the orders table — admin order management implicitly assumes "all delivery-role users see all handed-over orders". Decide whether a single delivery account services everything (then no schema change needed; just gate by `hasRole("delivery")`) or whether each handover targets a specific driver (then add the column + an `myAppAdminAssignDriver` mutation on the admin handover step + scope `myAppListMyDeliveries` by `assigned_driver_id = caller.id`).
  3. **New `resolvers/delivery.ts` file.** Needs `myAppListMyDeliveries({filter?})` (returns the caller's assigned orders with the full shape mockDelivery exposes: customer name/email/phone + address + addressNote + items + paymentMethod + codAmount + status + history), `myAppGetMyDelivery({id})` (single order, scoped by `caller.id` if drivers are assigned), and `myAppDeliveryUpdateStatus({orderId, status, reason?, note?})` (writes a `reason`-tagged history entry; rejects transitions outside `handed_to_delivery → delivered` / `→ failed_delivery`; queues the `orderDelivered` / a new `orderDeliveryFailed` email via `queueEmail`). All resolvers gated by `hasRole(caller, "delivery")` (plus optionally admin for visibility).
  4. **New `orderDeliveryFailed` email template.** Lives in `Server/servers/repair/src/graphql/emailTemplates.ts`. Includes the failed reason + admin contact details + retry instructions. The admin Order detail surface should show the failed reason on the row (already in the order history but worth surfacing in the detail drawer).
