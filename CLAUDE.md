# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This file scopes guidance to the `repair/` frontend. The root `RepByRep/CLAUDE.md` covers the backend (`Server/`) and the sibling `client/` app — read it first when a task crosses the wire, since the GraphQL contract, auth model, and resolver inventory all live on the server side.

## What this app is

`repair/` is the **e-commerce frontend** for the "repair" clothing-store system described in the project brief. It is the **only** frontend for that system — there is no separate admin app. The four user roles in the requirements (Customer, Admin, Delivery, Accounting) are all served by this same Next.js app, gated by role at runtime. The sibling `client/` directory is a different product (RepByRep forms); do not import from it or copy patterns out of it.

This app talks exclusively to the **`repair` sub-server** at `POST /repair/graphql`. Do **not** point requests at `/form/graphql` — that is the formServer sub-server for the other product, with a different user model, different schema, and no e-commerce resolvers.

## Current state

Auth and marketing surfaces are largely presentational. The landing header fetches live data from the backend (the first GraphQL call wired up from this app). The customer storefront's `/shop` page is built end-to-end as a presentational shell against mock data — every state from Figma is implemented (default grid, sale/discount cards, empty state, "coming soon" overlay, filter sheet, add-to-cart sheet) but no backend call yet; the data source is `src/lib/mockShop.js` and will swap to `repairQuery("myAppListProducts", …)` when the storefront resolvers land. The **admin console** is scaffolded end-to-end as presentational UI against `src/lib/mockAdmin.js` — 12 pages under `(admin)/r3pr-console/*` (dashboard, categories, products, orders, stock-alerts, messages, reports, promo-codes, users, discounts, broadcast, settings). The **accountant ledger** is scaffolded as a single read-only Finance Overview page under `(accountant)/r3pr-ledger/*` against `src/lib/mockFinance.js`. The URL prefixes `/r3pr-console` and `/r3pr-ledger` are **deliberately obscure** so neither role surface is discoverable by URL guessing — keep them that way; don't rename to `/admin` or `/finance`.

```
src/app/
  (auth)/
    layout.js             ← white-bg split shell (HeroPanel + form column, md:w-[40%])
    sign-in/page.js
    sign-up/page.js
    email-sent/page.js    ← "enter email to receive reset link"
    reset-password/page.js
  (customer)/
    layout.js             ← white shell with sticky ShopHeader + dark ShopFooter (footer is `hidden md:block` — desktop-only, to match Figma mobile which has no footer); used by every customer-facing page past the public landing
    shop/page.js          ← server component, reads searchParams; routes to CategoryPicker when no ?category=, otherwise ShopPageClient
    products/[slug]/page.js ← server component, hands a MOCK_PRODUCT to <ProductPageClient> for now; will swap to `repairQuery("myAppGetProductDetail", { slug })` once that resolver lands
    account/
      page.js             ← server component, the customer account hub. Renders Personal Information + Payment Methods + Address Information sections in one screen by handing PROFILE / PAYMENT_METHODS / ADDRESSES to AccountClient. Addresses are managed here — there is no separate `/account/addresses` page.
      orders/page.js      ← server component, reads ?status=<csv>&date=<slug>; runs `filterOrders(ORDERS, …)` and hands the filtered list + parsed filters to OrdersPageClient
      wishlist/page.js    ← server component, reads ?type=<csv>&price=<slug>; runs `filterWishlist(WISHLIST, …)` and hands the filtered list + totalCount + parsed filters to WishlistPageClient
    contact/page.js       ← server component, narrow centered eyebrow heading + <ContactForm />. Figma mobile 66:2449 only — no separate desktop frame, so the mobile design is centered and gently scaled up at md/lg. Linked from Sidebar's `contactHref="/contact"`.
    cart/page.js          ← server component, hands off to <CartPageClient />. Pure presentational against `src/lib/mockCart.js` for now — swap to `repairQuery("myAppGetMyCart")` once the storefront cart-read resolver lands on `cart.ts` (toggle/add/remove primitives are already there). Tax formula in `mockCart.js` is **9% on `subtotal + shipping`** (no discount subtraction) so the seeded JOD 186 / JOD 8 cart computes the same `JOD 17.46 → JOD 192.86` as Figma 83:5144. Mounted under (customer)/ so it inherits ShopHeader + ShopFooter.
    checkout/page.js      ← server component, hands off to <CheckoutDetailsClient />. Step 2 of the cart→details→payment flow (Figma mobile 82:3618 + desktop 119:5592). Adds the contact-info / shipping-address / delivery-method sections on top of the shared cart chrome (stepper + free-shipping banner + items + promo + order totals + trust + payment + support). Reads from `CHECKOUT_CONTACT` / `CHECKOUT_ADDRESSES` / `SHIPPING_METHODS` in `mockCart.js`. Mounted under (customer)/.
    checkout/payment/page.js ← server component, hands off to <PaymentPageClient />. Step 3 of the checkout flow (Figma mobile 84:6733 + desktop 119:5877). Payment-method radio list (cards + Apple Pay + Google Pay + Cash on Delivery), read-only shipping-address display, T&C checkbox gating the place-order CTA. Reads from `PAYMENT_METHODS` + `CHECKOUT_ADDRESSES` in `mockCart.js`. Mounted under (customer)/.
    checkout/success/page.js ← server component, hands off to <OrderSuccessClient />. Post-checkout success screen reached when `PaymentPageClient`'s "PAY & CONFIRM ORDER" CTA fires (Figma mobile 85:8692 + desktop 119:6418). Renders the dark check badge + "Order Successful!" heading, an "Apply for coaching" promo card (with the real coach image at `/public/cart/coaching-model.png` — downloaded from Figma node 128:6031 to dodge the 7-day asset URL expiry), the order-info card (#ORD-2847391 + bag tile + Estimated Delivery + Total + Payment Method), the items list, and the shipping-address card. Mounted under (customer)/.
    checkout/failed/page.js ← server component, hands off to <OrderFailedClient />. Payment-declined screen (Figma mobile 111:3587 + desktop 119:6627). Dark X badge + "Payment Declined" heading, two-section Payment Error card (red-tinted top with reason + bottom meta rows for Transaction ID / Attempted Amount / Card Ending), items list, and a 3-CTA stack: `TRY ANOTHER METHOD` / `RETRY PAYMENT` / `RETURN TO CART`. Desktop right column swaps to an Action Card with the "ACTION REQUIRED" eyebrow + "Payment Failed" heading + Visa-chip Attempted Method row + Total Amount + the same CTAs, plus a help box with two pipe-separated "Contact Bank Support | Chat with Us" links. Mounted under (customer)/.
  (admin)/
    layout.js             ← wraps every admin page in <AdminShell> (sticky dark SideNav + white TopBar + #f7f7f8 content panel); sets metadata.robots = noindex,nofollow
    r3pr-console/
      page.js             ← redirect("/r3pr-console/dashboard")
      dashboard/page.js   ← KPIs, sales LineChart, revenue Donut, top products, low-stock alerts; renders RecentOrdersTable (client child) — see note on the function-prop boundary
      categories/page.js  ← <CategoryManager />
      products/page.js    ← <ProductManager />
      orders/page.js        ← <OrderManager />
      stock-alerts/page.js  ← <StockAlertManager /> — pending "notify when available" requests
      reports/page.js       ← <ReportsView />
      promo-codes/page.js   ← <PromoManager />
      users/page.js         ← <UserManager />
      discounts/page.js     ← <DiscountManager />
      broadcast/page.js     ← <BroadcastComposer />
      messages/page.js      ← <ContactManager />
      settings/page.js      ← <SettingsManager />
  (accountant)/
    layout.js             ← wraps every accountant page in <AccountantShell> (sticky dark SideNav + white TopBar + #f7f7f8 content panel — forks the admin shell shape but with a finance-only nav and no NotificationPanel); sets metadata.robots = noindex,nofollow
    r3pr-ledger/
      page.js             ← redirect("/r3pr-ledger/overview")
      overview/page.js    ← server component, renders <PageHeader /> from admin + <FinanceOverview /> client child
  layout.js               ← root: loads Geist + Zalando_Sans + Zalando_Sans_Expanded; sets html h-full / body min-h-full flex-col
  page.js                 ← public marketing landing (NOT under a (customer) group); renders <HeaderShell /> at the top
  globals.css             ← Tailwind v4 import + @theme tokens (font-display = Zalando Expanded, font-body = Zalando Sans) + splash keyframes

src/lib/
  repairApi.js            ← thin POST helper for /repair/graphql (server-side reads only for now; ISR-cached via fetch `next.revalidate`)
  mockShop.js             ← in-process mock catalogue for /shop. Exports MAJOR_CATEGORIES (Discover All / Just Dropped / Women / Men / Sale), ALL_PRODUCTS + SALE_PRODUCTS (with `salePrice`), `getCategory(slug)`, `listProducts({ category, sub, filters })`, `ACTIVE_FILTER_COUNT(filters)`, and FILTER_OPTIONS (numericSizes 36-48 / sizes S-XXL / 10 colors / 4 materials / 5 item types / priceRange). Swap to a repairQuery call when the backend exposes a storefront list resolver.
  mockOrders.js           ← in-process mock data for the customer `/account/orders` page. Exports ORDERS (6 sample rows with both `purchaseDate` display string + `purchasedAt` ISO date), `badgeFor(status)` (collapses pending/processing/dispatched into "On the way"), `ORDER_STATUS_OPTIONS` (4 multi-select chips — `delivered` / `on-the-way` / `cancelled` / `returned`; each carries `rawStatuses` so one chip covers all matching server-side statuses), `ORDER_DATE_RANGES` (single-select: `all` / `30d` / `6m` / `year`), `filterOrders(orders, { statuses, dateRange })`, and `ACTIVE_ORDER_FILTER_COUNT(filters)`. Swap to `repairQuery("myAppListMyOrders", …)` once the customer-scoped order-list resolver lands on `orders.ts`.
  mockWishlist.js         ← in-process mock data for the customer `/account/wishlist` page. Exports WISHLIST (4 sample rows; each carries product display info + a swatch palette + a `type` matching FILTER_OPTIONS.types from mockShop), `WISHLIST_TYPE_OPTIONS` (multi-select pills sourced from `FILTER_OPTIONS.types`), `WISHLIST_PRICE_RANGES` (single-select preset: `all` / `under30` / `30to50` / `over50`), `filterWishlist(items, { types, priceRange })`, and `ACTIVE_WISHLIST_FILTER_COUNT(filters)`. **Filter axes (TYPE + PRICE) are invented** — there's no Figma drawer spec for wishlist; swap them when one lands. Swap to `repairQuery("myAppListMyWishlist", { filters })` once the customer-scoped wishlist list resolver lands on `cart.ts` (toggle/clear primitives are already there).
  mockAccount.js          ← in-process mock data for the customer `/account` hub. Exports PROFILE (email / phone / DOB), PAYMENT_METHODS (`{id, brand, last4, expiry, isDefault}`), and ADDRESSES (`{id, label, kind ("home"|"office"|"other"), line, phone, isDefault}` plus the structured `country`/`city`/`neighborhood`/`street`/`building`/`apartment` fields the edit drawer reads back). Swap to `repairQuery("myAppGetMyAccount", …)` + customer-scoped address/payment-method resolvers as they land — the address model exists in `addresses.ts` on the server; payment-methods is not yet modelled (decision pending: Stripe-managed vs. local PCI-scoped table).
  countryCodes.js         ← ISO 3166-1 alpha-2 country list (~245 rows) with `{iso2, name, dial}`. Exports `flagEmoji(iso2)` which derives the flag at render time from regional-indicator codepoints (no static flag table), and `DEFAULT_COUNTRY` (Jordan / +962 — the project's home market, matches `JOD` pricing throughout the storefront). Consumed by the contact form's `CountryCodePicker`; reuse this list for any future phone-number input.
  mockCart.js             ← in-process mock data + pricing helpers for the cart + checkout flow. Exports CART_ITEMS (3 sample rows), PROMO_CODES (`SUMMER25` + `FIRST15`) + SUGGESTED_PROMOS, `formatJOD`, `calcSubtotal` / `calcDiscount` / `calcShipping` / `calcTotals` (the canonical 9%-on-`subtotal+shipping` tax formula matches Figma's JOD 186 → JOD 192.86), FREE_SHIPPING_THRESHOLD (200 JOD) + SHIPPING_FEE (8 JOD flat below the threshold). **Checkout-flow extensions**: CHECKOUT_CONTACT (email + dial-code + phone), CHECKOUT_ADDRESSES (Home + Office rows, each carrying both the flat `line` display string AND the structured country/city/neighborhood/street/building/apartment fields the `AddAddressDrawer` reads back), SHIPPING_METHODS (Standard / Express / Pickup) + DEFAULT_SHIPPING_METHOD_ID, PAYMENT_METHODS (Visa + Mastercard cards + Apple Pay + Google Pay + Cash on Delivery) + DEFAULT_PAYMENT_METHOD_ID, and `buildAddressLine(parts)` — rebuilds the flat `line` string from the structured drawer-submit fields (mirrors the pattern in customer/account/AccountClient). Swap to `repairQuery("myAppGetMyCart", ...)` + customer-scoped address / payment-method resolvers as they land on the server.
  mockFinance.js          ← in-process mock data + helpers for the accountant `/r3pr-ledger/overview` page. Derives from `mockAdmin.js` so a single edit there propagates here. Exports **FINANCE_DAILY** (30 rows synthesised from `SALES_SERIES_30` — each `{ day, revenue, prevRevenue, orders, prevOrders, discount, prevDiscount, net, prevNet }`; AOV ≈ JOD 75, discount ≈ 14% of gross), **DISCOUNT_BY_CODE** (joined to `PROMO_CODES` from mockAdmin + 2 synthetic codes, sorted by impact), **REVENUE_BY_PRODUCT** (extends `TOP_PRODUCTS` with 5 more rows so the product breakdown table doesn't read empty), and helpers `aggregate(rows)` / `pctDelta(curr, prev)` / `rollupBy(rows, "day"|"week"|"month")` (the rollup parses the implicit-2026 `"May 3"`-style day labels and buckets to ISO-week-ish or `Mon YYYY` keys). Re-exports `REVENUE_BY_CATEGORY` + `formatCurrency` + `formatNumber` from mockAdmin so the page only imports from one lib. Swap to `repairQuery("myAppFinance…", { from, to })` calls when the finance resolvers land — `Server/servers/repair/src/graphql/resolvers/reports.ts` already returns aggregated JSON close to this shape, so wiring is mostly renaming fields.
  mockAdmin.js            ← in-process mock data for every admin surface. Exports KPIS, SALES_BY_DAY (7-day, used by the dashboard), **SALES_SERIES_30** (30-day, used by the reports Sales tab — each row `{ day, current, prev }` for comparison-line support), **SIGNUPS_SERIES_30** (30-day customer sign-ups, same `{ day, current, prev }` shape, used by the reports Customers tab), REVENUE_BY_CATEGORY, TOP_PRODUCTS, RECENT_ORDERS, LOW_STOCK, MAJOR_CATEGORIES + SUB_CATEGORIES, COLORS, SIZES, PRODUCTS (with per-variant qty + `subtitle` string + `composition` array of `{ pct, material }` rows), ORDERS (with history timeline), PROMO_CODES, DISCOUNTS, USERS (admin/delivery/accounting/customer), BROADCAST_HISTORY, **CONTACT_MESSAGES** (9 sample messages; each row `{ id, firstName, lastName, email, phone, message, date, status }` where status ∈ `unread|read|replied|archived`), **STOCK_ALERTS** (8 sample notify-when-available requests; each row `{ id, customer: { name, email }, product, color, size, requestedAt, status }` where status ∈ `pending|notified|dismissed`), ORDER_STATUSES, STATUS_TONE (badge-color map keyed by status / role / payment state), plus `formatCurrency` / `formatNumber` helpers. **ORDER_STATUSES has no `pending` entry** — the 6 statuses are `processing` / `prepared` / `handed_to_delivery` / `delivered` / `cancelled` / `returned`. Orders start at `processing` (history entries begin `from: null, to: "processing"`). STATUS_TONE entries for the order pipeline: `processing` blue `#1d4ed8`, `prepared` teal `#0d9488`, `handed_to_delivery` indigo `#4f46e5`, `delivered` green `#16a34a`; `dispatched` is kept as a legacy fallback tone only. STATUS_TONE entries for contact messages: `unread` blue, `read` grey, `replied` green, `archived` muted slate. STATUS_TONE entries for stock alerts: `notified` green (same tone as `replied`/`delivered`), `dismissed` muted slate (same tone as `archived`) — all tones are in the shared `STATUS_TONE` map so `StatusBadge` resolves them automatically. Swap individual sections to `repairQuery("myAppAdminXxx", …)` as the admin client wiring lands.

src/components/
  public/homePage/        ← landing-page sections
    SplashSection.jsx     ← 4-tile entry overlay, "use client", locks body scroll 3s then fades 700ms
    HeaderShell.jsx       ← async server component; fetches myAppListCategoriesTree (with dummy fallback), passes shaped tree to Header
    Header.jsx            ← "use client" — fixed translucent nav (bg-black/30 + backdrop-blur). Renders desktop nav + hover dropdown from `categories` prop, and forwards a sidebar item list to Sidebar
    HeroSection.jsx
    ColorwaysIntro.jsx
    ColorwaySection.jsx   ← reusable, used 3× from page.js (Bright White multi-swatch, Deep Blue, Fresh Green)
    CraftedToLast.jsx
    StatsSection.jsx
    BrowseCollection.jsx
    Footer.jsx            ← white footer (contrasts the otherwise black landing); NOT used on /shop — see ShopFooter
    GlassButton.jsx       ← shared dark-glass CTA used by hero
  customer/shop/          ← shop surface (matches Figma file pLwxbw3UUexnNKG7lhFotU, frame sets 2065:4344 desktop + 15:810 mobile)
    ShopHeader.jsx        ← "use client" — white sticky header with scroll-shadow. Mobile: sort icon | RE logo (absolutely centered) | search + bag. Desktop: HOME/JUST DROPPED/WOMEN/MEN nav | RE logo | search + bag(+count badge) + account
    ShopFooter.jsx        ← dark #11191f footer, 4 columns (Brand / Shop / Help / Newsletter) + bottom bar (copyright + Privacy / Terms / Cookies)
    FilterBar.jsx         ← "HOME/<CATEGORY>" breadcrumb + product count + Filter button. Mobile = small text link, desktop = outlined chip with inline 3-track "tune" SVG icon (vuesax-style: 3 horizontal lines + circular knobs at x=4.5 / 8.25 / 3.75 — see Figma 119:3876)
    FilterDrawer.jsx      ← "use client" — splits into MobileCard (bottom-anchored floating card matching AddToCartDrawer footprint; Figma 11:1797) + DesktopCard (full-height right-side floating drawer, 387px, 32px margins, radius-8; Figma 126:3792 → node 126:4056). Mobile = chips 24x24 / 1px border / 2px radius / footer side-by-side; desktop = chips 32x32 / 1.5px border / 3px radius / pills with 14px text / 16x16 price-slider handles / STACKED buttons (APPLY ALL filled on top, CLEAR ALL outlined below, h:48 each). Chip/swatch/pill components are parameterized so both breakpoints reuse them.
    ProductCard.jsx       ← "use client" — splits into MobileCard (compact prop, 176x264 image, 8x8 swatches, 24x24 plus button) + DesktopCard (326x489 image, 12x12 swatches @ left:16/bottom:16, 40x40 plus button @ right:16/bottom:16 with white/70 outline). Subtitle uses #78716c (stone-500). Sale layout: strikethrough JOD price + blue chip "JOD 15.99" below
    ProductGrid.jsx       ← renders two separate grids (mobile 2-col @ gap-x-2 gap-y-4 + desktop 4-col @ gap-6) with respective ProductCard variants — DO NOT use a single grid with display:contents (the wrapper propagates children to the grid container and breaks the layout)
    EmptyState.jsx        ← filter-remove icon (88x88 desktop / 48x48 mobile) + 22px Zalando Sans Expanded Medium "NO PRODUCTS WERE FOUND / TRY REMOVING A FILTER." (Figma 126:4397). flex-1 + items-center + justify-center so it fills the remaining vertical space below FilterBar
    ComingSoonOverlay.jsx ← grayscale + 8px-blurred product grid (8 cards both breakpoints: mobile 2x4 / desktop 4x2) + sticky centered overlay (104x104 timer icon + 28px Zalando Sans Expanded Medium "MORE PRODUCTS ARE COMING SOON. STAY TUNED."; Figma 198:5080). Sticky positioning keeps the overlay in the viewport center as the user scrolls through the tall mobile grid. Rendered without FilterBar — see ShopPageClient.
    CategoryPicker.jsx    ← rendered at bare /shop (no ?category= param). Grid of major-category tiles with image + name + tagline
    AddToCartDrawer.jsx   ← "use client" — quick-add dialog from the product-card plus button. Mobile: bottom-anchored card (361px wide, 24px above the viewport bottom; NO image, just COLOR / SIZE / QUANTITY / "ADD TO CART"; Figma 7:1358 → node 12:2286). Desktop: floating right-side card 387px wide / 32px margins top-right-bottom / radius-8 / WITH image + price; chip sizes 32x32 / 1.5px border / 3px radius (Figma 120:6999 → node 120:7368). Has its own minimal scrollbar via the `.drawer-scroll` class.
    SearchOverlay.jsx     ← "use client" — splits into MobileBody (full-screen white overlay; Figma 39:1525) + DesktopBody (fixed overlay slotted below the 80px sticky ShopHeader at top:80/inset-x-0/bottom:0, z-20 so the header's z-30 stays on top; Figma 120:5217). Mobile: 40px outlined input → "MOST POPULAR" / "RESULTS" rows (56x84 thumb + title/subtitle + 4 swatches + price). Desktop: 1px #11191F outlined search bar (radius 8, 17px T/B + 25px L/R, 0/4/12 shadow) with Zalando Sans Expanded Bold 18px uppercase input + 15x20 close-X on the right; "SEARCH RESULTS" / "MOST POPULAR" heading (Zalando Sans Expanded SemiBold 18/28, 0.45px tracking); 4-col grid (24px gap) reusing `ProductCard` DesktopCard (capped 8 cards empty / 12 filtering). Both variants share `query` state, scroll-lock, Escape-to-close, autofocus the visible input via `matchMedia("(min-width: 768px)")`, and linear 240ms fade via `.search-overlay`. Live-filters `listProducts({ category: "discover-all" })` by name + subtitle. No-results state on both breakpoints: centered "NO PRODUCTS WERE FOUND / TRY SEARCHING FOR SOMETHING ELSE." + "YOU MIGHT LIKE" 4 suggestions. Triggered by the magnifying-glass icon in ShopHeader on both mobile and desktop. Typed mobile text is Bold 14px #11191F (placeholder rule lives in globals.css).
    AddedToCartBanner.jsx ← "use client" — top toast that auto-dismisses after 3s (Figma 120:7466)
    ShopPageClient.jsx    ← "use client" — owns drawer + cart-banner state, lifts filter changes back into URL query params via next/navigation router. Hides FilterBar when `showComingSoon` is true so the blur extends from below the header to the footer. Render order: ComingSoonOverlay → EmptyState → ProductGrid.
  customer/account/       ← post-auth account surfaces (orders: Figma mobile 41:1420 + desktop 119:4406; wishlist: Figma mobile 41:1613 + desktop 119:4743)
    OrderCard.jsx         ← exports `MobileOrderCard` (drop-shadow card, 56x84 image, Zalando Sans Condensed body, h-32 buttons, 10px CTA text) + `DesktopOrderCard` (1px #f3f4f6 border + shadow, 96x144 image, Zalando Expanded Bold title/price, h-48 buttons, 14px CTA text). Split-by-variant per the customer/shop/ProductCard pattern because the title font family — not just size — differs.
    OrderStatusBadge.jsx  ← inline-SVG pill (no Figma asset URLs — those expire after 7 days). Tones: `delivered` #dcfce7/#166534 + check-circle, `on-the-way` #dbeafe/#1e40af + truck, `cancelled` #fee2e2/#991b1b + x-circle, `returned` #f3e8ff/#6b21a8 + return arrow. `size="sm"` for the mobile card (10px text), default for desktop (12px).
    OrderFilterDrawer.jsx ← "use client" — same footprint and motion as the shop FilterDrawer (mobile `.bottom-card`, desktop `.right-drawer`, shared `.drawer-backdrop`, local `useDelayedUnmount` hook, Escape-to-close). Sections: STATUS (multi pills from ORDER_STATUS_OPTIONS) + PURCHASE DATE (single-select pills from ORDER_DATE_RANGES). Mobile footer = side-by-side CLEAR ALL + APPLY (N) h-32; desktop footer = stacked APPLY ALL (filled) on top + CLEAR ALL (outlined) below, h-48.
    OrdersPageClient.jsx  ← "use client" — owns the Filter drawer's open/close state. Renders mobile single-column / desktop xl:3-col grid (lg:2-col between), shows "Filter (N)" with the active count, lifts filter changes back into URL query params via next/navigation (`?status=…&date=…`). Renders an EmptyOrders block when the filter set yields zero rows.
    WishlistCard.jsx        ← exports `MobileWishlistCard` (drop-shadow card, 56x84 image, 4 small 12x12 swatches with 0.75px rgba(17,25,31,0.10) border, 32x32 outlined heart button, h-32 buttons, 10px CTA text — Figma 41:1613 cards under 77:2248) + `DesktopWishlistCard` (1px #f9fafb border + drop shadow, 96x144 image, 24x24 swatches with 1.714px #e5e7eb border, 36x36 #e5e7eb heart button, h-48 buttons, 11px CTA text @ tracking 0.55px — Figma 119:4743 cards 126:3671 / 126:3697 / 119:4809 / 126:3723). Inlines the heart SVG (no Figma asset URLs). Split-by-variant per the OrderCard pattern.
    WishlistFilterDrawer.jsx ← "use client" — copy of OrderFilterDrawer's structure (same `.bottom-card` / `.right-drawer` / `.drawer-backdrop` classes, same local `useDelayedUnmount`, same chip primitive). Axes: TYPE (multi pills from `WISHLIST_TYPE_OPTIONS`, sourced from `FILTER_OPTIONS.types` in mockShop) + PRICE (single-select preset pills from `WISHLIST_PRICE_RANGES`). **Both axes are invented** — no Figma drawer spec for wishlist; swap when one lands.
    WishlistPageClient.jsx  ← "use client" — owns the Filter drawer's open/close state. Renders mobile single-column / desktop xl:3-col grid (lg:2-col between), shows "Filter (N)" with the active count, lifts filter changes back into URL query params via next/navigation (`?type=…&price=…`). EmptyWishlist branches by `totalCount > 0` — "NO ITEMS MATCH YOUR FILTERS" when filtered to zero vs "YOUR WISHLIST IS EMPTY" when there genuinely are none.
    AccountClient.jsx       ← "use client" — central state holder for the `/account` hub. Owns `paymentMethods` + `addressList` state and the open/close state for every drawer + toast on the page (add-card, delete-card, add/edit-address, delete-address, plus card-added / card-deleted / address-added / address-deleted / cannot-delete-default banners). Renders Personal Information + Payment Methods + Address Information cards once each, with `md:hidden` / `hidden md:flex` swapping the mobile vs desktop variants from a single component instance — **don't mount AccountClient twice per breakpoint** (creates two independent state instances, drawers and toasts fall out of sync). The default-toggle helper `setSoleDefault(rows, id)` enforces the at-most-one radio behaviour for both payment methods and addresses (see auto-memory `feedback_repair_default_toggle.md`). `cannotDeleteKind` is a `null | "card" | "address"` triple-state that doubles as visibility + which copy the shared `CannotDeleteDefaultBanner` should render.
    PersonalInformationCard.jsx ← exports `MobilePersonalInformationCard` + `DesktopPersonalInformationCard` plus the shared `SectionCard` + `SectionHeader` chrome (re-used by Payment + Address sections). Read-only on both breakpoints.
    PaymentMethodsCard.jsx  ← exports Mobile + Desktop variants. Each row renders a `BrandTile` (Visa / Mastercard inline glyphs — no Figma asset URLs because those expire in 7 days), name + (Default) chip, expiry, star toggle, and trash. ADD MORE header link triggers the add-card drawer.
    AddressInformationCard.jsx ← exports Mobile + Desktop variants. `AddressKindIcon` resolves the row's `kind` to `HomeIcon` / `BuildingIcon` / `LocationIcon` (pin) — the third option is for arbitrary user-chosen labels like "Mom's" / "Warehouse". Each row carries star toggle + trash + EDIT DETAILS (opens the same `AddAddressDrawer` in edit mode).
    AccountIcons.jsx        ← inline SVG icon library for the hub. `StarIcon` uses the exact Figma vuesax/linear/star path with a `filled` prop (filled = `currentColor`, unfilled = stroke only); `TrashIcon` hardcodes its stroke to `#A50013` (red danger affordance); `DangerIcon` hardcodes stroke white (sits on the red error-toast background). `HomeIcon` / `BuildingIcon` / `LocationIcon` are simple filled glyphs that inherit color. `BrandTile` renders inline VISA / Mastercard logos. **All icons are inlined deliberately — referencing the Figma asset URLs breaks after 7 days when they expire.**
    AddCardDrawer.jsx       ← "use client" — Figma mobile 79:3149. Mobile bottom-card + desktop right-drawer (same animation conventions as the shop drawers). Permissive validation (≥12 digits + name + 4 expiry digits + 3+ CVV digits) — the expiry input auto-inserts the `/` between MM and YY/YYYY. ADD CARD button background set inline (`#11191F` / `rgba(17,25,31,0.5)`) per the Tailwind v4 + Turbopack gotcha for conditionally-rendered components.
    DeleteCardDrawer.jsx    ← "use client" — Figma mobile 79:2191. Confirmation drawer with the same mobile/desktop footprint as `AddCardDrawer`. Body shows a `BrandTile` + "{Brand} ending in {last4}" + expiry on a `#f0f0f0` grey strip. Footer is CANCEL outlined + DELETE CARD red (`#A50013`, inline-styled). Last-shown method held in a ref so the preview survives the exit animation.
    AddAddressDrawer.jsx    ← "use client" — Figma mobile 79:4500. Same mobile/desktop footprint. Optional `initial` prop puts the drawer in edit mode: title becomes "EDIT ADDRESS", submit becomes "SAVE CHANGES", fields pre-fill from the row. Re-keyed by `initial?.id ?? "new"` so consecutive opens get a fresh form-state bucket. Form fields: Address Title + 3-tile KindPicker (Home / Office / Other) + Country / City / Neighborhood / Street / [Building | Apartment]. Apartment is optional; every other field is required. Submit emits `{ label, kind, country, city, neighborhood, street, building, apartment }`.
    DeleteAddressDrawer.jsx ← "use client" — Figma mobile 79:5098. Twin of `DeleteCardDrawer`. Body shows the kind icon on a white inset over `#f0f0f0` grey strip + label + (Default) chip + the address line + phone. Footer: CANCEL + DELETE ADDRESS (`#A50013`).
    CardAddedBanner.jsx     ← "use client" — dark `#11191F` full-width slab toast, "CARD ADDED SUCCESSFULLY", 3s auto-dismiss, fixed at `top-[64px] md:top-[80px]` (below the sticky ShopHeader). Background set via inline `style.backgroundColor` per the gotcha.
    CardDeletedBanner.jsx   ← "use client" — same chrome as `CardAddedBanner`, copy "CARD DELETED".
    AddressAddedBanner.jsx  ← "use client" — same chrome, copy "ADDRESS ADDED SUCCESSFULLY". Fired only on add, not on edit (the edit branch in `saveAddress` returns early).
    AddressDeletedBanner.jsx ← "use client" — same chrome, copy "ADDRESS DELETED".
    CannotDeleteDefaultBanner.jsx ← "use client" — red `#A50013` full-width slab with `DangerIcon`, 4s auto-dismiss, `role="alert"` `aria-live="assertive"`. Accepts a `kind` prop (`"card"` | `"address"`) that interpolates into "You can't delete your default {kind}" — same component serves both delete flows.
  customer/product/       ← product detail surface (matches Figma 2119:2515 hero + the 8 marketing sections below)
    ProductPageClient.jsx ← "use client" — full product detail page. Mobile: swipeable image carousel + product info + CTAs (touch-pan-y, 15%/50px swipe threshold). Desktop hero (md+): thumbnail strip (lg+ only) + main image + info column. **Widths flex** with `flex-1 min-w-0` and `lg:max-w-[508px]` / `lg:max-w-[545.33px]` caps — do NOT re-pin `lg:w-[508px]` / `lg:w-[545.33px]` (that's what overflowed everything between lg and the Figma 1376 design width — see Conventions). Below the hero, 8 shared marketing sections: Crafted to Last (stats grid-cols-3) → Stays Dry → Move Without Limits → Sculpted Support → Four Colorways → mobile-only Gallery row → The Details → Complete Your Set (related products grid). Inline `PlusIcon` / `MinusIcon` / `HeartIcon` / etc. avoid the 7-day Figma asset URL expiry. `LowStockBanner` reads `itemsLeft` + `lowStockLimit` from the product object — once wired, derive from `variant.quantity` + `variant.low_stock_threshold` (catalog.ts surfaces both). `CtaRow` and `PriceBlock` / `DesktopPriceBlock` are shared between mobile + desktop blocks.
  customer/contact/        ← contact-us surface (Figma mobile 66:2449 only)
    ContactForm.jsx       ← "use client" — 5-field presentational form (first name / last name / email / country-code + phone / message) + black SEND MESSAGE CTA. No onSubmit yet — matches the auth-pages posture. Every field wears the `.contact-input` class so native input + ::placeholder font-family pin to Zalando Sans Expanded Medium (the body's `font-family: Arial` would otherwise leak into native form controls in some UAs — see globals.css for the rule, and the matching `.add-card-input` precedent). Field heights/text scale up at md+ (40→48px, 10→12px) while preserving the mobile Figma proportions below md.
    CountryCodePicker.jsx ← "use client" — dial-code popover anchored to the `+XXX` cluster inside the phone field. Closes on outside-click + Escape. Search filters by country name / dial / iso2. Flag emoji computed from `flagEmoji(iso2)` (`@/lib/countryCodes`) — no static flag table. Defaults to Jordan / +962 via `DEFAULT_COUNTRY`.
  customer/cart/           ← /cart + /checkout + /checkout/payment + /checkout/success + /checkout/failed surfaces (Figma cart mobile 83:5144 + desktop 119:5240; details mobile 82:3618 + desktop 119:5592; payment mobile 84:6733 + desktop 119:5877; success mobile 85:8692 + desktop 119:6418; failed mobile 111:3587 + desktop 119:6627)
    CartPageClient.jsx    ← "use client" — owns the /cart page AND exports the shared chrome reused by the details + payment pages. Splits each section into per-variant render paths driven by a `variant: "mobile" | "desktop"` prop. **Named exports** consumed by sibling clients: `Icon`, `CHECKOUT_STEPS`, `condensed`, `Stepper`, `FreeShippingBanner`, `ItemsSection`, `PromoCodeSection`, `OrderTotalsBlock`, `TrustBadgesRow`, `PaymentMethodsRow`, `SupportCard`, `PoliciesFootnote`, `StickyCheckoutBar`. **`Stepper` accepts `activeStep: "cart" | "details" | "payment"`** (default `"cart"`) and derives per-step status (`completed`/`active`/`pending`). Step 1 (`"cart"`) special-cases its `active` state to show the check icon (the entry-point step visually merges with `completed`); steps 2+ show their index number when active. Desktop also colors each connector line: BLACK `#11191f` when the lower step is `completed`, GREY `#e5e7eb` otherwise — so /cart shows two grey lines, /checkout shows cart→details black + details→payment grey, /checkout/payment shows both lines black. Desktop stepper geometry follows Figma's exact absolute offsets: 3 steps at `left: 0 / 296.28 / 609.97` inside a 672×56 container; 2 connector lines centered at `top: 25` with 224px visible width (256 outer − 32 px-4 padding). `StickyCheckoutBar` accepts `ctaText` (default "Continue to Next Step"; payment page passes "PAY & CONFIRM ORDER"). Cart's "Continue to Next Step" routes to `/checkout` via `router.push`; promo chips render in the SAME outlined-white state on both mobile and desktop — the "applied" indicator lives only on the green discount pill in the Order Total card, never on the chips themselves. **Responsive split** between md and lg: the desktop two-column layout stacks below `lg` (1024px) — the right Order Summary column drops below the items list — and only goes side-by-side at `lg+` where the 901.33 + 426.66 px Figma geometry fits cleanly. Don't pin `lg:w-[901.33px]` on the left column; use `flex-1 min-w-0` + `lg:max-w-[901.33px]` (and `lg:w-[426.66px] lg:shrink-0` on the aside).
    CheckoutDetailsClient.jsx ← "use client" — owns the /checkout page (step 2). Imports the shared chrome from CartPageClient as named exports + adds the details-only sections inline: `ContactInformationSection`, `ShippingAddressSection` (mobile single card + ADD-NEW-CARD muted button; desktop multi-card radio list with Add More + per-card EDIT DETAILS), `DeliveryMethodSection` (3 radio rows — Standard / Express / Store Pickup; selected card 2px dark border on mobile, 1px dark border on desktop; pickup price renders as green "Free"), `OrderSummaryToggle` (mobile-only collapsed bar that scrolls to the items section), `MiniItemsList` + `DesktopOrderSummaryCard` (desktop right-column composite: ORDER SUMMARY heading + mini items list + ORDER TOTAL breakdown + Total row + Continue CTA + policy line + Encrypted & Secure pill). Owns `addressList` state + an `addressDrawer = { open, initial }` discriminator that drives the reused `AddAddressDrawer` from `customer/account/` — heading **Edit** (mobile), heading **Add More** (desktop), per-card **EDIT DETAILS** (desktop), and the mobile **ADD NEW CARD** muted button all open the drawer in the matching mode. `handleSaveAddress` rebuilds the flat display `line` via `buildAddressLine` from `mockCart.js`. Continue routes to `/checkout/payment` via `router.push`.
    PaymentPageClient.jsx ← "use client" — owns the /checkout/payment page (step 3). Reuses the same shared chrome + adds the payment-only sections inline: `PaymentMethodSection` (radio list with one row per method; selected card 2px dark border on mobile, 1px dark border on desktop; ADD NEW CARD muted button below opens the reused `AddCardDrawer` from `customer/account/`), `ShippingAddressDisplay` (read-only address card with an Edit button that opens the reused `AddAddressDrawer` in edit-mode pre-filled with the shipping address), `TermsCheckbox` (T&C agreement that gates the place-order CTA — pill-wrapped #fafafa on mobile, plain row on desktop), `PaymentOrderSummaryCard` (desktop right column composite — mini items list + totals + embedded T&C + `CONFIRM & PAY` CTA + policy line + Encrypted & Secure pill). Mobile sticky CTA reuses `StickyCheckoutBar` with `ctaText="PAY & CONFIRM ORDER"`. **The place-order CTA now `router.push("/checkout/success")`** — it used to fire a `window.alert` placeholder. `handleAddCard` appends to `paymentMethods` state + auto-selects the new card. **Brand tiles are inline SVG components** (`VisaTile` / `MastercardTile` / `ApplePayTile` / `GooglePayTile` / `CashOnDeliveryTile`) matching Figma exactly — e.g. Visa is a light-grey 40×40 outer tile holding a dark inner mini-card with white italic "VISA" text (reads as a credit card); Apple Pay is a BLACK 40×40 tile with just the white Apple silhouette (no "Pay" wordmark). Don't reach for the wordmark PNGs in `/public/cart/` for the row tiles — those are for the small "we accept" payment-logo row, not the radio cards.
    OrderSuccessClient.jsx ← "use client" — owns the /checkout/success page. Mobile (Figma 85:8692) stacks: 128px dark gradient-blur disc with white check → "Order Successful!" 24px bold + 16px condensed subtitle → coaching CTA card (dark teal gradient + 106×120 coach photo + APPLY FOR COACHING white pill) → CONTINUE SHOPPING dark CTA → order-info card (shadowed, white, #ORD-2847391 + 48px dark bag tile + Estimated Delivery / Total Amount meta rows) → items list (84×126 thumbs, Zalando Condensed variant labels) → shipping-address card. Desktop (Figma 119:6418) centers a flat 96px #11191f disc with red-tinted check + heading at top, then two columns — left (coaching card → items list with #f9fafb pill rows, 96×128 thumbs → bordered shipping-address card with circular #11191f icon) and right (white shadowed Order Info Card with stacked CONTINUE SHOPPING / TRACK ORDER, then help box "Have a question about your order?" → `Contact Support Team`). Order metadata (`ORDER_NUMBER` / `ORDER_TOTAL` / `ESTIMATED_DELIVERY` / `PAYMENT_METHOD`) is hardcoded against the seeded cart — swap to data returned from `myAppCheckout` when the mutation lands (resolver already exists in `orders.ts`). All SVGs (check, bag, home) inlined per the 7-day asset-URL expiry rule; the coaching photo lives at `/public/cart/coaching-model.png`.
    OrderFailedClient.jsx ← "use client" — owns the /checkout/failed page. Mobile (Figma 111:3587) matches the design exactly: 128px dark gradient-blur disc with white X → "Payment Declined" 24px bold + 16px condensed subtitle → Payment Error card (white, shadowed, with a 40×40 #ff6b6b tile holding a white bang icon, then 3 condensed meta rows — Transaction ID, Attempted Amount in #ff6b6b, Card Ending) → items list → 3-CTA stack (`TRY ANOTHER METHOD` filled #11191f, `RETRY PAYMENT` 2px dark outlined, `RETURN TO CART` text-only). Desktop (Figma 119:6627) flat 96px #1a1a1a disc with red `#ef4444` pulse ring + 36px ExtraBold heading. Two columns — left (Payment Error card with `#fee2e2` outer border and a red-tinted top section using a `rgba(239,68,68,0.1)` icon tile + white bottom section with the 3 meta rows; ●●●● 4532 split-render matches the Figma node 119:6666; then items list — **no shipping address on desktop**) and right (Action Card: "ACTION REQUIRED" red eyebrow + "Payment Failed" 24px heading + #fef2f2 card-icon tile + Attempted Method row with inline `VisaChip` + 24px Total Amount + 3 CTAs — `Try Another Method` filled #111827, `Retry Payment` outlined #d1d5db NOT 2px dark, `Return to Cart` underlined text #666; then help box "Think this is a mistake?" with pipe-separated `Contact Bank Support` | `Chat with Us` links). All routes: `TRY ANOTHER METHOD` and `RETRY PAYMENT` → `/checkout/payment`; `RETURN TO CART` → `/cart`. **`VisaChip` is an inline 26×18 #1a1f71 chip with italic white "VISA"** — same pattern as the row tiles in PaymentPageClient, just smaller. Transaction id / attempted amount / card last4 are seeded constants; replace with the rejection payload from `myAppCheckout` (or whatever payment-processor adapter lands).
  shared/
    Sidebar.jsx           ← "use client" — mobile slide-in menu w/ drilldown for items that have children. Consumed by Header AND ShopHeader (mobile sort/menu button)
  auth/                   ← shared auth UI (9 files)
    HeroPanel.jsx         ← desktop-only 60%-wide image panel w/ logo + quote, hidden on mobile
    AuthHeader.jsx        ← logo + "Don't have an account? Sign Up" style header, mobile/desktop variants
    AuthInput.jsx, PasswordInput.jsx, PhoneInput.jsx
    AuthButton.jsx        ← primary white-pill submit
    GoogleButton.jsx, Divider.jsx, TermsFooter.jsx
  admin/                  ← admin console UI — all client-component-heavy because every page is interactive (forms, drawers, filters). Mock data only; no backend calls yet.
    layout/
      AdminShell.jsx      ← "use client" — sticky SideNav (lg+) + mobile off-canvas drawer + TopBar. Locks body scroll while the off-canvas is open. Single layout consumed by every admin page via (admin)/layout.js.
      SideNav.jsx         ← "use client" — dark #11191f sidebar with `SECTIONS` array (Overview / Catalog / Operations / Insight). Active route highlighted via `usePathname` exact-or-prefix match. **Add new admin pages by extending SECTIONS here** — don't recreate the navigation per page. Imports `ORDERS`, `CONTACT_MESSAGES`, and `STOCK_ALERTS` from `mockAdmin.js` to compute live badge counts: `processingCount` on Orders, `unreadCount` on Contact Messages, `pendingAlertCount` on Stock Alerts — each renders a `#1d4ed8` pill badge when > 0. Swap to props from AdminShell when wiring to real data.
      TopBar.jsx          ← "use client" — sticky white bar with mobile menu button, search input (presentational), notification bell, profile dropdown. The bell button toggles `NotificationPanel` open/close; a `useRef` + `mousedown` listener closes the panel when clicking outside it. The bell badge shows the live total count (processing orders + unread messages + pending stock alerts + all low-stock variants); hidden when zero. Profile dropdown uses the same outside-click pattern.
      NotificationPanel.jsx ← "use client" — dropdown panel rendered below the bell button. Receives pre-filtered slices as props (`orders`, `messages`, `stockAlerts`, `lowStock`) — TopBar computes these at module level. Shows 4 grouped sections: **New Orders** (blue, links to `/r3pr-console/orders`), **Unread Messages** (indigo, links to `/r3pr-console/messages`), **Stock Alert Requests** (amber, links to `/r3pr-console/stock-alerts`), **Low Stock** (red, links to `/r3pr-console/products`). Each section has a coloured icon header with count chip + "View all" link, up to 3 item rows (primary text, truncated secondary, timestamp), and a "+N more" link when extras exist. All links call `onClose` so the panel dismisses on navigation. Empty state shows "All caught up" when total count is 0.
      PageHeader.jsx      ← shared `{ eyebrow, title, description, actions }` page-title block — every admin page renders one at the top before its manager component.
    shared/               ← admin design primitives (NOT to be promoted to /components/shared/ — they're admin-specific colors + radii)
      Button.jsx          ← 6 variants (primary / accent / secondary / ghost / danger / dangerSolid) × 3 sizes. Plus `<IconButton>`. Uppercase Zalando Expanded, 2px radius. Don't reach for the shop's GlassButton / AuthButton inside admin — they're tonally wrong.
      Drawer.jsx          ← right-side drawer, reuses the `.right-drawer` keyframes from globals.css (the shop's AddToCartDrawer animation). Owns its own `useDelayedUnmount` + body-scroll lock + Escape handler.
      Modal.jsx           ← centered confirmation dialog, reuses the `.filter-card-desktop` keyframes for the open/close animation.
      DataTable.jsx       ← "use client" — generic `{ columns: [{ key, label, align, width, render(row) }], rows, onRowClick, empty }`. Required to be a client component because column `render` callbacks are functions — see "Server→client function-prop boundary" in Conventions.
      StatusBadge.jsx     ← rounded-full pill, color comes from `STATUS_TONE` in mockAdmin.js keyed by `status` (order status / payment / role / active|inactive). Centralised tone map so a new status only needs one new entry.
      KpiCard.jsx         ← dashboard KPI tile. **Value font is `clamp(20px, 4.2vw, 28px)`** so long currency strings ("JOD 184,320.00") scale down on phones without media queries. Delta + value wrap on a flex row at narrow widths.
      Charts.jsx          ← **"use client"** — Recharts wrapper. Exports `LineChart`, `BarChart`, `Donut`. All admin pages import charts exclusively from here; never import from `recharts` directly so a future library swap stays in one file. `LineChart({ data, series, height, color, yFormatter })` — `data` is `[{ day|label, value }]` or a multi-key object when `series` is provided; `series?: [{ key, color, name }]` drives multi-line comparison (omit for single-series); dots suppressed when `data.length > 20`; X-axis labels auto-thin to ~10 visible ticks for 30/60/90-day ranges. `BarChart({ data, height, color, horizontal, yFormatter })` — vertical by default; pass `horizontal` for category comparisons or when label count > 7; per-bar `color` field on each datum overrides `color`. `Donut({ data, size, label })` — Recharts `PieChart` with center label overlaid via CSS absolute positioning (not SVG `<text>`) so the font tokens apply. All three expose a `yFormatter` prop for Y-axis/tooltip value formatting. `TooltipBox` renders on hover for all chart types. **Server components can still import and render these** — they're client components, and Next.js serialises their plain-data props at the server→client boundary (no function props allowed).
      Form.jsx             ← Field / Label / TextInput / NumberInput / TextArea / Select / Toggle / Chip / SearchInput / DateInput. Standard admin form atoms. Tailwind v4-safe (no arbitrary `aspect-[]` patterns).
      Icons.jsx            ← 30 single-stroke SVG icons (Dashboard / Box / Folder / Cart / Chart / Tag / Users / Percent / Mail / MessageSquare / Search / Bell / Plus / Close / Chevron* / Edit / Trash / Eye / EyeOff / Grip / Download / Filter / Check / ArrowUp/Down / Logout / Alert / Calendar / Send / Copy / Settings). Stroke uses `currentColor` so they inherit parent color.
    dashboard/RecentOrdersTable.jsx ← "use client" wrapper around DataTable so the dashboard server page can pass plain `rows={…}` without crossing the function-prop boundary.
    categories/CategoryManager.jsx  ← "use client" — full category tree. Row click toggles the major's accordion (chevron is a visual indicator only); inner action buttons + grip stopPropagation so they don't toggle. Reorder is **Pointer Events drag-and-drop** (mouse / touch / pen) on the grip handle (`touch-none` to suppress scroll), with up/down arrows kept as keyboard fallback. Drop targets are hit-tested via `data-drop-kind`/`data-row-id` + `getBoundingClientRect`; majors only drop on majors, subs only drop within their parent major (scoped by `data-major-id`). A 2px `#1d4ed8` line indicates the insertion point and the dragged row dims to 50%. Visibility toggle, edit, delete are per-row. `CategoryDrawer` handles BOTH major and sub creation/edit via an `editing = { kind: "major" | "sub", row }` discriminator. **No `slug` field** — major_categories / sub_categories on the server only have `id`/`name`/`is_visible`/`sort_order`, so the drawer only writes `name`/`visible`/`majorId` (mockAdmin.js still carries a `slug` field but it's ignored). **All `editing.X` and `drag.X` access uses optional chaining** — even inside `{open ? ... : null}` guards or after `drag?.kind === "..."` narrowing — because React Compiler can pre-evaluate both ternary branches during memoization analysis.
    products/ProductManager.jsx     ← "use client" — three panels + table + drawer. **ColorPanel** manages the global color palette (`COLORS` in mockAdmin.js — `{id, name, hex}`); colors can be added, edited (name + hex swatch), and deleted (with a confirmation guard if variants reference them). **SizePanel** manages the global size list (`SIZES` — `{id, label}`); same add/edit/delete pattern. Both panels feed the per-product selectors in the drawer. The **product table** has search, category, and visibility filters. Clicking a row or "New Product" opens **ProductDrawer** — a right-side Drawer with 4 tabs:
      - **Details** — Name (required), Subtitle (required in text mode; in Composition mode the subtitle is auto-built from `{pct, material}` rows that must each have a name and must sum to 100%), Sub-category, Price (required, >0), Description, Material percentages (recycledPolyester + premiumElastane + performanceGuaranteed must sum to 100%), Product details (minimum 3 entries, each needs a title — header shows a live `N/3` counter), Visible toggle. A **Storefront preview** panel mirrors the exact mobile and desktop `ProductCard` label-row rendering live.
      - **Media** — **Available Colors** chip-row (subset of global palette → `draft.productColors`; shared state with Inventory tab). **Available Sizes** chip-row (`draft.productSizes`; shared with Inventory tab). Below the divider, one UploadThing-style dashed-border dropzone per selected color; "Choose photos" increments `draft.colorImages[colorId]` (mock counter — real upload deferred) and shows "N photos" beside the color name.
      - **Inventory** — Color chip-row and Size chip-row (same `draft.productColors`/`draft.productSizes` state — changes reflect in Media tab and vice versa). Below the divider, a color × size qty grid **scoped to selected combinations only** (not the full global palette). Qty inputs show empty string instead of `0` (prevents "02" prepend issue), `min="1"`. Setting a cell to 0/empty prunes that variant from `draft.variants`; `totalStock` updates live.
      - **Labels** — multi-select label chips + low-stock alert threshold.
      **Draft state shape**: `{ name, subtitle, subtitleMode ("text"|"composition"), composition [{pct,material}], subId, price, description, materials {recycledPolyester,premiumElastane,performanceGuaranteed}, details [{title,desc}], productColors [colorId], productSizes [sizeId], colorImages {[colorId]:number}, variants [{color,size,qty}], totalStock, visible, labels, lowStockThreshold }`. When editing an existing product whose mock data has `variants` but no `productColors`/`productSizes`, the `useEffect` derives them from the variant list (via `??` coalescing) so the grid renders correctly. `toggleProductColor(id)` / `toggleProductSize(id)` add/remove from draft and prune orphaned variants atomically.
      **Validation** (`validate()` → `handleSave()`): name required; subtitle required (text mode); price > 0; composition rows complete + sum 100%; material % sum 100%; details ≥ 3 with titles; ≥ 1 color selected; ≥ 1 size selected; `colorImages[id] > 0` for every selected color; every color × size cell ≥ 1. All errors collected at once; inline error text appears per field; tab buttons show a red dot while their tab has errors; Save auto-jumps to the first failing tab (Details → Media → Inventory priority). Individual field errors clear as the user fixes them via `clearError(key)`.
    orders/OrderManager.jsx          ← "use client" — collapsible **Settings accordion** at the top (delivery fee: JOD-prefixed number input + Save button with 2 s "Saved" confirmation + validation). Below that: status filter chips with live counts, search/export bar, table, and detail drawer. **Pipeline is 4 steps: Processing → Prepared → With Delivery → Delivered** (no Pending — orders enter at Processing). `PIPELINE` + `PIPELINE_LABEL` map live at the top of the file. Transition rules: (1) **sequential only** — the detail drawer shows a single "Mark as {next step}" button, never a skip-ahead list; (2) **confirmation modal** on the `handed_to_delivery → delivered` transition ("Are you sure?", Cancel / Yes, mark as delivered). Cancelled/returned are terminal — transition button is hidden. `OrderDetail` owns its own `confirmNext` state and imports `Modal` from admin shared.
    reports/ReportsView.jsx         ← "use client" — **date presets** (7D / 30D / 90D / YTD) above the date-range inputs; selecting a preset highlights it blue and fills both inputs. **Underline tab bar** (not chip pills) for the 7 tabs: Sales / Revenue / Promo usage / Inventory / Customers / Delivery / Returns. `MetricStat` shows a coloured delta arrow (green ▲ positive / red ▼ negative) + "vs prior period" hint. Charts use the Recharts-backed `Charts.jsx` primitives. Notable tab details: Sales and Customers tabs have a **Compare toggle** that adds a grey prior-period line to the LineChart (controlled by local `compare` state; data comes from the `current`/`prev` keys on `SALES_SERIES_30` / `SIGNUPS_SERIES_30`). Revenue and Inventory use horizontal `BarChart` for category comparisons. Delivery has both a daily bar chart and an outcome Donut. Returns has a horizontal reasons bar + Donut. The promo tab uses an inline `UsageBar` component (fill turns red when ≥ 90% of limit used). Export PDF / CSV buttons remain presentational.
    promo/PromoManager.jsx          ← "use client" — table with usage-progress bars + copy-to-clipboard, drawer with code generator + type/amount/min-order/usage-limit/expiry/active.
    users/UserManager.jsx           ← "use client" — role chips (Admin / Delivery / Accounting / Customer) with counts, table with inline active toggle, drawer with welcome-email notice for new accounts.
    discounts/DiscountManager.jsx   ← "use client" — scope chips (product / sub-category / major-category), drawer that swaps its target-picker `options` based on the selected scope, validity window + active toggle.
    broadcast/BroadcastComposer.jsx ← "use client" — side-by-side composer + live email preview, audience selector with counts, merge-tag chips, confirm-modal before send, sent-history table.
    contacts/ContactManager.jsx     ← "use client" — status filter chips (All / Unread / Read / Replied / Archived with live counts), search bar, table with sender avatar + name + email + phone + message preview + date + status badge + view/delete actions. Opening an unread row auto-marks it as read. Detail drawer shows full message body + "Mark as Replied" / "Mark as Read" quick actions + Archive/Unarchive + Delete footer buttons. Delete confirmation modal. Wire to a future `myAppListContactMessages` / `myAppUpdateContactMessageStatus` / `myAppDeleteContactMessage` resolver set when the backend adds a `contact_messages` table.
    stock-alerts/StockAlertManager.jsx ← "use client" — status filter chips (All / Pending / Notified / Dismissed with live counts), search by customer name, email, or product name, table with customer avatar + name/email + product/variant + date + status badge + delete action. Row click opens detail drawer showing customer block, variant block (product · color · size), and contextual quick-action buttons: "Mark as Notified" + "Dismiss" when pending; "Dismiss" only when notified. Delete confirmation modal. Wire to a future `myAppListStockAlerts` / `myAppUpdateStockAlertStatus` / `myAppDeleteStockAlert` resolver set when the backend adds a `stock_alerts` table.
    settings/SettingsManager.jsx ← "use client" — storefront-wide shipping/delivery rules. Two stacked `SettingsCard` blocks: **Free Delivery** (`Toggle` first to enable/disable the feature, then JOD-prefixed threshold input that greys out + disables when toggle is off; Save with 2s "Saved" pill + validation) and **Express Shipping** (`Toggle` first to show/hide the option from customers, then JOD-prefixed extra-fee input — added to standard delivery when the customer chooses Express; same Save/validation pattern). Shared inline `JodInput` + `SavedPill` helpers; validation skipped when the feature toggle is off (no point validating a disabled input). Mock-only — wire to a future `myAppGetShippingSettings` / `myAppUpdateShippingSettings` resolver pair (no `shipping_settings` table or resolvers exist yet on the server).
  accountant/             ← accountant ledger UI — forks the admin shell pattern with a finance-only nav. Reuses admin's `shared/` primitives (Button, DataTable, Charts, Form, Icons, PageHeader) — do NOT duplicate them under accountant/shared/. Mock data only.
    layout/
      AccountantShell.jsx ← "use client" — sticky SideNav (lg+) + mobile off-canvas drawer + TopBar. Body-scroll lock pattern copied from `AdminShell`. Single layout consumed by every accountant page via (accountant)/layout.js.
      SideNav.jsx         ← "use client" — dark `#11191f` sidebar with **Finance** section (Overview) + **Coming soon** section (Export history, Audit log) rendered as `aria-disabled` rows with a "Soon" pill. Footer carries a persistent **Read-only access** info block above Sign Out. Active route uses `usePathname` exact-or-prefix match, same as the admin SideNav. **Add new accountant pages by extending SECTIONS here** — don't recreate the navigation per page.
      TopBar.jsx          ← "use client" — slim sticky white bar with mobile menu button, breadcrumb cluster ("Finance Ledger / Read-only · Accountant"), and profile dropdown. **No notification bell + NotificationPanel** — the accountant role has no operational queue to action, so the bell was replaced with a permanent `Read-only` chip (blue `#eff6ff` / `#1d4ed8`). When wiring real data, surface signed-in user info via props rather than reintroducing the bell.
    overview/
      FinanceOverview.jsx ← "use client" — full finance dashboard. **Date-range filter bar** (presets 7D/30D/90D/YTD + DateInput From/To + Export PDF/CSV buttons — exports are presentational, same as admin Reports). **5 KpiCards** in a `grid-cols-1 sm:grid-cols-2 lg:grid-cols-5` row, each with a coloured 3px top stripe: Total revenue (blue) / Total orders (sky) / Discount impact (purple) / Avg. order value (amber) / Net revenue (green). All deltas computed via `pctDelta` against the prior-period column. **Revenue-over-time** LineChart with a Compare toggle that adds a grey prior-period series. **Breakdown tab bar** — Category / Product / Time period: Category renders a horizontal BarChart + Donut + a share-list aside; Product renders a ranked DataTable with rank chip, units, revenue, and an inline share-bar; Time period renders a Day/Week/Month granularity toggle that calls `rollupBy` then renders a BarChart + DataTable with orders/revenue/discount/net columns. Finally a **Discount impact by code** DataTable below the tabs covers the requirement-2 "discount impact" line item with per-code redemptions + JOD impact + active/expired badge. `_idx` is injected via `rows.map((r, i) => ({ ...r, _idx: i }))` so the rank column can read it inside `render(row)` (DataTable's render signature is row-only, no index — see Conventions). No hand-written `useMemo` per the React Compiler convention; filterDaily / aggregate / rollupBy run inline.

public/
  splash/   ← splash-{1..4}.png + logo-re.png (splash overlay)
  home/     ← hero hoodie, colorway shots, social/cta icons, logo-re.png
  auth/     ← desktop + mobile hero art for the auth split layout, blackLogo.png
  shop/     ← model-1..5.png (mock product photos pulled from Figma), icon-add.svg, icon-bag.svg, icon-filter-remove.svg, icon-search.svg, icon-sort.svg, icon-timer.svg (used by ComingSoonOverlay; vuesax/linear/timer, 104x104 viewBox, downloaded from Figma)
  cart/     ← cart + checkout chrome icons (visa.svg + amex.svg clean from Figma; mastercard / discover / applepay / gpay as PNGs after the `Cnan nan` SVG export bug — see "Figma SVG export NaN bug" below), plus checkout-result imagery: coaching-model.png (Asaad Hamawi photo for the /checkout/success coaching CTA, downloaded from Figma node 128:6031 via get_screenshot)
```

Of the **four role route groups originally planned** (`(customer)`, `(admin)`, `(deliveryCompany)`, `(accountant)`), `(customer)`, `(admin)`, and `(accountant)` now exist. `(deliveryCompany)` is still pending. The public storefront landing stays at the app root (`src/app/page.js`) — it's intentionally outside `(customer)` because it's pre-auth marketing. The role-group plan still applies for everything past sign-in:

| Requirement area | Route group |
| --- | --- |
| Storefront browse, product detail, cart, wishlist, checkout, order history, customer account hub (profile + payment methods + addresses) | `(customer)` |
| Admin dashboard, category/product/variant/image CRUD, discounts/promo codes, order management, reports, broadcast email, user management | `(admin)` |
| Delivery dashboard: list assigned orders, update status to Out for Delivery / Delivered / Failed | `(deliveryCompany)` |
| Read-only financial overview, revenue breakdown, export | `(accountant)` |

There is **no top-level `src/app/components/` folder** — shared UI lives under `src/components/<domain>/` (currently `public/homePage/`, `customer/shop/`, `auth/`, and `shared/`). Add a new domain folder when a new surface starts; promote a component up to `src/components/shared/` only when a second domain needs it (`Sidebar.jsx` lives there because both `Header` on the landing and `ShopHeader` in `customer/shop/` consume it for the mobile menu).

## Canonical route map

These are the URLs the app commits to. Nav links, redirects, and any hand-built `<Link>`s should match this shape exactly — don't invent variants.

| Surface | Route |
| --- | --- |
| Landing / home | `/` |
| Shop — category picker (no `?category=`) | `/shop` |
| Major category | `/shop?category=<slug>` |
| Sub-category filter (stacks on top of major) | `/shop?category=<slug>&sub=<slug>` |
| Product detail | `/products/[slug]` |
| Cart | `/cart` |
| Checkout — Details (step 2) | `/checkout` |
| Checkout — Payment (step 3) | `/checkout/payment` |
| Checkout — Order confirmed | `/checkout/success` |
| Checkout — Payment declined | `/checkout/failed` |
| Account hub (profile + payment methods + addresses) | `/account` |
| Order history | `/account/orders` |
| Wishlist | `/account/wishlist` |
| Contact us | `/contact` |
| Admin console index (redirects to dashboard) | `/r3pr-console` |
| Admin dashboard | `/r3pr-console/dashboard` |
| Admin — categories / products / orders / stock-alerts / messages / reports / promo-codes / users / discounts / broadcast / settings | `/r3pr-console/<section>` |
| Accountant ledger index (redirects to overview) | `/r3pr-ledger` |
| Accountant — finance overview (KPIs + revenue chart + category/product/time breakdowns + discount-by-code table) | `/r3pr-ledger/overview` |

Notes:
- **`/shop` never shows an all-products listing.** When `?category=` is absent, render `CategoryPicker` so the user always commits to a major category before seeing products. This is a deliberate product decision — don't add an "all products" page back.
- Browse and filter state lives in **query params on `/shop`**, not nested path segments. Adding facets later (color, size, price, sort) follows the same pattern — append query params, don't add new path segments.
- Current filter query params consumed by `shop/page.js` → `parseFilters`: `category`, `sub`, `sizes`, `colors`, `materials`, `types` (all CSV), `min`, `max`, plus debug flags `empty=1` (force empty state) and `comingSoon=1` (force the coming-soon overlay over the grid). The filter group was originally `fits`; it was renamed to `materials` to match the Figma mobile filter sheet — don't reintroduce `fits` without updating both `FILTER_OPTIONS` and `ACTIVE_FILTER_COUNT` in `mockShop.js`.
- **Coming-soon overlay triggers** (handled in `shop/page.js`, on top of the `?comingSoon=1` debug flag): (1) unknown major category — `getCategory(slug)` returns falsy, (2) unknown sub-category — `?sub=<slug>` doesn't match any entry in `cat.subs`, (3) **inactive sub-category** — the matching entry has `active: false` on its record. The page falls back to `listProducts({ category: "discover-all" }).slice(0, 8)` to populate the blurred grid behind the overlay when the real category returns nothing. Breadcrumb falls back to a humanised label via `slugToLabel(slug)`. Don't drop the `active` check — it's the contract the admin surface will write against when sub-categories get a moderation toggle.
- **Shop page top padding** is `md:pt-24` (96px) — matches Figma 119:3858 desktop spacing below the sticky header. Don't reduce; the breadcrumb and grid both rely on it for vertical breathing room.
- `/account` is the profile + payment-methods + addresses hub (single page, not a redirect). Sibling routes `/account/orders` and `/account/wishlist` exist; there is **no** separate `/account/addresses` route — addresses are managed inline in the hub.
- `Header.jsx` slugifies major/sub names client-side for URLs. Once the backend exposes a stable `slug` column on `major_categories` / `sub_categories`, switch to that to survive renames.
- **The admin URL prefix `/r3pr-console` is intentional, not a placeholder.** It's a security-through-obscurity layer on top of the role gating that will eventually live behind these routes — don't rename to `/admin` for "tidiness". Add new admin pages under `src/app/(admin)/r3pr-console/<feature>/page.js` AND extend `SECTIONS` in `src/components/admin/layout/SideNav.jsx` so the nav picks them up.
- **The accountant URL prefix `/r3pr-ledger` follows the same obscurity pattern.** Add new accountant pages under `src/app/(accountant)/r3pr-ledger/<feature>/page.js` AND extend `SECTIONS` in `src/components/accountant/layout/SideNav.jsx` (the "Coming soon" items already include `/r3pr-ledger/exports` and `/r3pr-ledger/audit` as disabled placeholders — flip `disabled: false` and create the page when implementing).

## Commands

Run from `repair/`:

- `npm run dev` — Next.js dev server (default port 3000). The `Server/` dev server also defaults to port 3000, so when running both locally start the backend first and set the frontend on another port (`PORT=3001 npm run dev`), or move the backend via its `PORT` env var.
- `npm run build` — production build.
- `npm start` — serve the built app.
- `npm run lint` — ESLint flat config (`eslint-config-next/core-web-vitals`).

There is no test runner wired up yet.

## Conventions

- **JavaScript, not TypeScript.** `jsconfig.json` is the only config; do not introduce `.ts`/`.tsx` files or a `tsconfig.json` without discussing it. (Note: the sibling `client/` app is TypeScript — do not copy its patterns wholesale.)
- **Path alias `@/*` → `./src/*`** (`jsconfig.json`). Use it for cross-folder imports instead of long relative paths.
- **React Compiler is enabled** (`next.config.mjs` → `reactCompiler: true`). Don't hand-write `useMemo` / `useCallback` for things the compiler can hoist; reach for them only when profiling shows a real issue.
- **React Compiler + ternary guards — use optional chaining defensively.** The compiler's memoization analysis can pre-evaluate both branches of a ternary expression, so an "obviously guarded" access like `{state ? <X title={state.kind} /> : null}` can still throw `Cannot read properties of null (reading 'kind')` at runtime when `state` is null. **Use `state?.kind` (or hoist a guard `if (!state) return …`) even inside branches you think are protected.** This bit `CategoryManager` on initial render and required the same pattern across every admin manager component — see `src/components/admin/categories/CategoryManager.jsx` for the canonical fix (guard at the top of the save handler, `?.` on every `editing.X` access, and `{open && editing ? …}` instead of `{open ? …}` for the drawer body).
- **Server→client function-prop boundary.** Server components can't pass functions to client components — Next.js production builds throw `Functions cannot be passed directly to Client Components` and the page fails to prerender. The dashboard hits this with `DataTable`'s `columns` array (each column has a `render: (row) => …` callback). **Pattern:** when a server page needs a `DataTable`, render a tiny `"use client"` wrapper component that owns the column definitions and accepts plain `rows={…}` from the server — see `src/components/admin/dashboard/RecentOrdersTable.jsx`. Manager pages (Products, Orders, etc.) don't need this because they're already client components.
- **Tailwind v4** via `@tailwindcss/postcss`. There is no `tailwind.config.js` — theme tokens live in `globals.css` under `@theme inline { ... }`. Add new design tokens there, not in a JS config file. There is **no animation library** — splash uses `@keyframes` in `globals.css` plus inline `animation:` styles, and that's the established pattern. Don't add framer-motion / gsap / etc. without discussing.
- **Tailwind v4 + Turbopack gotcha:** arbitrary `aspect-[w/h]` classes (e.g. `aspect-[176/264]`) silently don't apply under the current Turbopack content scanner — the parent collapses to zero height and `next/image` with `fill` renders nothing. Use inline `style={{ aspectRatio: "176 / 264" }}` for image card containers. `bg-[#hex]` / `text-[#hex]` arbitrary values work fine for non-toggled classes, but conditional arbitrary classes inside template literals can also miss the scanner — when a sale price chip or strikethrough needs to toggle, prefer inline `style` (`backgroundColor`, `textDecoration`) over a conditional `bg-[#1d4ed8]` / `line-through` class. See `customer/shop/ProductCard.jsx` for the pattern. **The same scanner gotcha extends to positioning/sizing/outline arbitraries** — at least these forms have all been observed being silently dropped in `customer/cart/CartPageClient.jsx`: `top-[Npx]`, `left-[Npx]`, `right-[Npx]`, `w-[Npx]`, `h-[Npx]`, `outline-[#hex]`, `-outline-offset-1`, and even non-arbitrary classes like `w-64` in some contexts. Symptoms: an absolutely-positioned line collapses to width 0, an outlined card shows the default text color as its outline, a stepper connector lines up at `top: 0` instead of the intended Y. **Pattern:** for any absolutely-positioned divider, decorative element, or outlined surface, default to inline `style` for `left/right/top/width/height/outlineColor/outlineOffset` from the start — don't trust arbitrary positioning/outline classes here. See the multiple `style={{ left, top, width, height }}` and `style={{ outlineColor: "#…", outlineOffset: "-1px" }}` usages in `CartPageClient.jsx` for the canonical fix.
- **Responsive widths between `md` and Figma design width — flex, don't pin `lg:` widths.** Figma desktop frames are ~1376–1440 wide; pinning a child to `lg:w-[508px]` / `lg:w-[545.33px]` / etc. only works once the viewport reaches the Figma width plus padding, so every viewport between `lg` (1024) and ~1500 overflows. The pattern that works is `flex-1 min-w-0` on the flex children plus `lg:max-w-[<figma-width>]` as a ceiling — the layout fluidly fills whatever space the section's `max-w-[1376px]` and section-level padding leave, and only locks to the Figma proportions at the widest viewports. This caused the original `/products/[slug]` hero to overflow by 269px at `lg` and 109px at `xl`; the fix is in `customer/product/ProductPageClient.jsx`. Apply the same pattern when porting any Figma desktop hero into code.
- **Responsive typography ramps `md → lg → xl`, don't jump straight to Figma sizes at `md`.** When a section has very different mobile vs desktop type scales (e.g. 24px → 48px heading, 36px → 72px stat numbers), use an intermediate `md:` size, an `lg:` size, and the Figma value only at `xl:`. Otherwise the section reads as "shrunken Figma" between 768 and ~1280. The "Crafted to Last" section in `customer/product/ProductPageClient.jsx` is the worked example — see the heading + stat-value class lists for the ramp shape.
- **App Router only.** No `pages/` directory; don't add one.
- **`viewport` export in `src/app/layout.js` is load-bearing.** Without it, the page is missing `<meta name="viewport" content="width=device-width, initial-scale=1">` and mobile browsers (plus any headless screenshot tool) lay out at the legacy 980px default — every storefront page then renders content wider than the visual viewport and clips on the right. The fix is the App Router idiomatic `export const viewport = { width: "device-width", initialScale: 1 }` already in the root layout; don't remove it.
- **Figma SVG export NaN bug.** When the Figma MCP `get_design_context` API returns SVG assets, complex paths occasionally serialize a degenerate cubic curve as the literal text `Cnan nan` inside the `d` attribute. That's invalid SVG path syntax, so the renderer stops at the first invalid command — the icon ends up as a fragment of the original mark (Mastercard rendered as just two dots, Apple Pay cut off mid-"Pay", etc.). **Pattern:** before trusting any SVG downloaded from Figma's MCP, `grep -c nan` it. If it has `nan`, fall back to the PNG via the `get_screenshot` MCP API on the same node id — the screenshot pipeline rasterizes correctly because it bypasses path serialization. The 6 payment icons in `repair/public/cart/` are the worked example: `visa.svg` + `amex.svg` are clean Figma SVGs, the other 4 (mastercard / discover / applepay / gpay) are PNGs from `get_screenshot`. See `PAYMENT_BRANDS` in `CartPageClient.jsx` for the per-icon `{ name, ext, w }` map.
- **Server-side reads go through `src/lib/repairApi.js`.** It exports `repairQuery(operationName, variables, { revalidate })` — a thin POST wrapper around `/repair/graphql` that hides the `MyAppInput → MyAppResponse` envelope and JSON-decodes `serverResponse`. Currently used by `HeaderShell.jsx` for `myAppListCategoriesTree`. Endpoint defaults to `http://localhost:3000/repair/graphql`; override via `REPAIR_API_URL` env. Auth-bearing calls aren't supported yet — when you add them, extend this helper rather than duplicating fetch logic in components.
- **No state library, no client GraphQL client, no auth context yet.** Forms in `(auth)/` render without `onSubmit` handlers — still pure presentation. `ShopPageClient.jsx` keeps its drawer / cart-banner state in plain `useState`, and lifts filter changes into URL query params via `useRouter`/`useSearchParams` from `next/navigation` (no Zustand / Redux / SWR — keep it that way until a real shared cart spans pages). The next concrete data-layer decision is the **client-side** story (mutations, auth state, refresh-rotation). Pick deliberately and document the choice here when it lands. The backend issues JWT access tokens + opaque refresh tokens on every sign-in path (`myAppLogin`, `myAppGoogleAuth`, `myAppSignUp`, `myAppRefreshToken`) and rotates refresh tokens on use — whatever client storage / refresh strategy you pick must respect that rotation, or the next refresh call will be rejected as a reused token.
- **`"use client"` only when actually needed.** Server-component-first. Current client components: `SplashSection.jsx` (drives the fade and toggles `body.style.overflow`), `Header.jsx` (menu state + `usePathname` + hover dropdown), and `shared/Sidebar.jsx` (drilldown state + `Escape` key handling). Everything else — auth pages, footer, colorway sections — is a server component. Keep it that way: when a form needs `onSubmit` or state, isolate just the interactive piece into a client component rather than flipping the whole page. For data-fetching surfaces, prefer the `HeaderShell` pattern: a thin async server component that calls `repairApi` and forwards plain props to a client child.

## Design system in play

Figma is the source of truth for the visual design (file `pLwxbw3UUexnNKG7lhFotU`, landing root node `2026:3962`, shop desktop set `2065:4344` (frames `119:3858` default / `126:3792` filter / `126:4397` empty / `198:5080` coming-soon / `120:6999` add-to-cart drawer / `120:7466` added-to-cart banner / `120:5217` search overlay), shop mobile set `15:810` (frames `2:12` default / `2:561` newly dropped / `2:709` category / `2:857` scroll / `5:217` filter sheet / `7:244` sale / `7:1358` add-to-cart sheet / `39:1525` search overlay) — see auto-memory `reference_repair_landing_figma.md` for per-section node IDs). Translate from Figma, don't invent.

Tokens already wired:
- `font-display` → Zalando Sans Expanded (uppercase display type — h1s, CTAs, nav)
- `font-body` → Zalando Sans (with inline `style={{ fontStretch: "75%" }}` to fake the "Zalando Sans Condensed" weight Figma uses for product names / chips on mobile — Next/font/google doesn't ship a separate Condensed family)
- `font-sans` / `font-mono` → Geist (Next default, mostly unused on these surfaces)

The four font CSS vars (`--font-geist-sans`, `--font-geist-mono`, `--font-zalando-sans`, `--font-zalando-expanded`) are wired in `src/app/layout.js` and exposed via `@theme inline` in `globals.css`. Use `font-display` / `font-body` class utilities; don't reach for the Geist vars on visual surfaces.

Color + surface conventions established by the existing pages:
- **Landing** is a black surface (`bg-black text-white`) with a deliberately white footer for visual relief.
- **Shop** (`(customer)/shop`) is a white surface (`bg-white text-[#11191f]`) with a sticky white header (`ShopHeader`) and a dark `#11191f` footer (`ShopFooter`). **The footer is desktop-only** — wrapped in `hidden md:block` at the `(customer)/layout.js` level because the Figma mobile shop screens (frame set 15:810) have no footer; do not re-render `ShopFooter` per-page or remove the `hidden md:block` wrapper without checking the mobile design. The shop has its OWN header / footer pair — do not reuse the landing's `Header.jsx` (translucent black) or `Footer.jsx` (white) inside customer surfaces.
- **Auth** is a white surface (`bg-white text-[#11191f]`) split 60/40 desktop with a hero photo on the left (`HeroPanel`, hidden under `md`) and form column on the right.
- **Admin** (`/r3pr-console/*`) is a `#f7f7f8` content panel with a dark `#11191f` sticky sidebar (`SideNav`, `lg:flex` — collapses to an off-canvas drawer below `lg`) and a white sticky `TopBar`. Cards are white with `#e5e7eb` borders, `[2px]–[4px]` radii (NOT the shop's larger radii). Accent color is `#1d4ed8` (matches the storefront sale chip), with semantic colors for status badges: success `#16a34a`, warning `#f59e0b`, danger `#dc2626`, info-purple `#a855f7`. Add new status tones to `STATUS_TONE` in `mockAdmin.js` — don't inline new hex values across components.
- **Hero CTA** on the landing → dark glass (`GlassButton`).
- **Action CTA** in auth forms → solid white pill (`AuthButton`).
- **Sale price treatment** (used on product cards in the Sale category): original `JOD 30` rendered with `textDecoration: "line-through"` + `color: "rgba(17,25,31,0.5)"`; discounted price below it as a solid `backgroundColor: "#1d4ed8"` chip with white bold text. Applied via inline `style` rather than Tailwind arbitrary classes — see the Tailwind v4 gotcha in Conventions.
- Body brand text color is `#11191f` (not pure black) — use this when matching.

When building a new landing section, follow the existing pattern: presentational server component under `src/components/public/homePage/`, sized via Tailwind, imported into `src/app/page.js` in display order. New customer-storefront surfaces (cart, checkout, account/*, products/[slug]) should mount under `(customer)/` so they pick up the `ShopHeader` + `ShopFooter` layout automatically — don't re-create the header/footer per page. Do not introduce a CMS or content layer for marketing copy yet.

## Shop drawers / overlays — animation + scroll conventions

The drawers and full-screen overlays under `customer/shop/` (FilterDrawer, AddToCartDrawer, SearchOverlay, ComingSoonOverlay) share a small animation system defined in `globals.css`. **Reuse it instead of inventing new keyframes.**

**Delayed-unmount hook.** Open/close animations only work if the element stays mounted through the exit. Each drawer file inlines its own `useDelayedUnmount(open, exitMs)` (15-line hook — duplicated, not shared) that holds `render=true` and toggles `data-state="open" | "closing"` on the aside + backdrop. CSS picks the enter / exit keyframe off the data-state, and the hook unmounts only after `exitMs`. The hook is now copied into **five** files (FilterDrawer, AddToCartDrawer, OrderFilterDrawer, WishlistFilterDrawer, admin Drawer) — the original "abstract on the fourth drawer" threshold has been crossed. Promoting to `customer/shared/useDelayedUnmount.js` (or a shared `BottomSheet` / `RightDrawer` primitive that bundles the hook + Escape handler + backdrop) is now reasonable next-cleanup work; until that lands, keep copying.

**CSS classes by surface type:**

| Class | Surface | Open animation | Exit animation |
| --- | --- | --- | --- |
| `.drawer-backdrop` | Backdrop scrim (rgba(17,25,31,0.50)) shared by all drawers | 280ms linear fade in | 280ms linear fade out |
| `.bottom-card` | Mobile bottom-anchored card — FilterDrawer mobile + AddToCartDrawer mobile + OrderFilterDrawer mobile + WishlistFilterDrawer mobile | 320ms linear slide up from `translateY(calc(100% + 24px))` (pure vertical) | 320ms linear slide back down |
| `.right-drawer` | Desktop right-side floating card — AddToCartDrawer desktop + FilterDrawer desktop + OrderFilterDrawer desktop + WishlistFilterDrawer desktop + admin Drawer | 300ms linear slide from `translateX(100%)` | 300ms linear slide back right |
| `.search-overlay` | SearchOverlay full-screen | 240ms linear fade in | 240ms linear fade out |

**All timings are `linear`** (deliberate — the previous spring/eased curves were rejected in favor of consistent mechanical motion). Don't reintroduce cubic-bezier for these.

**Mobile bottom-card centering.** `.bottom-card` is anchored at `left-0 right-0 mx-auto` (NOT `left-1/2 -translate-x-1/2`) because Tailwind v4's translate utilities use the `translate` CSS property while the keyframe uses `transform: translateY(...)` — combining them produces diagonal motion. Keep the centering off of transform.

**Reduced motion.** `globals.css` has a `@media (prefers-reduced-motion: reduce)` block that disables all four classes + `.splash-anim` + `.sidebar-view-anim`. Add any new animated drawer class to that block.

**Custom thin scrollbar.** `.drawer-scroll` gives a 4px transparent-track / `rgba(17,25,31,0.20)` thumb scrollbar (35% on hover) — Chromium via `::-webkit-scrollbar*`, Firefox via `scrollbar-width: thin` + `scrollbar-color`. Apply on any drawer/aside with `overflow-y-auto` to keep the design clean. AddToCartDrawer desktop uses it; FilterDrawer desktop uses it. Don't apply to the page itself.

**SearchOverlay placeholder typography.** `globals.css` includes `.search-overlay-input::placeholder` to set Zalando Sans Expanded Regular 12px @ `rgba(17,25,31,0.5)`. The input's resting text style is Bold 12px `#11191F` so typed content reads heavier than the placeholder, matching Figma 39:1953.

**ComingSoonOverlay sticky overlay.** When the blurred background grid exceeds viewport height (mobile, 8 cards in 2 cols = 4 rows), the centered icon+text uses `sticky top-1/2 -translate-y-1/2` so it stays visually centered as the user scrolls through the blur. On desktop where the grid fits the viewport, sticky behaves like regular centered absolute positioning. Don't replace with plain `absolute inset-0 justify-center` — that would push the overlay below the fold on tall mobile grids.

## Backend contract you are coding against

Every request from this app should go to `POST /repair/graphql`. The wire shape is the same `MyAppInput → MyAppResponse` envelope documented in the root CLAUDE.md, but in `repair` there is **no generic CRUD path** — each operation is a named resolver with its own auth + role check. The full inventory of named resolvers lives under `Server/servers/repair/src/graphql/resolvers/` (auth, catalog, adminCatalog, adminPricing, addresses, cart, orders, reports, admin). If a feature you are building has no matching resolver, the resolver needs to be added on the server side first; do not attempt to use a generic `myAppFindAll` / `myAppAddData` style call — those were removed in the May 2026 audit and a regression test fails fast if they reappear.

Auth header is `Authorization: Bearer <jwt>`. JWTs are signed with `{ userId }`, valid 7 days. Pair with the opaque refresh token returned alongside; exchange via `myAppRefreshToken` before the access token expires. Logout calls `myAppLogout` (single token) or `myAppLogoutAll` (all sessions for the user).

## Alignment with the project requirements

Backend covers most of the feature surface (customer auth + Google OAuth + password reset + refresh rotation, catalog browsing with filtering, cart + wishlist, addresses, checkout with promo codes + per-variant `FOR UPDATE` locking, order status state machine, admin CRUD for categories/products/variants/images/discounts/promo codes, 8 aggregated reports, admin broadcast email, queued transactional emails for every status transition).

Frontend progress:
- **Public marketing landing** — built, dark theme. 10 sections composed in `src/app/page.js`. Splash overlay → fixed translucent header → hero → three colorway sections → "Crafted to last" → stats → browse-collection grid → white footer.
- **Header / nav** — `HeaderShell.jsx` fetches `myAppListCategoriesTree` server-side and forwards a shaped tree to `Header.jsx`. Desktop nav is built from the fetched categories with a hover dropdown for majors that have sub-categories; the mobile `Sidebar` uses the same tree plus static items (Home, Shop, Orders, Wishlist, Addresses, Cart). If the backend is unreachable the helper falls back to a dummy Women/Men/Sale tree so the nav still renders.
- **Auth flow shells** — sign-in, sign-up, email-sent, reset-password — static UI in `(auth)/`. Forms have no submit handlers yet; no API calls, no validation, no error states. Google button is also presentational.
- **Customer storefront — `/shop`** — built end-to-end as presentational UI against `src/lib/mockShop.js`. Every state from Figma is implemented: category picker (bare `/shop`), default 4-col desktop / 2-col mobile grid, sale variant with strikethrough + blue chip, **filter drawer** (bottom-anchored floating card on mobile, full-height right-side floating drawer on desktop — Figma 11:1797 + 126:3792, with stacked APPLY ALL / CLEAR ALL buttons on desktop), **add-to-cart drawer** (bottom-anchored card on mobile, floating right-side card 387px/32px-margins/radius-8 on desktop — Figma 7:1358 + 120:6999), **search overlay** (mobile full-screen + desktop fixed below the sticky header, both triggered by the ShopHeader magnifying-glass; live-filters mock catalogue with no-results "RESULTS / YOU MIGHT LIKE" state — Figma 39:1525 + 39:1953 mobile, 120:5217 desktop), "item added" banner, empty filter state (Figma 126:4397), and "more products coming soon" overlay (Figma 198:5080 — triggered by unknown major / unknown sub / `active: false` sub, plus the `?comingSoon=1` debug flag; hides FilterBar so the blur covers the full content area). **Not yet wired to the backend** — when the storefront resolvers land, swap `listProducts` / `getCategory` in `mockShop.js` for `repairQuery("myAppListProducts", { category, sub, ... })` calls in the server component (`(customer)/shop/page.js`). The mobile add-to-cart sheet deliberately drops the product image to match Figma 7:1358; the desktop floating card keeps it.
- **Product detail — `/products/[slug]`** — built end-to-end as presentational UI against an inline `MOCK_PRODUCT` in `(customer)/products/[slug]/page.js`. Mobile (below `md`): swipeable image carousel + product info + qty/CTA row. Desktop (md+): hero with vertical thumb strip (lg+) + main image + info column, then 8 stacked marketing sections (Crafted to Last → … → Complete Your Set). Fully responsive between mobile and the Figma 1376 design width — widths flex with `lg:max-w-[…]` caps rather than pinning to Figma px values, and typography ramps `md → lg → xl` rather than jumping at `md` (see the two new Conventions bullets). **Not yet wired to the backend** — swap the inline `MOCK_PRODUCT` for `repairQuery("myAppGetProductDetail", { slug })` once that resolver lands on `catalog.ts`. The `itemsLeft` / `lowStockLimit` shape maps to `variant.quantity` / `variant.low_stock_threshold`.
- **Order history — `/account/orders`** — built end-to-end as presentational UI against `src/lib/mockOrders.js`. Server component reads `?status=<csv>&date=<slug>` from the URL, runs `filterOrders(ORDERS, …)`, and forwards the filtered list + parsed filters to `OrdersPageClient`. Filter drawer mirrors the shop's exactly (mobile bottom card / desktop right drawer, same animations + chip primitives) with two axes — STATUS (multi: Delivered / On the way / Cancelled / Returned; `on-the-way` collapses pending+processing+dispatched) + PURCHASE DATE (single: All time / Last 30 days / Last 6 months / This year — Figma mobile 41:1420 + desktop 119:4406). EmptyOrders state when filters yield no rows. **Not yet wired to the backend** — swap `ORDERS` for `repairQuery("myAppListMyOrders", { filters })` once the customer-scoped order-list resolver lands on `orders.ts` (admin + detail readers already exist there).
- **Wishlist — `/account/wishlist`** — built end-to-end as presentational UI against `src/lib/mockWishlist.js`. Server component reads `?type=<csv>&price=<slug>` from the URL, runs `filterWishlist(WISHLIST, …)`, and forwards the filtered list + `totalCount` + parsed filters to `WishlistPageClient`. Card shape (mobile drop-shadow / desktop bordered + shadow) and split-by-variant pattern copied from OrderCard — Figma mobile 41:1613 + desktop 119:4743. Filter drawer also copied from OrderFilterDrawer with axes **TYPE** (multi, sourced from `FILTER_OPTIONS.types` in mockShop) + **PRICE** (single-select preset). **Both axes are invented — no Figma drawer spec for wishlist; swap when one lands.** EmptyWishlist branches "filters → zero rows" vs "wishlist is genuinely empty". **Not yet wired to the backend** — swap `WISHLIST` for `repairQuery("myAppListMyWishlist", { filters })` once the customer-scoped wishlist list resolver lands on `cart.ts` (toggle/clear primitives already exist there). Card heart button is presentational; wire it to `myAppToggleWishlist` when wiring.
- **Account hub — `/account`** — built end-to-end as presentational UI against `src/lib/mockAccount.js`. Single page that wraps three sections (Personal Information / Payment Methods / Address Information) and every interactive flow attached to them: star-toggle a row as default (radio semantics — at most one default per list); ADD MORE on Payment Methods opens `AddCardDrawer`; trash on a card opens `DeleteCardDrawer` (or fires the red `CannotDeleteDefaultBanner` if it's the default); successful add / delete each fire a dark slide-down toast (`CardAddedBanner` / `CardDeletedBanner`). Addresses mirror the same flows: ADD MORE / EDIT DETAILS open `AddAddressDrawer` (edit mode prefills from the structured `country`/`city`/etc. fields plus `label` + `kind`); trash opens `DeleteAddressDrawer` (or fires the red banner with `kind="address"` if default); `AddressAddedBanner` / `AddressDeletedBanner` confirm. The Address form includes an "Address Title" input + a 3-tile KindPicker (Home / Office / Other → `LocationIcon` pin) so customers can label rows freely. All state is local in `AccountClient.jsx`. **Not yet wired to the backend** — the address model already exists server-side under `addresses.ts`; the payment-methods backend is unresolved (Stripe-managed vs. local PCI-scoped table is still an open decision — see Gaps).
- **Contact us — `/contact`** — built end-to-end as presentational UI against Figma mobile 66:2449. Five-field form (first name, last name, email, country-code + phone, message) + black SEND MESSAGE CTA, hosted by a thin centered shell under `(customer)/` so it inherits ShopHeader + ShopFooter. Country picker (`CountryCodePicker.jsx`) drives a 245-country dial-code list from `src/lib/countryCodes.js` with search-by-name / dial / iso2 and emoji flags computed from regional-indicator codepoints; default is Jordan / +962. **Not yet wired to the backend** — submit is a `preventDefault` stub matching the auth-pages posture; wire to a future `myAppSendContactMessage` resolver (not yet on the server) once it lands. Reuse `countryCodes.js` for any other phone input (sign-up, account hub).
- **Cart — `/cart`** — built end-to-end as presentational UI against `src/lib/mockCart.js` (Figma mobile 83:5144 + desktop 119:5240). Single `CartPageClient.jsx` owns both breakpoints via per-section `variant: "mobile" | "desktop"` rendering paths. Seeded with `DEFAULT_APPLIED_PROMO = PROMO_CODES.SUMMER25` so the Order Total renders with the green discount pill and `JOD 192.86` total out of the box. Tax formula is **9% on `subtotal + shipping`** (not `subtotal + shipping − discount`) so the seeded numbers match Figma's `JOD 17.46` / `JOD 192.86` exactly. **Not yet wired to the backend** — swap `CART_ITEMS` / `calcTotals` for `repairQuery("myAppGetMyCart", …)` once the storefront cart-read resolver lands on `cart.ts` (toggle/add/remove primitives already exist there).
- **Checkout — Details `/checkout`** (step 2) — built end-to-end as presentational UI against `CHECKOUT_CONTACT` / `CHECKOUT_ADDRESSES` / `SHIPPING_METHODS` in `mockCart.js`. Mounted under (customer)/, inherits ShopHeader + ShopFooter. Contact form, shipping-address radio list (with Add More + per-card Edit Details on desktop; mobile shows the default address + ADD NEW CARD button), delivery-method radio list (Standard / Express / Pickup), free-shipping progress banner, promo + order totals reused from cart, sticky CTA on mobile. Continue routes to `/checkout/payment`. Heading **Edit**, heading **Add More**, per-card **EDIT DETAILS**, and the mobile **ADD NEW CARD** muted button all open the same `AddAddressDrawer` from `customer/account/` (edit-mode when given an `initial` row). **Not yet wired to the backend** — addresses model already exists server-side under `addresses.ts`; swap `addressList` to `repairQuery("myAppListMyAddresses")` once a customer-scoped read lands. Shipping-rate selection is local-state only; selected method is NOT yet plumbed into `calcTotals` (the cart's flat-fee shipping math still wins).
- **Checkout — Payment `/checkout/payment`** (step 3) — built end-to-end as presentational UI against `PAYMENT_METHODS` + `CHECKOUT_ADDRESSES` in `mockCart.js`. Payment-method radio list (Visa default + Mastercard + Apple Pay + Google Pay + Cash on Delivery; brand icons are inline SVG matching Figma — see `PaymentPageClient.jsx`), read-only shipping-address display with Edit affordance, T&C checkbox gating the CTA, and the right-column composite `PaymentOrderSummaryCard` on desktop / sticky CTA "PAY & CONFIRM ORDER" on mobile. **ADD NEW CARD** opens the reused `AddCardDrawer` from `customer/account/`; Shipping Address **Edit** opens the reused `AddAddressDrawer` in edit-mode pre-filled with the displayed address. `handleAddCard` appends + auto-selects the new card; `handleSaveAddress` rebuilds the flat `line` via `buildAddressLine`. The place-order CTA now routes to `/checkout/success` via `router.push`. **Not yet wired to the backend** — the payment-methods model is still pending (Stripe-managed vs. local PCI-scoped table — see Gaps); the navigation is a placeholder until the `myAppCheckout` mutation is wired through to gate success vs. `/checkout/failed`.
- **Checkout — Order confirmed `/checkout/success`** — built end-to-end as presentational UI against `CART_ITEMS` + `CHECKOUT_ADDRESSES` in `mockCart.js` plus hardcoded order metadata constants. Matches Figma mobile 85:8692 + desktop 119:6418. Owns `OrderSuccessClient.jsx`. Inherits ShopHeader + ShopFooter from `(customer)/layout.js`. CONTINUE SHOPPING routes to `/shop`; desktop TRACK ORDER routes to `/account/orders`. **Not yet wired to the backend** — the displayed order number / total / estimated delivery / payment-method label are seeded constants; swap to the response from `myAppCheckout` (resolver already exists in `orders.ts` and returns the created order + totals).
- **Checkout — Payment declined `/checkout/failed`** — built end-to-end as presentational UI against `CART_ITEMS` in `mockCart.js` plus hardcoded transaction-failure constants. Matches Figma mobile 111:3587 + desktop 119:6627. Owns `OrderFailedClient.jsx`. Inherits ShopHeader + ShopFooter. TRY ANOTHER METHOD and RETRY PAYMENT route to `/checkout/payment`; RETURN TO CART routes to `/cart`. **Not yet wired to the backend** — `myAppCheckout` doesn't currently distinguish payment-processor rejection from validation errors, so the failure branch is unreachable until a payment adapter lands. Wire by selecting between this page and `/checkout/success` based on the mutation's `payment_status` field.
- **Other storefront pages** — none remaining past auth + landing + shop + product + cart + checkout + checkout/payment + checkout/success + checkout/failed + account hub + orders + wishlist + contact.
- **Admin console** — scaffolded end-to-end as presentational UI under `(admin)/r3pr-console/*`, 12 pages backed by `src/lib/mockAdmin.js`. Dashboard (KPIs / line + donut charts / recent orders / top products / low stock), Categories (major+sub tree with reorder + visibility), Products (filter table + tabbed drawer with color×size inventory grid), Orders (status pipeline + detail drawer + transitions), Stock Alerts (pending notify-when-available requests; status filter + search + table + detail drawer with Mark as Notified / Dismiss actions), Contact Messages (status filter chips + search + table + detail drawer with status transitions + archive/delete), Reports (7 tabs × stat cards + charts, with PDF/CSV export buttons still presentational), Promo Codes (usage progress + drawer + generator), Users (4 roles, inline active toggle, drawer), Discounts (product/sub/major scope + validity window), Broadcast (composer + live preview + sent history), Settings (storefront-wide shipping/delivery rules — Free Delivery toggle + threshold, Express Shipping toggle + extra fee). The TopBar bell icon opens `NotificationPanel` — a live dropdown aggregating processing orders + unread messages + pending stock alerts + low-stock variants into 4 coloured sections with per-item links to the relevant page. **Fully responsive**: KPI grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`; chart + secondary-column rows stay stacked until `xl:grid-cols-3` because the donut needs ≥250px for its legend; KpiCard value uses `clamp(20px, 4.2vw, 28px)` so long currency strings never overflow. **Not yet wired to the backend** — every manager is a client component holding mock data in `useState`. Swap section-by-section to `repairQuery("myAppAdmin…", …)` once the client-side auth/mutation story lands; admin resolvers already exist server-side under `Server/servers/repair/src/graphql/resolvers/{adminCatalog,adminPricing,orders,reports,admin,auth}.ts`. The contact messages and stock alerts surfaces need new resolver files on the server (no `contact_messages` or `stock_alerts` tables or resolvers exist yet).
- **Accountant ledger — `/r3pr-ledger/overview`** — scaffolded as a single read-only Finance Overview page backed by `src/lib/mockFinance.js`. Forks the admin shell shape (`AccountantShell` + `SideNav` + `TopBar` under `src/components/accountant/layout/`) with a finance-only nav, a slimmer TopBar (no NotificationPanel — read-only role has nothing to action; replaced with a permanent `Read-only` chip), and a SideNav footer carrying a "Read-only access" info block above Sign Out. The dashboard owns the date-range filter (7D/30D/YTD/90D presets + custom From/To inputs that filter `FINANCE_DAILY` by the implicit-2026 day labels), a 5-KPI row (Total revenue / Total orders / Discount impact / AOV / Net revenue — each with delta vs prior period and a coloured top stripe), a Revenue-over-time LineChart with Compare toggle, a Category/Product/Time-period tabbed breakdown (category = horizontal bar + donut + share list; product = ranked DataTable with inline share-bars; time = day/week/month granularity toggle that calls `rollupBy` and renders both a bar chart and a per-bucket DataTable with orders/revenue/discount/net columns), and a Discount-impact-by-code DataTable below the tabs. Export PDF + Export CSV buttons match the admin Reports posture — still presentational (see Gaps for the export-decision note). **Not yet wired to the backend** — swap `FINANCE_DAILY` / `DISCOUNT_BY_CODE` / `REVENUE_BY_PRODUCT` for `repairQuery("myAppFinance…", { from, to })` calls when the finance resolvers land. `Server/servers/repair/src/graphql/resolvers/reports.ts` already returns aggregated JSON close to the shape mockFinance.js exposes — wiring is mostly renaming fields + gating by `hasRole("accounting")`. The existing `USERS` mock in `mockAdmin.js` already carries an `accounting` role; backend role-check helpers will need it added too (currently `hasRole` is checked against `"admin"` in adminCatalog/adminPricing — the finance resolvers should accept either `accounting` or `admin`).
- **Delivery surface** — not started. No client GraphQL client, no auth context, no role guards. Build as `(deliveryCompany)/<prefix>/...` with either the same `AdminShell`-style chrome or a domain-specific shell (the accountant role chose the fork-AdminShell path; do the same here if the delivery surface stays a thin read-update list, or build a different shell if the surface is more like a worker queue).

Treat anything past auth as greenfield UI work against an existing GraphQL surface. The auth pages exist and can be wired up to `myAppLogin` / `myAppSignUp` / `myAppGoogleAuth` / `myAppRequestPasswordReset` / `myAppConfirmPasswordReset` without redesigning — the forms already have the right fields. The next concrete step on the data side is picking the **client-side** GraphQL story + token storage + refresh-rotation strategy; do that before adding submit handlers, so all four forms share one path. (The server-side path is already settled — see `src/lib/repairApi.js`.)

Gaps worth flagging when planning work:
- The sign-up form **collects a phone number** (`PhoneInput` is wired into `sign-up/page.js`), but the backend's `myAppSignUp` is email/password only — it has no phone column on `users`. Either add a `phone` column + migration on the server, or drop the field from the form. Don't ship the wiring with the field silently discarded.
- The shop UI assumes the backend will eventually return a `salePrice` field per product (and a "Sale" major category). The current resolvers under `Server/servers/repair/src/graphql/resolvers/catalog.ts` don't surface a discounted price separately — products carry one `price` and discounts get applied at the cart/checkout boundary via `applyDiscount`. Decide whether the storefront list endpoint should pre-compute and return both `price` + `salePrice` per variant, or whether the frontend should compute it from the active discount window. The current mock matches the former shape.
- The shop UI's filter set is `sizes` / `colors` / `materials` / `types` / `priceMin..priceMax` (URL query params). There's no backend list resolver that accepts this filter shape yet — design it to match these param names so the frontend can switch over without renaming.
- The brief lists **report export as "document"**; the backend's `reports.ts` returns aggregated JSON. The admin Reports page renders presentational "Export PDF" / "Export CSV" buttons but they're no-ops — pick PDF (jsPDF / pdfmake / server-side render?) and CSV (client `Blob` is enough) before wiring.
- **Chart library: Recharts is installed** (`recharts` in `repair/package.json`). The SVG stand-ins in `Charts.jsx` have been replaced with Recharts-backed components. All charts now have hover tooltips, responsive containers, and auto-thinning X-axis labels for dense data. If the library is ever swapped, the change lives entirely in `Charts.jsx` — no other file imports from `recharts` directly. Mock data for 30-day series lives in `SALES_SERIES_30` and `SIGNUPS_SERIES_30` in `mockAdmin.js` (each row carries `current` + `prev` keys for comparison charts). The reports page PDF/CSV export is still a no-op — see the export-decision note above.
- **Payment methods has no server model yet.** The account hub renders payment-method rows from `mockAccount.js` (`{id, brand, last4, expiry, isDefault}`). Decide before wiring whether to (a) store payment methods as a local DB table (PCI-scoped) and surface customer-scoped CRUD resolvers, or (b) keep payment methods in Stripe (token-only references on the user row, all CRUD through Stripe APIs). The chosen shape determines what `myAppListMyPaymentMethods` / `myAppAddPaymentMethod` / `myAppDeletePaymentMethod` / `myAppSetDefaultPaymentMethod` return.
- **Admin → backend wiring is the next concrete data-layer step alongside the client-side auth/mutation story.** Every admin manager is currently a client component that holds mock data in `useState`. When wiring, choose whether each manager fetches its own slice or whether a higher-level layout provides a shared admin context — and pick the strategy once so all 11 surfaces look the same. `NotificationPanel` in `TopBar.jsx` currently pre-filters module-level constants; once real data lands it should derive counts from the same admin context or dedicated hooks.
- **Contact messages has no server model yet.** The admin `/r3pr-console/messages` page is fully presentational against `CONTACT_MESSAGES` in `mockAdmin.js`. The backend needs a `contact_messages` table (columns: `id`, `first_name`, `last_name`, `email`, `phone`, `message`, `status`, `created_at`), a migration, a model, and named resolvers (`myAppListContactMessages`, `myAppUpdateContactMessageStatus`, `myAppDeleteContactMessage`). The customer-facing `/contact` form also needs its `onSubmit` wired to a `myAppSendContactMessage` mutation once that resolver lands.
- **Stock alerts has no server model yet.** The admin `/r3pr-console/stock-alerts` page is fully presentational against `STOCK_ALERTS` in `mockAdmin.js`. The backend needs a `stock_alerts` table (columns: `id`, `user_id` FK → users, `product_variant_id` FK → productVariants, `status` enum `pending|notified|dismissed`, `created_at`), a migration, a model, and named resolvers (`myAppListStockAlerts`, `myAppUpdateStockAlertStatus`, `myAppDeleteStockAlert`). The storefront product detail page also needs a "Notify me when available" button that calls a `myAppRequestStockAlert` mutation when a selected variant is out of stock.
- **Finance ledger resolvers don't exist yet, and the `accounting` role has no role gate.** The accountant `/r3pr-ledger/overview` page reads from `mockFinance.js` only. The backend's `reports.ts` exposes 8 aggregated reports already, but none are scoped to a `from`/`to` window or partitioned the way the ledger page expects (daily revenue + orders + discount + net + prior-period comparison per row, plus discount-by-code aggregation). Likely shape: `myAppFinanceDailySeries({ from, to })` → `[{ day, revenue, prevRevenue, orders, prevOrders, discount, prevDiscount, net, prevNet }]`; `myAppFinanceDiscountByCode({ from, to })` → `[{ code, type, redemptions, impact, active }]`; `myAppFinanceRevenueByProduct({ from, to, limit })` → ranked product list; reuse the existing category/revenue aggregator for `myAppFinanceRevenueByCategory`. **Role gating note:** `repair`'s `hasRole(user, role)` helper currently gets called with `"admin"` across `adminCatalog.ts` / `adminPricing.ts` / `admin.ts`; the finance resolvers should accept `accounting` OR `admin` so the existing admin user can still view the ledger (otherwise admins get a 403 on a surface they need to verify in QA). The `accounting` value already exists on `users.role` per `USERS` in `mockAdmin.js` and the `myAppAdminCreateUser` path on the server.
- **Shipping settings has no server model yet.** The admin `/r3pr-console/settings` page is fully presentational against local `useState` (no mock data file yet). The backend needs a single-row `shipping_settings` table (columns: `free_delivery_enabled` bool, `free_delivery_threshold` decimal, `express_shipping_enabled` bool, `express_shipping_fee` decimal, plus the existing flat `delivery_fee` from the OrderManager accordion if that gets consolidated here), a migration, a model, and a `myAppGetShippingSettings` / `myAppUpdateShippingSettings` resolver pair gated by `hasRole("admin")`. The storefront's `mockCart.js` currently hardcodes `FREE_SHIPPING_THRESHOLD = 200 JOD` and `SHIPPING_FEE = 8 JOD` — once the settings resolver exists, both should read from it so admin changes propagate to the cart/checkout free-shipping banner and the Express row in `SHIPPING_METHODS`.
