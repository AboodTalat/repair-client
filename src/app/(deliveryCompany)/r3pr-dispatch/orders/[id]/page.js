"use client";

import OrderDetailPage from "@/components/delivery/orders/OrderDetailPage";

export default function DispatchOrderDetailPage({ params }) {
  return <OrderDetailPage params={params} />;
}
